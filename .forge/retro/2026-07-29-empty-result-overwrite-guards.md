# 회고 — 빈 결과 전면 덮어쓰기 가드 5곳 (task#242)

- **실행**: 2026-07-29 · 커밋 `0822d53` · **회고**: 2026-07-30 (fg-next all 자동 스킵 → 일괄 승급)
- **검증**: 신규 pytest 14건·전체 1411 green · 라이브 40단언 ALL PASS + 육안 4장 · 사용자 크롤 1회로 정상 경로 확정

## 이 작업의 핵심 학습은 이미 승급돼 있다

빈 결과 가드 계열(⭐ 항목, task#242·#243·#244)이 CLAUDE.md에 있다. 이 작업은 그 시리즈의 1번으로, "빈/all-None 결과를 캐시에 박제 금지"를 5곳에 적용했다.

## 이번 일괄 승급에서 새로 CLAUDE.md에 올린 것

**④ⓐ 보정 — 육안 확인의 가치가 *어디에 쏠리는지*를 명시.** (divergence 7)

④ⓐ는 "시각 변경은 프로브 PASS 후에도 스크린샷 1장을 완료기준에 넣을 것"이고, 그게 유일한 포착 수단이었던 사례가 이제 **3회**다(#235·#241·#247). 그런데 **이번 작업에선 육안이 새 결함을 찾지 못했다** — 프로브 PASS와 화면 정상이 일치했다. 차이는 명확하다: 육안이 프로브를 이긴 3회는 전부 **시각·레이아웃을 *바꾸는*** 변경이었고, 이번은 **백엔드 가드만** 바꿨다.

→ ④ⓐ에 한 줄 붙였다: **시각 표면을 건드리지 않는 변경에서 육안을 의식처럼 붙일 필요는 없다(반대로 시각을 건드리면 예외 없이 붙일 것).** 규칙을 약화시키는 게 아니라 **적용 범위를 날카롭게** 해 의식화를 막는다.

## 검토했으나 승급하지 않은 것

- **divergence 6 — 프로브의 `chart-ticks` 축이 첫 실행에 한 번도 발동하지 않았다.** 시장지표 섹션이 기본 **접힘**이라 `open` 분기가 전부 false였다. 섹션을 클릭해 펼친 뒤 재측정해 32→**40단언**으로 보강. 이미 ⑧ⓐ("커버리지를 출력하라 — 발동 안 한 단언은 측정 실패")가 덮는다. 부수 사실: 회고 `2026-07-19-market-sections-default-open.md`의 "기본 펼침"은 **이 화면엔 적용돼 있지 않다**(현재 접힘이 기본) — 프로브를 쓸 때 참고.
- **divergence 4 — "선제적 전수 grep을 작업량으로 세지 말 것"이 *반환타입 변경*에도 적용됐다.** `save_guru_managers`를 `-> bool`로 바꿀 때 계획은 patch 3곳 갱신을 경고했지만 실제로는 불필요했다(두 테스트가 `scrape_all_managers`를 `[]`로 patch하므로 falsy 반환이 오히려 정확한 mock). task#230·231·232 가토의 4번째 관측. 관측 횟수 갱신·범위 확장은 이번 승급에서 사용자가 선택하지 않았다(재발 시 후보).
- **divergence 2 — 계획의 지시가 실행 불가였고 착수 시 역산으로 보강했다.** 계획은 두 호출부에 `{len(managers)}/{total}`을 실으라 했지만 **`total`은 호출부에 없다**(`scrape_all_managers`가 `on_progress` 콜백으로만 노출하고 `jobs.py`는 콜백을 안 넘긴다). 스크레이퍼 말미 1줄이 배치·수동 **두 경로를 모두** 덮는다. ⑥이 이미 지시하는 절차의 성공 사례.
- **divergence 5 — 국채의 클로버 리스크가 계획 기술보다 좁았다.** `get_treasury`엔 이미 per-key 저장 폴백이 있어 `_raw_histories`가 있으면 fetch 전멸에도 `rates`가 비지 않는다. 실제 빈은 legacy 행이나 최초 fetch뿐. 가드는 그 legacy 경로에서 유효.
- **TDD red가 의미 있었다** — `Expected 'execute'/'_mc_save' to not have been called. Called 1 times.`로 **옛 구현이 실제로 빈 값을 저장함을 관측**했다. 회고 #234 ④ 준수(이미 CLAUDE.md에 있는 규칙의 성공 사례).

## 후속 후보 (미해소)

- **부분 실패 임계값** — 이번엔 관측만(성공률 로그 `[Guru] 수집 83/83`). 로그가 쌓이면 "83명 중 N명 미만이면 저장 생략" 도입 여부 판단.
- **`.forge/codebase/CONCERNS.md` G1(HIGH) 해소 반영** — 무가드 5곳이 0이 됐으므로 지도가 stale. fg-map 후보.

## 문서 갱신

- **CLAUDE.md ④ⓐ 보정**(육안 확인의 적용 범위 — 시각을 바꾸는 변경에 쏠린다 + 유일 포착 3회로 갱신).
- 빈 결과 가드 계열은 ⭐ 항목으로 이미 승급됨.
- ADR·CONTEXT 추가 없음.
