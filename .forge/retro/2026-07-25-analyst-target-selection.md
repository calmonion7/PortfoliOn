# 2026-07-25 — 애널리스트 리포트 대상 지정 + 즉시 발행 버튼

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: `tickers.analyst_target`(schema+`_migrate` 쌍), admin PUT, 루틴 프롬프트 대상 규칙 교체, 상세 헤더 토글·발행 버튼. 라이브에서 fire → **~2분에 035420 발행 201 도착**.
- 이탈: **admin 2엔드포인트 게이트를 `require_admin` → `require_admin_or_api_key`로 확장** — admin 계정 크레덴셜 없이는 라이브 positive UAT가 불가능했기 때문(Cowork-facing 쓰기의 기존 컨벤션과도 일치, UI 동작 무변).

## 학습
- **admin 전용 표면은 테스트 계정(비admin)으로 라이브 검증이 원리적으로 불가하다.** 계획 단계에서 이 제약을 인지하고 셋 중 하나를 미리 고를 것: ① 게이트를 `require_admin_or_api_key`로 열어 API로 positive 검증(이번 선택 — Cowork 쓰기 컨벤션과 일치할 때만) ② vitest + 기능경로 API 검증으로 대체하고 **버튼 렌더는 사용자 화면 확인으로 넘김** ③ admin 크레덴셜을 사람에게서 받음. 이걸 안 정하면 UAT 단계에서 막혀 계획을 되돌린다(#215·#222·#224에서 반복됨).
- 즉시 발행처럼 **비동기 외부 파이프라인을 트리거하는 버튼은 "지시 도착"과 "결과 도착"을 분리해 검증**해야 한다(fire 201 → ~2분 후 발행물 확인).

## 문서 갱신
- **CLAUDE.md Gotchas 승급**: admin 표면 UAT 제약과 3선택지(사용자 승인, 일괄 승급 2026-07-28).
