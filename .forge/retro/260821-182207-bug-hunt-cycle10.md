# 2026-08-21 — 10차 버그 헌트: 비목표의 *전제*를 재는 렌즈를 두었고 그 전제가 깨진 사이클

fg-loop 드라이브가 자동 스킵한 회고의 **유예 승급**(batch promotion). 봉인 아카이브 `.forge/done/260821-175716-bug-hunt-cycle10/`.

## 계획 대비 실제

- **계획대로 간 것**
  - SHA 고정 범위 준수(`b93afce..0262e39`) · 프로덕션 코드 diff **0**(변경 파일은 `CONCERNS.md` 단 1개) · S0 다섯 값 중 **넷이 계획서와 정확히 일치**(191파일 분류 39·22·43·26·54·7 · §0 열린 건 29 · B번호 최대 B63 · 라이브 번들 == 로컬 `dist` 5/5).
  - 12렌즈 전부 findings + `scanned` **목록** 반환(**렌즈 사망 0**) · C 판정 31건 **누락 0 · 판정불가 0**.
  - **검증 절단 0 · 미검증 0 · 최종 검증기 사망 0** · 정지조건 5축 전부 PASS.
  - 하드 제약 **위반 0** — git 되돌리기 0 · 프론트 빌드 0 · 서브에이전트 commit/push 0 · 라이브 쓰기 0 · DB 쓰기 0. 7·8·9차 처방이 **4연속 실효**.
  - **A4 렌즈(비목표의 전제를 재는 렌즈)가 설계 의도대로 작동했다** — 「프론트 시각 렌즈를 빼도 된다(프로브가 덮었다)」의 근거를 사냥 대상으로 만들었더니 그 전제가 **부분적으로 거짓**임이 드러났다(B78 `uat282` 13축이 라이브 미측정 · B79 `uat298` 거짓 FAIL 24건). 이번 사이클의 방법론적 주된 산출이다.
  - **B3이 「3사이클째 이월」을 끊었다** — §8.1 인메모리 캐시 스레드 안전성이 2사이클 연속 「도구 범위 밖」이었던 것을 `threading.Barrier` 하니스로 실제 재현해 판정했다(B69 + 파생 B68·B77). **비용은 몇 줄이었다.**
  - **4경로 보존 장치가 처음으로 실전 발동** — 검증 6에이전트가 세션 한도로 전멸했는데 17건 전부 `verdict: null`로 생존했다. 7차에 `filter(Boolean)`이 최고 심각도 클러스터를 증발시킨 그 사고가 재발하지 않았다.

- **이탈**
  1. **C 판정 분모는 32가 아니라 31.** 계획이 「§0 29건 + 대조군 3건」으로 적었으나 `B54`는 29건 **안에 이미 있는 행**이라 32는 이중 계수다. 실분모 = 29 + 표에서 제거된 `B60`·`B61` = 31.
  2. **HEAD가 고정 범위를 4커밋 초과한 상태로 착수.** 계획 범위 끝은 `0262e39`인데 HEAD는 `f7e01cf`(task#324). SHA 고정을 지켜 넓히지 않았고 그 대가로 **프로덕션·스크립트 17파일이 사각**으로 남았다.
  3. **⚠️ 블라인드 설계가 계획 수준에서 깨져 있었다.** 프롬프트는 §0 미열람을 못박았고 렌즈들이 실제로 지켰는데(`scanned`에 「§0[33-98] 의도적으로 미열람」 기록), **계획서의 「볼 것」이 §0의 `B9`(프론트 refresh 경로 부재)·`B62`(억원 기본값 폴백)를 실질적으로 복창**했다. 완료기준 ⑥이 요구한 확인의 결과가 「깨짐」이므로 교차 지표를 **무효 표시**했다. → **승급함**.
  4. **dedup 에이전트의 교차지표가 두 군데 틀려 메인 세션이 정정.** ⓐ 분모: 「열림 건의 파일이 *findings의 `file` 필드*에 있는지」 프록시로 **3**을 냈으나(자기 한계를 스스로 명시했다), `scanned` 목록으로 재계산하면 **18**이다 — 프록시가 분모를 **6배 과소평가**해 지표를 실제보다 좋게 보이게 했다. ⓑ 분자: `B4-1`을 `§0 B62`의 재발견으로 보아 1을 냈으나 **오판**이다. `_table_unit` 6줄 안에 결함이 **둘**이다(B62 = 캡션을 *못 찾아* 억원 가정 / B64 = `find_previous`가 거리 제한 없이 전역 탐색해 *엉뚱한 캡션을 주움*). 도달조건도 증상도 다르므로 **B4-1은 신규 확정분이고 분자는 0**이다. 최종: 파일 재접촉 18/28 · 독립 재발견 **0/18**.
  5. **검증 6에이전트 1차 전멸**(`session limit`). 계측 실패이고 판정 실패가 아니므로 유계 재시도 1회(`resumeFromRunId`)로 회복 — 12렌즈+dedup은 캐시 replay, 죽은 6개만 라이브.
  6. **워크플로우가 세션 권고(15)를 초과 — 19에이전트.** 계획이 「절단 0을 지키고 초과를 기록」으로 미리 정해 뒀다(9차도 19).
  7. **eco가 켜진 상태로 돌아 전 서브에이전트가 `sonnet`으로 캡됐다.** 품질 저하 징후는 없었다 — 대조군 3/3 통과 · 17건 거의 전부가 실제 재현(`.venv` monkeypatch · `threading.Barrier` · postgres 조회 · 기존 산출물 집계) · 검증기가 **스스로 4건 심각도 하향** + 1건 정확 기각. 설계상 가장 약한 고리(검증기 판정)에 **메인 세션(opus) 전건 재확인**이 걸려 있었고 그 재확인이 실제로 dedup 오판 2건과 심각도 4건을 정정했다.
  8. **완료기준 ③(배정 목록↔「볼 것」 1:1 대조)이 실제로는 수행되지 않았다.** A4에 배정된 「신규·변경 테스트 43」에 볼 이유가 주어지지 않아 **테스트 24개가 미열람**으로 남았다(A 커버리지 82/130 = 63%). **9차 학습 2의 직접 재발**이며, 계획이 그것을 완료기준으로 옮겨 적었음에도 재발했다. → **승급함**.
  9. **내가 앵커 규율 회귀를 만들었고 계획의 그 체크가 잡았다.** §0에 새 행 16개를 쓰며 `파일:줄번호` 포인터를 **7건** 넣어 baseline 0을 깨뜨렸다(task#290이 구조적으로 없앤 클래스). 그 grep이 즉시 잡아 전부 심볼 앵커로 되돌렸다. **두 번째 층이 더 교훈적이다** — 7건을 고친 뒤 grep은 0을 냈지만 `(:42)`·`:64` 같은 **bare `:NNN` 6건**이 남아 있었고 그 형태는 같은 이유로 드리프트하는데 그 grep이 원리적으로 못 본다. HEAD 기준 bare `:NNN`도 0이었으므로(이 파일의 실제 관례는 「어떤 형태의 줄번호 포인터도 0」) 그것까지 제거해 baseline과 정확히 맞췄다.
  10. **메인 세션의 계측 도구가 「단언 0」을 냈다.** A4-2 근거를 검증하려 `screenshots-uat298/result.json`을 집계했는데 내 스크립트가 `{ok,name}` 형태를 가정해 0을 냈다(실제 키는 `{msg,ok,tag}`). **그 0을 「검증기가 틀렸다」로 읽지 않고 내 계측 실패를 먼저 의심한 것이 맞았다** — 재집계 결과 검증기 주장대로 284건 중 FAIL 24건이었다.

## 학습

- **다음에 다르게 할 것**
  1. **블라인드를 재려면 「볼 것」을 결함 *카탈로그*가 아니라 가토의 결함 *클래스*에서만 파생시킬 것.** 이탈 3. 블라인드와 「배정 파일을 볼 *이유* 주기」(9차 학습 2)는 **직접 충돌하는 목표**이고, 10차 계획은 그 충돌을 인지하지 못한 채 둘 다 요구했다. 클래스는 일반형이라 특정 항목을 지목하지 않으므로 둘을 동시에 만족시킬 수 있다. **(승급 — CLAUDE.md)**
  2. **완료기준에 적는 것만으로는 부족하다 — 그 대조를 *실행하는 단계*가 슬라이스로 있어야 한다.** 이탈 8. task#285가 확립한 「회고 처방을 완료기준에 넣어라」는 **필요조건이지 충분조건이 아니다**. 「~를 1:1 대조한다」처럼 *행위*를 요구하는 완료기준은 그 행위를 수행하는 슬라이스가 없으면 선언으로 남는다. 반면 task#285·#290에서 실효했던 것들은 전부 **한 줄 명령**(`grep …`)이었다 — 즉 완료기준은 **그 자리에서 실행 가능한 명령**이거나, 아니면 **별도 슬라이스**여야 한다. **(승급 — CLAUDE.md)**
  3. **다음 사이클의 범위 시작점은 `0262e39`다.** 이탈 2. `f7e01cf`로 시작하면 이번에 사각으로 남긴 17파일(`App.jsx`·`components/reports/*` 6종·`PlayerTable.jsx`·`navSections.js`·`pages/{AnalystReport,Reports}.jsx`·`AnalystReportsRoute.jsx`·`scripts/` 5종)이 **영구 사각**이 된다. (미승급 — 사냥 범위 한정)
  4. **수렴을 지표로 쓰지 말 것 — 커버리지가 더 정직하다.** 공유 표면 5사이클(1·0·2·0·0)에 이어 대체 지표인 블라인드 교차 재발견도 **0/18**로 같은 결론에 도달했다. 원인이 확인됐다 — **렌즈의 스탠스가 무엇을 볼지 결정하므로 같은 파일을 배정해도 수렴이 만들어지지 않는다**(실측: `routers/auth.py`를 B1과 C3가 함께 봤으나 B1은 `ALL_MENUS` 드리프트·GitHub OAuth 크래시를, C3는 `SESSION_SECRET` 임포트타임 폴백·레이트리밋 부재를 지목 — **파일은 겹쳤고 결함은 겹치지 않았다**). 반면 커버리지는 실제로 작동해 테스트 24개 미열람을 잡아냈다. (미승급 — 사냥 방법론 한정. ADR 신설도 사용자 판단으로 보류: 되돌리기 쉽고 이 회고 + 리포트 「공유 표면 폐지 사유」에 근거가 남아 다음 계획이 읽는다)
  5. **지표 산출을 에이전트에 위임할 땐 분모를 만들 데이터를 함께 줄 것.** 이탈 4ⓐ. 안 주면 에이전트는 **프록시를 발명**하고, 그 프록시는 **관측 가능한 것만 세므로 낙관적**이다(1/3 = 33% vs 실제 0/18 = 0%). 이번엔 `scanned` 목록이 반환값에 있었는데도 dedup 프롬프트에 넘기지 않았다. (미승급 — 사용자 판단. `subagent-orchestration` 스킬 쪽이 더 맞을 수 있어 다음 워크플로우 설계 시 재검토)
  6. **감사 baseline은 「0이면 통과」가 아니라 「HEAD와 같으면 통과」로 쓸 것.** 이탈 9. 전자는 내가 무엇을 늘렸는지를 드러내지 못하고, 좁은 패턴을 통과시키면 같은 클래스의 다른 표현형이 살아남는다. (미승급 — 사용자 판단. 기존 「감사 패턴을 좁히면 그 감사는 통과해도 무의미하다」의 N번째 사례이자 자기적용 사례로 이 회고에 남긴다)
  7. **eco(sonnet 캡)는 버그 헌트에 쓸 수 있다 — 단 약한 고리에 opus 재확인이 걸려 있을 때만.** 이탈 7. 다음에 헌트를 eco로 돌릴 때도 「CONFIRMED 전건 + 「닫힘」 판정을 메인 세션이 재확인」 구조를 반드시 유지할 것. 그 재확인이 이번에 실제로 6건을 정정했다. (미승급 — 관측)

- **확인된 것 (keep)**
  - **대조군 블라인드 투입 + 메인 세션 독립 판독**의 조합이 판정 레인의 이빨을 확실히 증명했다(3/3 × 2방향). 프롬프트에 「`CONCERNS.md`가 「닫힘」이라고 *말하는 것*은 근거가 아니다 — 코드를 열어라」를 못박은 것이 효과가 있었다(C 레인이 실제로 `next((r for r in rows if r.get("title")==title), None)` 같은 식을 인용해 왔다).
  - **9차 학습 5의 처방이 맞았다** — 「직전 수정 커밋이 아니라 오래 안 본 서브시스템으로 렌즈를 돌려라」. CONFIRMED 16건 중 **12건이 B 레인**이고 **HIGH 2건이 둘 다 B 레인**이다(B1 인증 · B4 추천). 신규 코드에는 결함이 적고 오래 안 본 코드에 몰려 있다. REFUTED도 4 → 1로 줄었다(9차의 REFUTED 4건 전부가 자기참조 범위 탓 「기록된 트레이드오프의 재발견」이었다).
  - **4경로 보존과 유계 재시도**를 계획에 미리 박아 둔 것이 세션 한도 전멸을 무해하게 만들었다. 「계측 실패를 판정 실패로 읽지 말 것」이 이번에 **두 번**(에이전트 전멸 · 내 집계 스크립트) 값을 했다.

## 문서 갱신

- **CONTEXT.md 승급: 없음** — 신규 도메인 용어가 나오지 않았다(6·7·8·9차와 동일. 헌트는 용어를 만들지 않는다).
- **ADR 신설: 없음** — 「수렴 지표 폐지」를 후보로 올렸으나 되돌리기 쉽고 이 회고 + 리포트에 근거가 남아 다음 계획이 읽으므로 사용자 판단으로 보류.
- **CLAUDE.md 가토 승급: 2건**
  1. **블라인드 ↔ 「볼 이유」 충돌** (신규 항목) — 블라인드는 「그 문서를 못 읽게 하는 것」이 아니라 「그 내용이 어떤 경로로도 닿지 않게 하는 것」이고, 「볼 것」을 결함 카탈로그에서 파생시키면 원리적으로 성립하지 않는다.
  2. **완료기준↔실행 슬라이스** (기존 task#285·#290 항목 보강) — 「완료기준에 넣어라」는 필요조건이지 충분조건이 아니다. *행위*를 요구하는 완료기준은 그것을 수행하는 슬라이스가 없으면 선언으로 남는다.
- 산출물: `.forge/bug-report.md`(10차, untracked) · `CONCERNS §0`에 **B64~B79** 16건 추가 + `§13.2` 재판정 마커(커밋 `be2d0bd`).

## 후속 큐 (리포트 우선순위)

1. **B72** GitHub OAuth 콜백 무가드 크래시 (HIGH — 지금 사용자가 밟는다)
2. **B65** 추천 배치 커버리지 임계 부재 (HIGH)
3. **B79 + B78** 프로브 모드 게이트 2건 (가장 값싸고 레버리지 큼 — 한 슬라이스로)
4. **B64 + B62** `_table_unit` 두 실패 경로 (같은 함수이므로 반드시 함께)
5. **B76** watchlist 실패 → 오추천 · **B77** `ProgressTracker` 전역 싱글턴
6. **B66 + B67** 키움 `close_price` 실패→0 (형제 복제, 함께)
7. 나머지 LOW · **9차 확정 미수정 9건**(C 레인이 전건 생존 확인 — 닫힌 것 0)
8. 부수: `STRUCTURE.md`의 `pages/ (24 jsx)` 실측 44 불일치(B59) → **`fg-map` 재실행**의 일 · R1의 정밀 귀속을 `§4.2`에 한 줄 흡수
