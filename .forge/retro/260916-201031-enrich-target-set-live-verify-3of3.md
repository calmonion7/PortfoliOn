# 2026-09-16 — 라이브 검증 + ADR 이행 절 추기 (task#356, fg-loop 일괄 승급)

## 계획 대비 실제
- 계획대로 된 것: 라이브 스모크 6/6(부작용 0 — fresh 종목만 호출), `MATCH n=17`, ADR 이행 절 추기, CONTEXT 3용어 무수정 일치, 서빙 번들 == 로컬 dist.
- 어긋난 것: 최종 정지조건 실행에서 **독립 Bash 3개를 한 턴에 보내자 cwd가 `frontend/`로 남아** C5 grep이 6파일 전부 「no such file」, C6는 `MODULE_NOT_FOUND` — 그런데 `| tail -1`이 파이프라인 exit를 0으로 만들어 `exit=0`이 찍혔다. 「계측 실패를 판정으로 오독」 가족의 재확인(CLAUDE.md task#318 ⓒ 「`| grep`은 exit 코드를 삼킨다」와 동일 형태). 절대경로 + `out=$(cmd); ec=$?`로 재실행해 잡았다.

## 학습
- 다음에 다르게: 정지조건·감사 명령은 **항상 `cd <절대 프로젝트 루트> &&`로 시작**하고, 병렬 Bash 호출 뒤에는 cwd를 신뢰하지 말 것(환경 업데이트 메시지가 cwd 이동을 알린다). 판정에 쓰는 명령의 exit는 파이프 앞에서 따로 받는다.
- 양성 관측: fg-loop의 「기존 게이트가 이미 통과하는가(C4)」·「목표값이 도달 가능한가(C5 ADR 축을 task#356이 담당)」를 착수 시 명시해 둔 덕에 C5의 「ADR task#354 0」이 fix-forward가 아니라 멤버 태스크 몫임을 원장에 적을 수 있었다 — replan 0회.

## 문서 갱신
- CONTEXT.md 승급: 없음
- ADR 추가: 없음(ADR 260916-132605 「이행」 절은 task 본문에서 추기)
