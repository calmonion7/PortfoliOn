# 2026-09-16 — 루틴 API 키 로그 노출 차단 — 프롬프트 리터럴 → 자식 env 주입 (task#349, fg-loop 일괄 승급)

## 계획 대비 실제
- 계획대로 된 것: 리스너 env 주입 + run.log 0600(red 5 → green 22), 프롬프트 리터럴 1→0, 기존 로그 192개 chmod(삭제 0), 라이브 muse 로그 키 0건(대조군 옛 로그 7~9건).
- 어긋난 것:
  - `test_api_key_goes_to_stdin_not_argv`가 `FAKE_KEY in stdin`을 단언해 정면 충돌 → task#254 계획을 읽어 「기록된 결정은 argv 노출 제거」·「프롬프트 유지는 비목표에 없음」으로 판별해 뒤집되, 그 단언이 증명하던 성질(키가 자식에 실제 전달됨)의 **대체 단언(env)**을 같은 커밋에 넣었다(없으면 키를 통째로 빠뜨려도 초록).
  - 배포를 20:42 야간 루틴 완료(20:52) 뒤로 미뤘다 — `deploy.sh`가 backend·nginx를 재기동해 루틴의 API 호출을 끊기 때문. 리스너는 작업트리 스크립트를 읽어 배포 없이도 수정이 즉시 유효했다. 미커밋 편집의 폴러 소실은 `LOCAL == origin/main`으로 관리.
  - 정지조건 「run.log 총계 == 191」이 야간 루틴의 정상 런 하나로 192가 되어 도달 불가 → 「chmod 직전·직후 비교」로 등가 정정(CLAUDE.md task#335ⓑ 「세션 상수 baseline」의 실측 사례).
  - 증거가 기대보다 강했다: 새 로그에 변수 **이름**이 11회 에코됐는데 값은 0건 — 「에코를 막았다」가 아니라 **「에코해도 안전하다」**는 근본 차단.

## 학습
- 다음에 다르게:
  - 시크릿은 프롬프트·인자에 싣지 말고 자식 프로세스 env로 — 출력 억제(증상)가 아니라 값 부재(근본)로 막는다. 검증은 「에코가 살아 있는 로그에서 값 0건」으로 하면 이빨이 선다.
  - 라이브 루틴이 돌고 있으면 배포(push)를 그 완료 뒤로 미룬다 — 이번엔 지켰다(#350은 못 지켰다). 리스너처럼 작업트리를 직접 읽는 구성요소는 배포 없이도 수정이 유효함을 기억.
  - 뒤집는 단언이 지키던 성질을 한 줄 적고 대체 단언을 같은 커밋에(CLAUDE.md task#336 세 번째 분기 — 이 태스크가 그 실측이다).
- **미결(사용자 결정, 안전벽 클래스)**: 이미 노출된 로그 191개와 `~/CoWork/PortPoilOn/portfolion-stock-enrich.skill`의 평문 키는 이 수정으로 무효화되지 않는다 → **`COWORK_API_KEY` 회전 권고**가 아직 열려 있다(로그는 0600으로 잠갔고 삭제하지 않음).

## 문서 갱신
- CONTEXT.md 승급: 없음
- ADR 추가: 없음
