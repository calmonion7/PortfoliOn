# 2026-08-13 — AI 데이터센터 설비·운영 2종 신규 발행 (task#302)

> fg-loop 드라이브가 자동 스킵한 회고를 일괄 승급(batch promotion 2026-08-13). 원재료는 봉인 아카이브
> `.forge/done/260813-092529-ai-datacenter-report-publish-2of2/run.md`. 코드 변경 0인 운영 태스크.

## 계획 대비 실제

- **계획대로 된 것**:
  - 2종 순차 fire **각 1회 성공**(재fire 0, 중복 발행 0). 설비 08:33:57→08:50:32(~16.5분),
    운영 08:51:29→09:13:43(~22분). 계획의 「slug당 10~15분」 추정보다 길었지만 순차 감독이라 무해했다.
  - **품질이 프롬프트 지시를 그대로 따랐다.** 설비 25업체·7섹터, 운영 22업체·5섹터, `category` 둘 다 100%,
    섹터명이 task#301 S5가 못박은 권장 목록과 **정확히 일치**(이탈 0). `share_basis`도 「각 섹터 내 …」
    형태로 기준을 나열했고, 두 문서가 서로를 `related.complements`에 등재했다. 계획이 대비한
    「섹터 분류가 어긋나면 프롬프트 보강 후속」 분기는 발동하지 않았다.
  - 422 스키마 함정 없음 — task#297이 넣은 교차필드 방어를 새 구조가 그대로 통과했다.

- **이탈**:
  1. **⭐ 판정 3분할이 실제로 값을 했다.** `~/portfolion-routine-runs/<ts>/run.log`는 발행이 끝날 때까지
     **0바이트**였다(`claude -p`는 스트리밍이 아니라 완료 시 1회 출력). 계획대로 **생존=프로세스 ·
     완료=DB 행 · 실패원인=로그(끝난 뒤에만)** 로만 판정해 재fire 유혹을 원천 차단했다. 로그 크기를 진행
     신호로 썼다면 16분·22분 두 번 다 「실패」로 오독해 같은 slug에 중복 발행을 쐈을 것이다.
     (task#300에서 승급된 가토의 2번째 관측 — **양성 사례**.)
  2. **⚠️ 내 임시 캡처 스크립트가 상세 경로를 `/tech-reports/{slug}`(복수)로 써서 6판 전부 「소제목 0 ·
     업체행 0」을 냈다.** 로그인·마스트헤드는 페이지와 독립이라 정상 렌더돼 **0이 앱 결함처럼 보였다**
     (가토 ⑧ⓘ의 역방향 — 저쪽은 틀린 대상 위에서 PASS, 이번은 틀린 대상 위에서 FAIL). 직전에 정식
     `uat301`이 같은 라이브에서 소제목 7·업체행 25를 측정한 상태였기에 앱이 아니라 **계측기를 먼저 의심**했고,
     `uat301`의 관용구를 읽어 실제 경로가 **`/tech-report/{slug}`(단수)**임을 확인해 정정했다. 정정 후 6판
     전부 정합(ops 5/22 · smr 5/11 · robotics 3/13 · reusable-rocket 3/9 · solid-state 2/12 — 전부 API
     `players` 수와 일치).
  3. **301에서 「도달 불가」로 게이트에서 뺀 축이 실데이터로 처음 성립했다.** task#301 착수 시 share 보유
     두 판이 둘 다 단일 카테고리여서 「점유율 ≥2 축 그룹」을 게이트에서 빼고 **커버리지 출력로만** 남겼는데,
     설비 판이 share를 **5개 섹터**(가속기 2 · 메모리 3 · 서버·랙 2 · 네트워크 3 · 냉각 1)에 실어 다중 그룹
     렌더가 처음 실측·육안 확인됐다. **축을 삭제하지 않은 설계가 정확히 값을 했다**(가토 ⑧ⓝ) — 삭제했다면
     이 데이터가 와도 아무도 재지 않았을 것이다.
  4. **`uat301` 단언 수가 128 → 112로 줄었다(측정 실패 아님).** 이전엔 SHARE_TARGET이 별도 페이지라 방문이
     8회였는데, 설비 판이 표·점유율을 모두 갖춰 SHARE_TARGET == PLAYER_TARGET이 되며 4회로 합쳐졌다
     (`console`·`page`·`page-h-scroll` 8→4, `visual-share-empty-label`은 라벨이 생겨 정의역 소멸). 반대로
     커버리지는 1924 → **2596**으로 늘었다(판이 25업체로 커짐). ⑧ⓑ대로 총계 변동의 원인을 확인한 결과
     **대상 변화**로 확정.
  5. **신규 데이터가 기존 프로브에 새 FAIL을 만들지 않았다.** `uat299` 175/175(단언 123→175, 판 증가로 설명)
     · `uat296` 468/468 · `uat280` 10 FAIL(전부 `clip` ×8 + `kpi-visible` ×2, 대상은 기존 smr·reusable-rocket
     뿐 — 신규 2판 기여 0). 데이터만 바뀌는 태스크도 C6을 다시 재야 한다는 판단이 옳았다.

## 배운 것

- **다음에 다르게 할 것**:
  - **일회용 계측 스크립트는 정식 하니스의 *라우트와 대기 관용구*를 복사할 것.** 특히 이 앱은 목록이
    `/tech-reports`(복수), 상세가 `/tech-report/{slug}`(단수)로 갈린다 — 추정하면 목록 경로의 미지 하위
    경로로 들어가 본문이 빈 채 마스트헤드만 렌더된다. 그리고 **직전에 정식 프로브가 같은 대상을 정상
    측정했다면 0은 앱이 아니라 계측기 결함이다** — 이 대조가 오귀속을 막았다.
  - **데이터만 바뀌는 운영 태스크도 회귀 축을 다시 잴 것.** 코드 변경 0이어도 기존 프로브가 새 판을
    순회하므로 새 FAIL이 날 수 있다(이번엔 0이었지만, 그 0을 *확인*한 것이 결론이다).
  - **커버리지 총계가 늘어도 단언 수는 줄 수 있다** — 방문 수와 표본 크기가 반대로 움직이는 경우다.
    둘을 함께 출력하지 않으면 어느 쪽 변화인지 갈리지 않는다.

- **양성 사례 2건**: 비동기 잡 3분할 판정(로그 0바이트를 실패로 읽지 않음 — 중복 프로덕션 쓰기 차단) ·
  도달 불가 축을 커버리지로 남긴 설계가 후속 데이터로 자동 성립.

## 승급하지 않은 것 (여기 남긴다)

- **로그 0바이트 3분할 판정** — task#300에서 이미 CLAUDE.md에 승급됐고 이번은 2번째 관측(양성).
- **도달 불가 축을 삭제하지 말고 커버리지로 남겨라** — 가토 ⑧ⓝ에 원리가 있어 중복 승급을 피한다.
  다만 「*게이트에서 뺀* 축」이라는 등장 맥락은 새로우니 3번째 관측이 나오면 승급 검토.
- **일회용 스크립트의 라우트 복사** — 가토 ⑧ⓘ 계열의 프로브 저작 관행이라 회고에만.
- **fire 소요가 계획 추정(10~15분)보다 길었다(16.5·22분)** — 순차 감독이라 무해했고 조치 없음.

## 문서 갱신

- **CLAUDE.md 승급: 없음** (이 태스크 몫). task#301 회고에서 3건 승급.
- **ADR 신설: 없음.**
- **CONTEXT.md 승급: 없음.**

## 후속

없음. 발행 품질이 프롬프트 권장 목록과 이탈 0이라 계획이 대비한 「프롬프트 보강 후속」은 불필요하다.
선재 결함 6종은 task#301 회고의 「후속」에 기록돼 있다.
