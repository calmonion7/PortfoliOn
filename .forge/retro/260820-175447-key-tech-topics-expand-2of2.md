# 2026-08-20 — 주요기술 15종 확장 (2/2) 발행 파이프라인 검증 (task#311)

fg-loop 드라이브 2번째 멤버 · 커밋 `406f6d5` · replan 0회 · **워크플로우 미사용**(검증·문서 위주)

## 계획 대 현실

- **계획대로 된 것**: 프로브 3축 ALL PASS(uat-tech-anatomy 235 · uat299 199 · uat301 121), 목록 카드
  **7장 == API 행 7종**(3폭 전부)으로 카드가 데이터에서 파생됨을 못박았다. 신규 프로브
  `uat311-tech15-visual`(40단언)을 남겼다.

- **이탈 3건**:

  1. **⭐ 계획의 예고 ②가 거짓이었다.** 목표문이 *"이 태스크에서 처음 관측되는 것 둘: ① 카드 7장 이상
     ② **「해부 미작성」 분기가 라이브에서 처음 활성화**"*라고 적었는데, 루틴이 신규 종을
     `composition`과 **함께** 발행해 그 분기는 **여전히 dormant**다(해부 보유 7 / 발행물 7).
     task#309가 *"6종 전부 해부 보유라 dormant"*라고 적은 자리가 그대로다.
     → 프로브가 그 축을 「통과」가 아니라 **「미검증」**으로 단언문에 실어 기록했다.
  2. **미작성 안내의 *하위 문구가 둘*이라는 것을 구별해야 했다**(`TechAnatomy.jsx:165` vs `:166`):
     미발행 slug의 「발행된 리포트가 없습니다」는 라이브로 밟히지만 「리포트는 있지만 기입되지
     않았습니다」는 표본 0이다. 구별하지 않으면 「미작성을 검증했다」가 **어느 쪽인지 흐려진다**.
  3. **S2가 fire 없이 이미 충족돼 있었다** — 착수 시 상태를 먼저 조회한 것이
     **같은 slug 중복 발행을 막았다**(계획의 「재fire 금지」가 그 자리다).

## 학습

- **다음에 다르게 할 것**: 「이 태스크에서 처음 관측된다」류 예고는 **데이터가 협조해야 성립**한다.
  그 예고를 완료기준으로 쓰면 협조하지 않았을 때 판정이 막히니, **「관측되면 이 축, 안 되면 미검증으로
  기록」을 계획에 미리 갈라 둘 것**. 그리고 **fire·발행류 부작용 있는 슬라이스는 착수 시 상태 조회를
  먼저** — 이미 충족된 것을 다시 쏘는 것이 가장 값비싼 실수다.
- **계획 밖 발견**: 278px 리포트 상세 가로 넘침이 **선재 3/7종**이다(`reusable-rocket` 306 ·
  `ai-datacenter-ops` 298 · `semiconductor-equipment` 296, cw 278). 원인은 `players` 표의 「선두 대비」 열.
  처음 축을 「신규 종이 안 넘친다」로 뒀더니 FAIL이 났고, **대조군으로 7종을 전부 재서** 내 탓이
  아님을 확정한 뒤 **집합 동일성** 축으로 회귀 가드화했다. **후속 후보로 이름 남김.**

## 문서 갱신

- CONTEXT.md 승급: 없음(S5 대상 문장이 이미 개수 독립하게 쓰여 있어 무변경)
- ADR 추가: 없음
