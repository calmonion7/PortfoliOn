# 2026-07-11 — 005930 "70k" 오진 포렌식 정정 (task#170)

fg-loop/fg-next all이 회고를 자동 스킵했고, **일괄 승급(2026-08-01)**에서 뒤늦게 작성했다.
원자료는 `.forge/done/2026-07-11-kr-70k-pollution-forensic-correction/run.md`.

## 계획 대비 실제

- **계획대로**: S1 ADR-0020 amendment(포렌식 판정 + 증거 6종 + falsification 조건), S2 CLAUDE.md
  230·231행 교정. 교차검증으로 "게이트/다수결 메커닉 보존 2건 · NXT ~1% 발견 보존 1건 ·
  70k 언급 3곳 모두 올바른 프레이밍"을 grep으로 확인. 코드 0터치(Non-goal 준수).
- **이탈**: 워크플로우 대신 직접 실행(doc-only·순차 의존·서술 판단 집약). 그 외 없음.

## 학습

- **다음에 다르게 할 것**
  1. **가드를 추가했는데 증상이 반복되면, 그 가드를 *우회하는* 경로가 있는지부터 확인할 것.**
     이 건의 핵심이다 — task#101이 박제-시 독립피드 게이트를, task#118이 ref 피드 강화를 넣었는데도
     "정확히 70000.0"이 계속 나타났다. 실제 원인은 **로컬 pytest가 prod DB에 fixture를 직접 쓴
     오염**이었고, 테스트는 애초에 그 게이트를 타지 않는 **제3의 writer**였다. 두 번의 가드 추가가
     증상을 못 막았다는 사실 자체가 "가드가 그 경로에 없다"는 신호였는데, 그때는 매번 피드 글리치
     쪽으로 원인을 더 정교하게 다듬는 방향으로 갔다.
     → CLAUDE.md 승급(BH7-L1/L5 "가드가 도달 불가" 항목의 사촌으로 1줄). 그쪽은 *앞의 코드*가
     판정을 가로챈 경우이고, 이건 *다른 writer가 게이트 밖에서 쓴* 경우다.
  2. **오진을 정정할 때 기존 서술을 삭제하지 말고 재프레이밍할 것.** 게이트·다수결은 "70k를
     해결했다"에서 "진짜 글리치 방어로 유지하되 관측된 70k엔 발동한 적 없다"로 바꿨고, NXT `_AL`
     ~1% 시간외가는 70k와 무관한 **실재 현상**이라 그대로 보존했다. 정정이 유효한 발견까지
     쓸어버리면 다음 사람이 같은 것을 다시 발견해야 한다.
  3. **판정에 falsification 조건을 함께 적을 것** — "conftest 가드 이후에도 70k가 재발하면 이
     판정이 틀린 것"을 ADR에 명시했다. 포렌식 결론은 확률적 귀속이므로 반증 조건이 없으면
     다음 사람이 그것을 확정 사실로 읽는다.

## 문서 갱신

- CONTEXT.md 승급: 없음.
- ADR 추가: 없음(이 태스크의 *산출물* 자체가 ADR-0020 amendment였다).
- 가토 승급: **학습 1**(사용자 승인 2026-08-01, 일괄 승급 — 가드 도달성 항목에 1줄).
