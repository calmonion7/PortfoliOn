# 2026-09-16 — 야간 enrich 정의역을 갱신 대상 집합으로 + 온디맨드 갱신 엔드포인트 (task#354, fg-loop 일괄 승급)

## 계획 대비 실제
- 계획대로 된 것: S1~S3 전부 red-first → 초록(21 단언), 라이브 identity `MATCH n=17`(ADR 실측과 일치), 「야간 전량」 8→0.
- 어긋난 것:
  - 옛 정의역(보유∪관심)을 단언하던 `test_cowork_trigger.py` 3건이 깨졌다 → ADR 260916-132605가 그 정의역을 **명시적으로 뒤집은 기록된 결정**이라 마이그레이션이 정당하다고 판별(계획서·ADR에 이름으로 등장). 「기록된 결정 vs 부수적 단언」 가토(task#264)의 양성 사례 — 이번엔 뒤집는 쪽이 옳았다.
  - 루틴 프롬프트의 모드명을 「[야간 전량 갱신 모드]」→「[대상 지정 모드]」로 (야간과 온디맨드가 같은 모드로 오므로 「야간」도 부적절) — 계획은 「[야간 갱신 모드]」였다.
  - 착수 직전 라이브가 502였다(코드와 무관). 17:47 배포 재기동 뒤 Docker의 `127.0.0.1:80` 포워딩이 `connection reset`; 컨테이너 내부는 정상. 정지조건 스크립트 커밋·push로 러너 `deploy.sh`를 돌렸으나(nginx rm+run) **회복되지 않았고**, `docker stop portfolion-nginx-1`(호스트 리스너 소멸 확인) → `docker start`로 즉시 회복. 09-15 12:55~12:57에도 배포 직후 같은 증상이 2분간 있었다(cloudflared err 로그). 다른 프로젝트의 0.0.0.0 포트와 새 컨테이너의 127.0.0.1 바인딩은 정상 → 포트 80의 기존 포워드 엔트리만 stuck.

## 학습
- 다음에 다르게:
  - 라이브 502 + `docker ps` 전부 Up + 백엔드 포트 열림 + `curl 127.0.0.1:80 → 000`이면 배포·코드·러너 진단으로 가지 말고 nginx 컨테이너 stop/start 1회부터. `deploy.sh` 재실행은 이 케이스를 못 고친다(실측). → CLAUDE.md Deployment 절 승급.
  - 갱신 대상 집합의 「열람」은 추적 종목으로 제한했다(미추적 종목엔 enrich 저장 행이 없다) — 라이브에선 17==17로 차이 0이지만 정의로 남긴다. 판정은 파이썬에서(mock 행 8축 테스트 가능), SQL 정합은 라이브 identity 스크립트가 따로 잰다 — 둘 다 있어야 한다.

## 문서 갱신
- CONTEXT.md 승급: 없음(용어 3종은 fg-ask 그릴링 때 이미 정의)
- ADR 추가: 없음(ADR 260916-132605 「이행」 절 추기는 task#356)
- CLAUDE.md: Deployment 절에 nginx 루프백 포워딩 stuck 분기 추가
