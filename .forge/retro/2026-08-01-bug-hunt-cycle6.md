# 2026-08-01 — 6차 버그 헌트 (5렌즈 16 에이전트, 원시 10 → CONFIRMED 7 / REFUTED 3)

fg-loop이 회고를 자동 스킵했고(`retro: skipped`), 일괄 승급 모드에서 뒤늦게 작성한 회고다.
원자료는 `.forge/done/2026-08-01-bug-hunt-cycle6/run.md`.

## 계획 대비 실제

- **계획대로 간 것**: SHA 고정 범위(`4393dde..4752112`) 준수 · 5렌즈 전부 원시 findings 반환(0건 렌즈 없음, scanned 파일 수·심볼 요약 동반) · 검증 상한 10 대비 절단 0 · 메인 세션이 CONFIRMED 7건 스팟 재검증 + REFUTED 3건 기각 논리 감사(5차 회고 교훈 4개 항목 전부 적용).
- **이탈**
  1. **수렴 0건** — 5차는 3렌즈가 H1을 독립 지목했는데 이번엔 렌즈 간 중복이 전무했다. 렌즈를 "이번에 바뀐 표면"에 정렬한 효과(중첩 감소)로 보이지만, **사각 증가와 구별되지 않는다** — 다음 사이클에서 수렴 0이 또 나오면 렌즈 경계가 너무 잘려 있는지 점검할 신호로 쓸 것.
  2. **M3는 신규 발굴이 아니라 `.forge/codebase/CONCERNS.md:110`에 이미 기록된 open gap의 재발견**이었다. 계획이 예상하지 않은 범주(지도에 있으나 미조치).
  3. 검증기가 계획의 "필요 시 재현"을 넘어 H1·M2·M3에서 **로컬 monkeypatch 재현까지** 수행 — 확정 강도가 5차(정적 확정 위주)보다 높다.
  4. L5 대상 기술 오차(계획 "vitest 신규 6파일" → 실측 3종). diff로 대상을 확정해 무영향.

## 학습

- **다음에 다르게 할 것**
  1. **헌트 착수 시 `CONCERNS.md`의 미해결 목록을 먼저 읽고, finding에 "지도 기록됨" 태그를 달 것.** 이탈 2번. 재발견 자체는 무가치하지 않다(우선순위 근거가 된다) — 문제는 **신규 발굴과 구별되지 않으면 "이번에 새로 생긴 결함"으로 오독**된다는 것이다. 리포트에 재발견 표시를 남겼으나, 다음 사이클은 아예 렌즈 프롬프트에 CONCERNS 대조를 넣는 게 낫다.
  2. **수렴 건수를 사이클 간 비교 지표로 남길 것.** 이탈 1번. 5차 "3렌즈 수렴" → 6차 "0"은 렌즈 설계 변화의 신호인데, 지금은 리포트에 숫자만 있고 해석 근거가 없다.
  3. **검증기에 "가능하면 로컬 재현"을 명시적으로 요구할 것.** 이탈 3번이 자발적으로 일어났는데, 재현된 3건이 리포트에서 가장 단단하다. 프롬프트에 넣으면 재현 가능한 건에서 일관되게 나온다.
  4. 렌즈별 `scanned`(파일 수·심볼 요약) 강제는 유지 — 0건 렌즈가 없었어도 "무엇을 봤는지"가 있어 리포트 신뢰도가 올랐다.

## 문서 갱신

- CONTEXT.md 승급: 없음 — 신규 도메인 용어 없음.
- ADR 추가: 없음 — 3조건 미달(리포트 산출물이고 결정이 아니다).
- **후속 큐(리포트에 우선순위 기록)**: H1+M2+M3(부분 페이로드 가드 3형제, 같은 실패 클래스) → M4+M1 → L1 → L2.
