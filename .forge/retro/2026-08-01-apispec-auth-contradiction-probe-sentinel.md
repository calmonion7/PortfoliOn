# 2026-08-01 — API_SPEC 인증 자기모순 · probe248 sentinel (task#263)

fg-loop이 회고를 자동 스킵했고(`retro: skipped`), 일괄 승급(2026-08-01)에서 작성했다.
원자료는 `.forge/done/2026-08-01-apispec-auth-contradiction-probe-sentinel/run.md`.

## 계획 대비 실제

- **계획대로 간 것**: `API_SPEC.md` 1766·1855·1890의 인라인 `**Auth 불필요.**`만 외과적으로 삭제(`grep -c '불필요'` 4→1, 남은 1건이 182행 ADR-0029 공개 엔드포인트임을 확정). probe248 `EXPECT_KEEP_TSM` 루프에 sentinel 가드 추가 — **같은 파일 ③ 블록이 이미 쓰는 관용구를 그대로** 재사용(새 관용구 만들지 않음). 스위트 1496 무변.
- **이탈**
  1. 워크플로우 미사용(삭제 3줄 + 추가 4줄, 로직 없음) — task#262보다 근거가 더 강한 eco 판단.
  2. 꼬리 문구 문맥이 서로 달라 일괄 치환이 불가해 3번의 개별 편집으로 갈렸다(도구 세부, 설계 변경 아님).

## 학습

- **다음에 다르게 할 것**
  1. **감사 grep 패턴이 좁으면 그 감사는 통과해도 무의미하다.** task#230·231·232 3부작이 `grep -n '\*\*Auth:\*\* 불필요' API_SPEC.md`(콜론 독립 줄)를 감사 도구로 못박았는데, 이번 3곳은 `**Auth 불필요.**`(문장 중간·콜론 없음)라 **그 감사가 원리적으로 볼 수 없었다**. CLAUDE.md의 해당 가토에 박힌 패턴을 `불필요`(콜론 무관)로 넓힐 것. → **가토 승급 후보**. 이것은 "프로브 판정축이 부족하면 ALL PASS가 무의미"(⑧ⓐ·④)의 **문서 감사판**이다.
  2. **테스트가 원리적으로 못 보는 표면은 문서에도 있다** — `test_api_doc_sync.py`는 엔드포인트 *존재*만 대조하고 auth 산문은 파싱하지 않는다. 그래서 같은 섹션 안의 문자 그대로의 모순이 스위트 초록 상태로 무기한 생존했다.

## ⚠️ 이 회고가 늦어서 실제로 재발했다

학습 1은 **이 run.md에 승급 후보로 명시돼 있었으나 승급되지 않았고**, 그 사이 같은 날 생성된 `.claude/agents/doc-sync.md:35`가 **똑같은 좁은 패턴을 지시로 박제**했다. 7차 버그 헌트가 이를 L3로 재발견했다(카드 생성 `4e204a3` 00:13 < 발견 `e148592` 10:45, 이후 카드 미갱신).

즉 **"승급 후보로 적어두는 것"과 "승급하는 것"의 간극이 실측 가능한 재발을 낳았다.** fg-loop/fg-next all의 회고 자동 스킵은 "나중에 일괄 승급" 약속에 기대는데, 그 약속의 지연 비용이 여기서 처음 수치화됐다(지연 ≈ 하루, 재발 1건). → **가토 승급 후보**(같은 사이클 안에서 실증된 교훈은 카드·지침 같은 *지시 문서*로 즉시 전파할 것).

## 문서 갱신

- CONTEXT.md 승급: 없음.
- ADR 추가: 없음.
- 가토 승급: 학습 1 + 위 재발 사례를 CLAUDE.md에 반영(아래 일괄 승급에서 처리).
- 후속 큐: `.claude/agents/doc-sync.md:35` 패턴 확대(7차 L3로 정식 등록) · probe248 sentinel 가드는 라이브 미실행(1회성 스크립트, CI 미배선 — 문법 확인까지가 게이트).
