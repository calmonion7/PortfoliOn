# 2026-08-12 — 주요기술 리포트 5종 일괄 갱신·발행 (데이터 센터 신규 포함) (task#300)

fg-loop 드라이브에서 회고 자동 스킵으로 봉인됐고, 2026-08-12 일괄 승급(batch promotion)에서 검토했다.

## 계획 대비 실제

- **계획대로 된 것**: 3슬라이스 전부 랜딩. slug 5종(`data-center`·`reusable-rocket`·`solid-state-battery`·
  `smr`·`robotics`)이 **각 1회 시도로** 발행됐다(15·14·10·12·12분, 총 ~67분). 재fire 0회 —
  사용자가 사전승인한 자동 재fire 권한을 쓸 일이 없었다. 코드 변경 0(계획대로), 실패 slug 없음.
  검증: 5행 전부 `2026-08-12` · 기존 4종 본문 md5 전부 변화 · 5종 전부 `variants`/`watch_items` 비-null ·
  프로브 ALL PASS 149/149. **발산은 낮다.**
- **발산 3건**:
  1. **⚠️ 가장 위험했던 것 — `claude -p`의 0바이트 로그를 실패로 오진할 뻔했다.** 첫 fire 후 10분간
     `run.log`가 0바이트였고 DB에도 행이 없었다. 계획의 실패 처리(「2회 시도로도 갱신되지 않으면…」)를
     그대로 적용하면 **재fire = 같은 slug에 중복 발행**이었다. 실제로는 정상 진행이었고(15분 뒤 발행)
     `claude -p`가 완료 시 1회 출력하는 것이 원인이었다. **직전 런(4782B/19분)과 대조**해서 「메커니즘은
     동작한다」를 먼저 확정한 것이 갈림점이었다. → **CLAUDE.md 승급 1건.**
  2. **계획의 「5섹션 렌더를 육안으로 확인」을 프로브 축으로 승급했다.** `required-sections` 축을 추가해
     KPI(testid) + `data-tech-section` 앵커 4개를 **존재만이 아니라 `getBoundingClientRect().height > 0`**
     까지 확인한다(빈 컨테이너 렌더 배제). 단언은 목표인 `data-center`에만, 출력은 5종 전부.
     이빨 확인: `variants` 셀렉터 주입 → 정확히 2건 FAIL(대상 2뷰포트). 단언 147 → 149.
  3. **전체페이지 캡처로는 육안 확인이 원리적으로 불가했다.** `m390-data-center.png`가 **48,693px**라
     렌더 축소 후 아무것도 읽을 수 없었다 → 섹션별 **뷰포트** 캡처 10장을 따로 떠서 확인
     (2뷰포트 × head/players/variants/watch/market, 각 `scrollIntoViewIfNeeded` 후).
- **계획의 우려가 코드로 이미 닫혀 있던 것 1건**: 「fire가 조용히 `False`를 반환하고 끝나므로 발사했다고
  착각하면 안 된다」 → `routers/admin.py::cowork_fire`는 미설정 **503** · `fire()` 실패 **502**를 던진다
  (`ok=False`가 200으로 새지 않는다). 확인 비용이 1콜이었으니 경고 자체는 값이 있었다.
- **재지정 본문의 정당성 확인**: `test_cowork_trigger_text.py`의 「정책 열거 금지」 가드는 *자동 생성*
  본문(`daily_text`·`manual_text`)에만 걸리고, 커스텀 `body.text`는 `test_admin_fire_custom_text_passthrough`
  가 **「(d) 재지정 경로 보존 핀」**으로 의도적으로 보존한 경로다. 다만 그 교훈을 본문 작성에 반영해
  **발행 규약을 재열거하지 않고 프롬프트 정본을 가리키고 범위만 재지정**했다(계획 문구의 정책명 열거를
  「다른 정책은 이번엔 건너뛴다」로 축약). 5/5가 프롬프트 규약대로 발행됐다.

## 학습

- **다음에 다르게 할 것**:
  - **비동기 잡의 진행 판정을 3분할할 것**: 생존 = 프로세스 · 완료 = 결과 상태(DB 행) · 실패 원인 = 로그
    (끝난 뒤에만 읽을 수 있다). **출력 파일 크기를 진행 신호로 쓰면 프로덕션 쓰기 경로에서 재시도가 곧
    부작용이 된다.** (승급됨)
  - **육안 확인을 완료기준에 넣을 때 「그 캡처로 실제로 볼 수 있는가」를 함께 확인할 것.** fullPage 캡처가
    수만 px이면 축소 후 무의미하다 — 섹션별 뷰포트 캡처를 별도로 두거나, 프로브가 fullPage와 뷰포트
    캡처를 둘 다 남기게 할 것. 이 저장소 프로브들은 관례적으로 fullPage를 생산하므로 재발한다.
  - **계획이 「육안으로 확인」이라고 쓴 항목은 축으로 승급할 수 있는지 먼저 볼 것.** 이번 5섹션 렌더는
    testid·앵커가 이미 있어 축 작성 비용이 낮았다(가토 ⑩ — 육안으로 잡을 수 있는 것을 축으로 올려두면
    다음번엔 육안에 기대지 않는다).
  - **「진짜 갱신됐는가」를 날짜로만 재지 말 것.** `ON CONFLICT (slug) DO UPDATE SET published_date`는
    본문이 동일해도 날짜를 올린다 — 빈 갱신과 구별하려면 **본문 해시 변화**를 함께 봐야 한다. 이번엔
    착수 시 md5 baseline을 떠 둔 덕에 4종 전부 변화를 증명했고(예: smr `ae24b168ee6b`→`5ac62c4b635d`),
    Optional인 신규 필드(`variants`/`watch_items`)가 null→채워짐으로 전환된 것이 두 번째 축이었다.
  - **순차 fire의 이유가 해소되면 병렬화를 재검토해도 되지만, 이번엔 유지가 옳았다.** 계획의 순차 근거는
    「신규 slug 실패 시 원인 규명」이었고 `data-center` 성공으로 해소됐지만, 4개 동시 opus 런은 rate-limit로
    배치 전체를 오염시킬 위험이 있어 순차를 유지했다(총 67분, 배경 잡이라 수용 가능).

## 승급하지 않은 것 (여기 남긴다)

- fullPage 캡처의 육안 부적합 — 가토 ⑧ⓓ(scrollIntoView·전용 캡처)의 구체 확장이라 회고에만.
- 「본문 해시 + 신규 필드」 2축 갱신 판정 — 이번 태스크 고유의 판정축 설계라 회고에만(다음에 같은
  「일괄 재발행」 태스크가 오면 이 절을 그대로 재사용할 수 있다).
- 메인 세션 자신의 실수 2건(폴링 판정이 *이전* 런의 `completed`를 잡은 identity 버그 · 프로브 최종
  실행의 「출력 없음」이 통과가 아니라 앞선 `cd frontend` 잔존으로 인한 측정 실패) — 가토 ⑧ⓘ와
  「0건/무출력을 성공으로 읽지 말라」에 이미 있는 원리의 자기적용 사례라 회고에만. 다만 **둘 다
  「출력이 비었다」를 통과로 읽지 않은 덕에 잡혔다**는 점은 위 승급 항목과 같은 가족이다.

## 미커버 (후속)

- **발행 내용의 사실 검증**은 이 작업의 범위가 아니다(계획 비목표). 스키마가 강제하는 것은 형식(단위
  enum·출처 필수·이산 척도)뿐이고 수치가 현실과 맞는지는 사람이 읽어야 한다 — ADR-0033 결과 절의
  미커버가 그대로 유효하며 **5종 전부 새 판이므로 이번엔 특히 크다.** 사용자에게 `data-center` 상세
  스크린샷 3장을 전달해 읽기 시작점을 만들어 뒀다.
- `TechLevelBand.jsx`의 `.tech-level-band__name` ellipsis에 `title=` 폴백 없음(task#299 관찰, 선재 상태).
  5종이 새 판이 되어 `leader_name` 길이 분포가 달라졌지만 잘림 2계열 4924검사에서 위반 0이었다.

## 문서 갱신

- CONTEXT.md 승급: none (용어 변화 없음 — 이번 태스크는 데이터 갱신이고 코드·용어 무변경)
- ADR 추가: none
- **CLAUDE.md 승급 1건**: 「비동기 잡의 *진행* 판정에 출력 파일 크기를 쓰지 말 것 — `claude -p`는 완료 시
  1회 출력하므로 0바이트 로그가 「죽음」과 「정상 진행」을 동시에 의미한다」를 `job_runs.record`
  가토(거짓 초록) 바로 뒤에 그 **거울상(거짓 빨강)**으로 삽입.
