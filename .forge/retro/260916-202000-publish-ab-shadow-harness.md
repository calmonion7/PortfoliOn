# 2026-09-16 — 발행 레인 A/B 섀도 하네스 — opus·muse·H1 3팔 실측 (task#350, fg-loop 일괄 승급)

## 계획 대비 실제
- 계획대로 된 것: 프록시 14·발행 15 테스트, 60산출물 error 0, prod 행 수 전후 동일, compare 20건. 결론은 사용자 결정으로 opus 복귀(ADR 260913 모델 파라미터 · task#350 커밋 5d650f3).
- 어긋난 것:
  - 착수 전 설계 2건 변경(사용자 지시): opus 팔을 신규 세션에서 **기존 발행물 수확**으로 · 무쓰기 보장을 「섀도 프롬프트 + 사후 카운트」에서 **쓰기 차단 프록시 + 더미 키 2겹**으로. 프록시가 **36건**을 실제 차단 — 원안이었으면 그만큼 prod에 쓰였다(기술 리포트는 upsert라 복구 불가).
  - 파일럿이 설계 결함 2건을 잡았다: 표본(기존 opus 판이 있는 종목) ↔ 루틴 7일 발행 게이트 충돌로 첫 발사가 38초 빈손 종료 · 「분량」 축이 DB 블롭 vs 요청 본문을 비교해 4배 오표기.
  - **내 배포가 측정을 깨뜨렸다** — 규칙(「라이브를 재는 에이전트가 도는 동안 push도 배포다」)을 알고 미리 push까지 해 둔 뒤 두 커밋 뒤에 다시 push해 JPM 세션이 502로 소실(재실행 복구).
  - 자동 축 3종(출처 URL 실존율·웹조사 횟수·토큰 정가) 미구현 — H1 검수의 `changes[]`가 더 직접적으로 대체. OpenCode 세션은 트랜스크립트가 없어 토큰 환산이 원리적으로 불가.
  - OOM이 유휴 폴링 루프를 반복 종료 → 폴링 대신 마무리 체인 스크립트로 전환.

## 학습
- 다음에 다르게:
  - **LLM 세션에 「쓰지 마라」는 가드가 아니다.** 무부작용 실험은 프록시·더미 자격증명처럼 **구조적으로 불가능**하게 만들고, 그 가드의 차단 카운트를 산출로 남길 것(0이면 가드가 안 탄 것). → CLAUDE.md 승급.
  - A/B 본 실행 전 파일럿 1건은 선택이 아니다 — 표본 선정과 대상 시스템의 게이트(7일 발행 게이트)가 충돌하는지, 측정 축의 두 팔이 같은 스케일을 재는지가 파일럿에서만 드러났다.
  - 게이트 우회를 트리거에 넣되 「어차피 저장 안 된다」는 모델에게 알리지 않는다(알리면 덜 노력해 품질 측정이 오염된다).
  - 라이브 세션이 도는 동안의 push 금지는 **두 번째 위반**이다 — 알고 있음이 재발을 막지 못했다. 다음엔 본 실행 시작 시 「push 금지 창 ~HH:MM」을 run.md 첫 줄에 적을 것.
  - 장기 대기는 폴링 sleep 루프가 아니라 체인 스크립트로(OOM이 유휴 프로세스를 먼저 죽인다).

## 문서 갱신
- CONTEXT.md 승급: 없음(「섀도 모드」 절은 task 본문 S8에서 재작성)
- ADR 추가: 없음
- CLAUDE.md: 「무쓰기 보장은 구조로」 가토 추가
