# 2026-08-16 — 「기술수준 비교」 밴드를 주요 업체 표로 흡수 + 선두 판정 단일화 (task#304)

> fg-loop 드라이브가 자동 스킵한 회고를 일괄 승급(batch promotion 2026-08-17). 원재료는 봉인 아카이브
> `.forge/done/260816-142300-tech-report-players-levels-merge/run.md`. 배포 `cd7509f` → `b51ba00`.

## 계획 대비 실제

- **계획대로 된 것**:
  - 밴드 섹션·목차 칩 제거, 표 셀 흡수, 범례 1줄, `isLeader` 단일화, 분야 소제목 선두 병기 — 전부 완료.
  - **클래스명을 개명하지 않기로 한 ADR-0041 결정 5가 값을 했다.** `uat277`이 그 이름으로 칸 수·채움색
    이빨을 단언하고 있어, 개명했다면 그 축을 통째 재작성해야 했다. 실제로는 컨테이너 셀렉터만 재지향했다.
  - **픽스처 판별력 한 줄(`expect(groupByCategory(FIXTURE)).not.toEqual([])`)이 task#301의 실패를
    반복하지 않게 막았다** — 이빨 있는 테스트가 그룹핑 분기를 안 타고 초록으로 통과하던 형태.

- **이탈**:
  1. **⭐ 폭 예산을 「상한」으로만 잡아 이웃 열이 비용을 치렀다(승급).** 예산 46.3px 안인 42px이었고
     문서 가로 스크롤도 0이었는데, **대체한 옛 텍스트(38px)보다 4px 넓어** m350에서 업체 열이
     95.7→93.3px로 줄고 `ArianeGroup·ESA (Themis)`가 216→**324px**로 쪼개졌다.
     → CLAUDE.md 가토 ⑰ **「비용이 이웃 열로 이전된다」**로 승급.
  2. **⭐ 육안이 6번째로 유일한 포착 수단이었고, 대조군이 「내 회귀 vs 선재」를 갈랐다.** 넘침·잘림·접힘
     축이 전부 *사실로* 통과했고 C4 독립 프로브도 **500/500 ALL PASS**였다. `addStyleTag`로 처방만
     무효화하니(밴드 셀 `display:none`) 업체 열 95.7 · 이름 216px로 복귀 → 쪼개짐 자체는 **선재**
     (task#296의 `overflow-wrap:anywhere` + 52px `정부주도` 배지가 같은 flex 줄), 내 몫은 악화분뿐.
     칸을 5px(37px)로 줄여 되돌리자 **대조군 값과 정확히 일치**했다(내 회귀 0).
  3. **⭐ 적대 검토와 프로브 갱신이 같은 wave여서 리뷰 결과가 형제의 프로브를 스테일하게 만들었다(승급).**
     리뷰 HIGH를 받아 캡션을 분류 유무로 게이팅하자, S3가 이미 써 둔 `caption` 5축 + `chain-gap-domain`이
     배포 후 **96건 FAIL**했다. → 병렬 서브에이전트 가토에 **여섯 번째 방향**으로 승급.
  4. **가토 ⑨의 아티팩트가 재발했다 — 문서에 있는데 에이전트가 옛 방식으로 썼다.** 「값 옆에 작은
     단위·배지는 이 저장소 표준 관용구이므로 `top` 집합으로 줄을 세면 재발한다」가 CLAUDE.md에 이미
     있는데, 새 축 3종이 전부 `new Set(...top).size`로 짜여 **22건 거짓 FAIL**. 진단 지문(「4뷰포트가
     전부 동일하게 실패하면 줄바꿈이 아니다」)이 그대로 맞았고, 실측(컨테이너 높이 16.5 == 숫자 높이,
     자식 6개가 가로 일렬)으로 확정한 뒤 겹침 기준으로 교체했다(**완화가 아니라 더 엄격**).
     (미승급 — 가토 ⑥의 N번째 관측. 다만 실천은 분명하다: 워크플로우 프롬프트에 「가토를 읽어라」가
     아니라 **그 관용구의 코드를 직접 박아** 줄 것.)
  5. **계획 전제 3건이 실측과 달랐다** — 발행물 3→**6종**, 최장 문자열 판이 codepoint/렌더폭 기준으로
     갈림, `smr`·`robotics`에 **부분 분류** 실재(+ 「점유율」 열이 2 slug엔 없어 "4열 예산"이 성립 안 함).
     대상을 4 slug로 다시 썼다. (미승급 — 가토 ⑤⑦의 관측 수 증가.)
  6. **⭐ 계획이 명시한 지시를 구현이 빠뜨렸는데 vitest 404건이 전부 초록이었다.** 계획 S2의
     「캡션 leaders는 **분류 없는 판에서만** 렌더」가 누락돼 평면 캡션과 분야 소제목이 동시에 떠
     **한 화면에 선두 주장 셋이 충돌**했다. 그 계약을 단언하는 테스트가 없었기 때문이다(적대 리뷰가 포착).
     → **계획의 조건절("~일 때만")은 그 자체로 테스트 축이다**를 회귀 2축으로 못박았다.
  7. **내 C4 프로브가 응답 봉투를 추정했고 내 sentinel이 공허하게 통과했다.** `/api/tech-reports/{slug}`는
     리포트가 아니라 `{slug, reports:[...]}`를 준다. 게다가 `Math.min(3, withLevel.length)` sentinel이
     정의역 0일 때 하한 0이 되어 **조인 실패를 못 걸렀다** — 내가 계속 인용하던 가토 ⑧ⓐ의 자기적용 실패.
     (사용자 판단으로 미승급 — 이미 문서화된 항목의 관측 수 증가.)

## 확인된 것

- **S0의 선재 FAIL 표가 이 태스크의 최대 산출물이었다.** 계획 DoD는 `uat280` 10건만 예상했으나 실제는
  4종 프로브 **43건**. 이 표가 없었으면 배포 후 33건이 「내 회귀」로 오독됐다.
- **프로브 전수 실행이 필요했다** — grep으로 좁혔다면 이름을 안 쓰고 성질만 재는 축(`uat281`의
  `gap-uniform` 사슬)을 놓쳤을 것이다(task#301이 그렇게 26 FAIL을 냈다).
- 도달 불가해진 전역 이빨(`caption-teeth`)을 **삭제·완화가 아니라 재지정**(`group-leader-teeth`)하고
  평면 캡션 커버리지 0을 사실로 출력하게 한 것이 옳았다.

## 다음에 다르게 할 것

1. **폭을 나눠 쓰는 표면에 요소를 넣을 땐 완료기준을 「예산 이하」가 아니라 「대체한 것의 min-content
   이하」로 쓸 것**, 그리고 착수 시 대체 대상의 min-content를 먼저 재라(승급됨).
2. **프로브 갱신은 적대 검토 뒤 직렬 단계로** 둘 것(승급됨).
3. **계획서의 조건절("~일 때만", "~인 판에서만")을 착수 시 그대로 테스트 축으로 옮길 것** — 구현이
   빠뜨려도 스위트가 초록이면 아무도 못 잡는다.
4. 시각 변경에서 육안이 결함을 잡으면 **대조군으로 「내 회귀 vs 선재」를 먼저 가를 것**(승급 ⑰ⓒ).

## 문서 갱신

- CLAUDE.md 가토 **⑰ 신설**(비용이 이웃 열로 이전된다 — 이탈 1·2).
- CLAUDE.md 병렬 서브에이전트 가토에 **여섯 번째 방향** 승급(이탈 3).
- ADR-0041(밴드 흡수 · 선두 판정 합집합 · 클래스명 유지)은 착수 전 작성분이 그대로 유효 — 변경 없음.
- `.forge/CONTEXT.md` 3항목 + `README.md` 갱신(태스크 중 반영, 신규 용어 없음).
- API_SPEC·CLAUDE_COWORK_API 무변경(확인됨) · 줄번호 참조 표본 11파일 0건.

## 후속 후보

- **m350에서 업체명이 2자/줄로 쪼개지는 선재 결함** — 근본 해결은 이름 셀의 flex 구조 재편(52px
  `정부주도` 배지를 이름과 같은 줄에서 빼거나 이름에 min-width 우선권)이며 task#296 설계 영역이다.
  → fg-ask 그릴링 후 백로그 계획으로.
- 선재 프로브 FAIL **62건**(uat277 6 · uat280 19 · uat281 21 · uat282 16). S0 baseline 표가 기준선.
