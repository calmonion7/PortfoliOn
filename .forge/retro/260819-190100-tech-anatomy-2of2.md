# 2026-08-19 — 기술 해부 2/2: `/tech-anatomy/:slug` 화면·진입점·라이브 프로브 (task#306)

- 슬러그: `tech-anatomy-2of2` · 배포 `57a3e48` → `ed7faef`(회귀 수정) → `90c0c03`(문서)
- 레인: fg-loop 무인 드라이브(회고 자동 스킵 → 2026-08-19 일괄 승급)

## 계획 대비 실제

- **계획대로 된 것**:
  - S2 축마다 [제목 + 기준 문구] → [100% 누적 막대] → [항목 목록]. **막대 조각에 텍스트 0**
    (조각의 `overflow:hidden`이 nowrap 라벨을 ellipsis 없이 자른다 — task#275가 겪은 그 표면)
  - S5 프로브 — 배포 전 라이브 red-first 45단언 중 33 FAIL 확보 → 배포 후 **ALL PASS 90/90**
    (커버리지 45→90, 세그먼트 폭 합이 막대 폭과 **정확히** 일치: 238/238 · 728/728 · 780/780)

- **이탈**:
  1. **⭐ 상호링크의 *세로 위치*가 계약이었다 — 20줄 아래 주석이 이미 경고한 자리에 넣었다.**
     「이 기술의 해부 →」를 리드 아래(= KPI 스트립 **위**)에 두자 그 한 줄(~37px)이 **모바일에서
     KPI 스트립을 첫 화면 밖으로** 밀었다(`uat280 kpi-visible` 선재 3 → 6). 넘침·잘림·접힘·`h-scroll`
     어느 축에도 안 걸리고 vitest는 jsdom 무레이아웃이라 원리적으로 못 본다. **같은 파일의 목차 블록
     주석이 "리드 밑에 두면 128px을 먹어 스트립이 밀린다"고 task#296 실측까지 적어 두었는데** 그 파일을
     읽고도 같은 자리에 넣었다. 스트립 아래로 옮겨 해소. → 스킬 ⑰ **세로판**으로 승급.
  2. **⭐ 내 검증 스크립트가 프로덕션을 덮었다.** 422 스모크의 **대조군**이 "유효한 composition은 201"을
     증명하려고 **placeholder 축을 실제로 발행**해 방금 조사·발행한 3축 실데이터를 클로버했다.
     **1차 실행에선 무해했다** — 그땐 그 slug의 `composition`이 `null`이었기 때문이고, 같은 스크립트가
     *데이터가 생긴 뒤* 유해해진 것이다. 복구 후 무손실 설계로 재작성(파괴적 경로는 이미 null인 slug에,
     긍정 경로는 항등 재발행, 마지막에 "실데이터가 시작 상태와 동일한가"를 단언). → 스킬 ⓢ 승급.
  3. **⭐ 자체 적대 검토가 형제 validator의 빠진 짝을 잡았다.** `Composition`에 항목명 유일성이 없어
     같은 이름 2개가 201로 통과했다(형제 `VariantAxis._option_names_unique`가 막는 클래스).
     결과는 ⓐ 같은 이름 행이 두 번 그려짐 ⓑ 렌더러 `key={item.name}` **충돌**. CLAUDE.md가 명시한
     체크리스트(task#297 「가장 닮은 형제의 validator를 전부 열거하라」)를 **읽은 상태에서 놓쳤다.**
     1/2는 이미 봉인됐으나 「알고도 미룬 결함을 후속으로 강등하지 말라」(task#248→#249)에 따라 닫았다.
  4. **⭐ 픽스처 분기 커버리지 단언이 실제로 red를 냈다(task#301 가토의 양성 사례).**
     「producers가 빈 광물도 있어 칩 없음 분기를 탄다」가 **실패**했다 — 라이브 발행분의 광물 5종이
     전부 producers를 갖고 있었다. 완화하지 않고 라이브 *형태*에서 파생한 변형으로 그 분기를 덮었다.
     이 단언이 없었으면 그 경로는 아무도 안 밟은 채 초록이었다.
  5. **신규 프로브의 red-first는 이빨 증명이 아니었다** — 결함주입 5축으로 따로 확인했다.
     ⚠️ **1차 주입 2건이 「이빨 없음」으로 나왔는데 그건 축이 아니라 *주입*의 결함이었다**
     (CSS `::after`는 `textContent`에 안 잡히고, 첫 clip 주입은 flex 축소에 흡수됐다 — 진단값
     `sw/cw`를 함께 출력했기 때문에 갈렸다). **이빨 검사도 대조·진단 없이는 거짓 음성을 낸다.**
  6. **목록 카드 전체가 `<Link>`라 구조를 풀어야 했다** — 앵커 중첩은 무효 마크업. 기존 단언
     `card.getAttribute('href')`가 깨졌고, **기록된 결정인지 부수적 단언인지**를 task#276 계획으로
     판별했다(완료기준·비목표 어디에도 "카드 루트가 앵커일 것"이 없음 → 부수적, 이유를 주석에 남김).

## 확인된 것

- **⭐ 선재 FAIL baseline 표가 63건 중 내 것 3건을 즉시 갈랐다.** task#304가 `uat277 6 · uat280
  19(clip 16 + kpi-visible 3) · uat281 21 · uat282 16`을 **숫자와 축 내역까지** 남긴 덕에,
  이번 전수 실행에서 uat277·281·282가 baseline과 **정확히 동일**함이 확인돼 **uat280의 +3만**이
  내 회귀로 특정됐다(수정 후 재실행 19로 복귀, 내역까지 일치). → 스킬 승급.
- **프로브 전수 실행이 또 필요했다** — 계획이 지목한 1종(`uat299`)만 돌렸으면 uat280의 회귀를
  놓쳤을 것이다(그 축은 「kpi-visible」이라 내 변경의 어떤 이름도 쓰지 않는다, task#301 ⑧ⓟ 3번째 관측).
- **uat298의 24 FAIL은 데이터 스테일**이었다 — "`variants`/`watch_items`가 NULL인 발행물엔 그 섹션이
  없어야 한다"는 단언인데 라이브 6종이 전부 채워진 상태로 재발행돼 있었다(대상 slug published=2026-08-12).

## 다음에 다르게 할 것

- **첫 화면 예산이 있는 화면에 요소를 끼울 땐 "이 줄이 아래 무엇을 미는가"를 물을 것**(승급됨).
  그리고 **삽입 지점 근처의 기존 주석부터 읽을 것** — 이 저장소는 세로 예산 실측을 주석에 남긴다.
- **push 전에 선재 FAIL baseline을 직접 잴 것**(승급됨). 이번엔 선행 태스크 기록이 구제했다.
- **프로덕션에 쓰는 검증은 "성공해도·실패해도 무엇이 남는가"를 대상 선정 단계에서 물을 것**(승급됨).
  그 답은 **데이터 상태가 바뀌면 달라진다**.
- **신규 구조 필드를 만들 땐 형제의 `model_validator`를 *목록으로 적어놓고* 하나씩 지울 것** —
  "읽었다"로는 부족했다(이번이 task#297 체크리스트의 재발이다).

## 문서 갱신

- **`.claude/skills/live-uat-probes/SKILL.md`** — ⑰에 **세로판**(첫 화면 예산) + **선재 baseline 표 규율**,
  ⓢ에 **무해성의 유효기간**(검증 스크립트의 프로덕션 클로버) 추가.
- **ADR-0042 보정 3** — 한 축 안 항목명 유일성(다른 축은 무관, 양성 테스트로 못박음).
- 승급하지 않음: 픽스처 분기 커버리지 양성 사례 · 이빨 주입의 거짓 음성 · 앵커 중첩 · uat298 스테일.

## 후속 후보

- **선재 FAIL 62건**(uat277 6 · uat280 19 · uat281 21 · uat282 16) + **uat298 24건**은 그대로 남는다.
  `clip` 계열 다수는 SVG `<text>`(clientWidth 0)를 재는 축이라 **프로브 결함일 가능성이 높으나**
  판정에는 별도 사이클이 필요하다. uat298은 데이터 상태 전제를 갱신하면 닫힌다.
- 해부가 채워진 기술은 아직 1종(전고체 배터리)이다 — 나머지 5종은 루틴 기입 대기.
