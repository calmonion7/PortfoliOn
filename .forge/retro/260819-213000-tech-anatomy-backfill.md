# 2026-08-19 — 기술 해부 백필: 나머지 5종의 `composition` 조사·발행 (task#308)

- 슬러그: `tech-anatomy-backfill` · 배포 `91bf0f0`(S1 프로브) → `49bb60f`(S6 래칫)
- 레인: fg-loop 무인 드라이브(회고 자동 스킵 → 2026-08-19 일괄 승급)

## 계획 대비 실제

- **계획대로 된 것**:
  - S1을 데이터보다 **먼저** 둔 순서가 정확히 옳았다 — 5종을 채우는 순간 「composition 없는 실제
    slug」 대조군이 0개가 되어 프로브가 `exit(1)`로 죽는다. 계획이 그 이유까지 적어 두었고 실제로 그랬다.
  - 무손실 발행 5/5 — POST 201 · composition 드리프트 0 · 본문 md5 6/6 동일 · 규율 전수 통과.
  - 프로브 ALL PASS 90 → 111(백필 전) → **191**(백필 후), `rule` 커버리지 17 → 97.

- **이탈**:
  1. **계획의 「광물 축 생략 후보」 가설이 실측으로 확증됐다** — `ai-datacenter-ops`만 2축.
     리포트 자신의 difficulty 근거가 "실제 제약은 기술이 아니라 **전력 인가·장기 조달계약·자본 조달**"
     이라 적고 있고 원가 구조는 감가상각+전력이다. 운영업은 완제품 가속기와 전기를 **사는** 사업이라
     「원재료비」라는 자가 성립하지 않는다. 억지로 채우는 것이 곧 허위정밀이므로 통째 생략(ADR-0042 결정 2).
     부수 이득: 이 slug이 S7 육안의 「축이 생략된 slug」 케이스가 됐고, `axes-count` 기대값을 3축 고정에서
     「그 대상이 실제로 가진 축」으로 고치게 만들었다(고정이면 옳은 리포트가 FAIL 난다).
  2. **`leaders: []`인 항목이 3개 생겼다 — 리포트가 「선도가 없다」고 *명시한* 자리다.**
     robotics 「교대 근무 길이 전원계」, ai-datacenter-ops 「유연부하 운전 표준화」·「가속기 내구연한 실측」.
     각각 "해법을 제시한 주체가 없다"/"표준 운전 절차가 없다"/"실측 표본 자체가 존재하지 않는다"고
     리포트가 적은 항목이라 선도기업을 지어내는 것이 `wrong` 쪽이다. rationale에 **왜 비웠는지**를
     문장으로 남겨 빈 칸이 누락이 아니라 판단임을 드러냈다.
  3. **계획이 예상한 「7항목 축」은 나타나지 않았다(최대 5).**
     계획 S7이 "7항목이면 278px에서 조각 폭이 좁아지니 육안 확인"을 경고했는데 실제 최대는 5였다.
     **프로브 편의를 위해 항목 수를 부풀리지 않았다** — 축 항목 수는 분해의 자연스러운 결과여야지
     관측 대상을 만들려고 늘릴 값이 아니다.
  4. **DOM 대상이 solid-state-battery → ai-datacenter-equipment로 바뀌었다(의도된 결과).**
     대상 선정을 「목록 순서」에서 「항목 수 최대, 동률은 slug 사전순」으로 바꿨고, 백필 후 14항목이
     동률이 되어 사전순 앞선 쪽이 선택됐다. 결정적이라 재실행 간 흔들리지 않는다.

## 확인된 것

- **`rule` 커버리지 97이 전수 실행의 산술 증명이다** — `Σ(축수×5 + 2)` = 3축 5종×17 + 2축 1종×12 = 97.
  숫자가 축 구성과 정확히 맞아떨어지므로 "6종을 돌았다"가 출력만의 주장이 아니다.
- **주입 대조군이 실제로 작동했다** — 3축이 실재하는 대상을 `null`로 덮었는데 `control-axes0`이
  통과했다. 즉 축 0개 관측이 「데이터가 원래 없어서」가 아니라 **「빈 상태를 옳게 그린다」**의 증거다.
- **배지 testid는 vitest 로컬 픽스처만 단언한다** — 라이브 프로브는 `card-anatomy-pending`을 안 본다.
  그래서 5종을 채워도 기존 프로브 전수 재실행이 불필요했다(계획의 조건부가 grep으로 해소됨).

## 다음에 다르게 할 것

- **데이터를 채우는 작업은 「하한 래칫을 진값으로 올리는 것」까지가 그 작업의 일부다**(승급됨).
- **발행 前에도 baseline과 대조**할 것 — 발행기가 발행 *전에* 본문 md5를 baseline과 비교해
  불일치면 중단하게 했더니, 외부(루틴 등)가 그 사이 본문을 바꾼 경우를 자동 감지하는 축이 됐다.
  "발행 후 보존 확인"만 있으면 내가 덮어쓴 건지 원래 달랐던 건지 구별되지 않는다.
- **`producers[]`를 한 건도 채우지 않은 것은 의도된 절제다** — 업체별 생산 점유율의 신뢰 출처를
  못 구했고, `share_pct`(판단값)와 `top_source_pct`(출처 있는 외부 사실)의 구별이 스키마에 이미
  설계돼 있어 그 경계를 지켰다. 확인한 4건만 수치를 실었다(티타늄 중국 70% · 희토류 중국 ~70% ·
  우라늄 카자흐스탄 ~40% · 니켈 인도네시아 62%). 나머지는 산지국만.

## 문서 갱신

- **`.claude/skills/live-uat-probes/SKILL.md`** — 신규 항목 ①(대조군 소멸 → `page.route` 주입 합성 +
  `control-injected` sentinel)·②(재는 대상이 사라지면 프로브는 침묵한다 → 보유 수 하한 래칫).
- 승급하지 않음: 광물 축 생략 판단(ADR-0042 결정 2의 적용 사례일 뿐 새 결정 아님) · `leaders: []` 관례 ·
  7항목 축 미출현.

## 후속 후보

- **루틴 upsert의 `composition` 클로버 방지** — 계획이 비목표로 명시한 그대로 남는다. 현재 계약은
  「키 생략 = 삭제」라 루틴이 한 번 돌면 6종 해부가 사라질 수 있고, 그때 **프로브의 `floor` 축만이**
  그것을 드러낸다. 계약을 「생략 = 보존」으로 바꾸려면 5필드(`key_points`·`milestones`·`variants`·
  `watch_items`·`composition`) 전체와 ADR·명세 3종이 함께 움직여야 한다.
- `producers[]` 6종 전부 비어 있음 — 신뢰 출처를 확보하면 채울 수 있다.
