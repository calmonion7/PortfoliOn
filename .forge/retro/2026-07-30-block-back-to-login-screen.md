# 회고 — 로그인 후 뒤로가기로 로그인 화면 재진입 차단 (task#245)

- **실행**: 2026-07-30 · 커밋 `fa7b15f` · **회고**: 2026-07-30 (fg-next all 자동 스킵 → 일괄 승급)
- **검증**: vitest 222 passed(26파일) · 라이브 프로브 단언 14건 PASS·FAIL 0·**미측정 4건**

## 이 작업의 두 학습은 이미 승급돼 있다

task#246이 `332cb91`로 CLAUDE.md에 올렸다:
- **라이브 UAT ⑤** — Playwright로는 bfcache를 검증할 수 없다(chromium·webkit·firefox 전부, 대조군으로 확정). `--disable-back-forward-cache`를 `ignoreDefaultArgs`로 빼도 `BackForwardCacheDisabledForDelegate`가 남는다.
- **⑧ⓔ** — 0건을 대상 탓으로 귀속하기 전에 대조군으로 관측 가능성부터 증명하라. 그리고 **합성 이벤트를 쓸 땐 `isTrusted`로 자기 자극을 배제**할 것(이 작업의 D3 — `persisted×2`가 전부 프로브 자신의 `dispatchEvent`였고 "실제 복원 후에도 PASS"라는 **없는 증거**를 만들었다).

## 이번 일괄 승급에서 새로 CLAUDE.md에 올린 것

**⑧ⓖ — 메커니즘이 발동하지 않아도 *목표*는 게이트되게 판정을 2축으로 쪼개라.** (D5)

계획의 정직성 규칙은 "bfcache 미발동 시 **미측정**으로 라벨링"이었다. 정직하지만 불충분했다 — 1차 실행에서 뒤로가기 단언이 **통째로 '미측정'**이 되어 **목표 자체가 미검증으로 남았다.** ⓔ가 다루는 "0건" 상황에서 라벨링만 하면 정확히 이렇게 된다.

해법은 판정을 두 축으로 분리하는 것:
1. **목표 단언**("뒤로가기 후 로그인/앱 화면이 안 보인다")은 메커니즘 발동 여부와 **무관하게 항상 검사**
2. **메커니즘이 실제로 참여했는지**(`persisted && isTrusted`)는 **커버리지로 별도 보고**

그러면 목표는 게이트되고 메커니즘 미달은 정직하게 남는다. 일반화: **라벨링과 게이팅을 같은 축에 두지 말 것.** 이 작업이 최종적으로 도달한 형태이고(14 PASS / 미측정 4), 계획엔 없던 실행 중 보강이다.

## 검토했으나 승급하지 않은 것

- **D4 프로브 FAIL이 구현 결함이 아니라 프로브 결함이었다** — `button[title="로그아웃"]`를 `.first()`로 집었는데 PC 마스트헤드와 모바일 헤더가 **둘 다 같은 title 버튼을 DOM에 갖고 CSS가 한쪽만 숨긴다**(2개 중 1개 visible). 모바일에서 숨겨진 PC 버튼 클릭 → 타임아웃 → `.catch(()=>{})`가 삼킴. `:visible` 필터 + **전제 단언**(로그아웃이 실제로 세션을 끊었는가)으로 정정. → 이미 ⑧ⓒ("판정 범위를 좁혀라 · FAIL 시 완화 전에 정체를 실측")가 덮는다. 이번이 그 **재발**이며 규칙은 이미 있다. 다만 *부수 사실*("PC/모바일 표면이 같은 셀렉터를 공유하고 CSS로만 갈린다")은 코드베이스 사실로 남길 가치가 있어 여기 기록.
- **D2 가드를 훅 파일로 분리한 이유** — 이 코드베이스는 **테스트에서 App을 import하지 않는 관례**(`test/route-redirects.test.jsx`가 App의 매핑을 *재현*해 검증)라, 그 관례를 따르면 가드도 재현 테스트가 되어 실제 코드를 검증하지 못한다. 훅으로 빼면 App이 실제로 import하는 코드를 `renderHook`으로 단언할 수 있다. **테스트 가능성이 배치를 결정한 사례** — 유용하지만 이 앱 특유라 승급 대신 기록.
- **S3 mutation 확인** — 가드 조건과 `api.js`의 replace를 각각 깨뜨려 정확히 3건이 실패함을 확인해 "테스트가 실제로 게이트임"을 증명했다. 좋은 습관이지만 이미 일반 규약 수준.

## 남은 한계 / 후속 후보

- **실 bfcache 복원 경로는 여전히 라이브 미검증** — 실기기(모바일 Safari / 데스크톱 Chrome)에서 로그인→뒤로가기, 로그아웃→뒤로가기 1회가 유일한 남은 확인 수단. 가드 분기는 vitest 4분기 + 라이브 2단언으로 고정돼 있고, 남은 미지수는 "브라우저가 이 문서를 bfcache에 넣는가"뿐이다(task#246이 도구로는 불가임을 확정했다).
- **D6 (승인된 선택의 부작용, 미수정)** — OAuth 시작을 `replace`로 바꿔 **구글/깃허브 화면에서 뒤로가기로 우리 로그인 화면에 돌아올 수 없다.** 목표 달성에 이 변경이 필수는 아니었다(OAuth 사후 경로는 bfcache 가드가 덮는다). 사용자가 "세 경로 다 막기"를 명시 선택해 유지. 탈출로는 있다(구글 취소 → 백엔드 `?error=` → App의 oauthError 분기). 되돌리려면 `handleGoogle`/`handleGithub`만 `href`로 환원.
- **다중 탭 세션 동기화**(탭 A 로그아웃 시 탭 B가 앱을 계속 표시) — storage 이벤트 부재로 인한 **기존 성질**, bfcache와 무관. 범위 밖.

## 문서 갱신

- **CLAUDE.md ⑧ⓖ 승급**(목표/메커니즘 2축 분리).
- bfcache 검증 불가·`isTrusted` 자기 자극 배제는 task#246 `332cb91`로 이미 승급됨.
- ADR·CONTEXT 추가 없음.
