# 2026-07-24 — KR R&D 집약도 사업보고서 파싱 (verified: failed → 같은 날 fix-forward)

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 1차: `fnlttSinglAcntAll`(R&D 라인 구조적 부재) → 사업보고서 `document.xml` "연구개발비용" 표 파싱으로 재작성, fixture 3형 green·pytest 1298. **라이브 DART 대조에서 5종목 중 4종 sane, 035420(네이버) None → `verified: failed`.**
- fix-forward(사용자 승인): 비율행 분모 라벨 매칭을 `"매출액"` → `("매출" or "영업수익")`으로 확장, 2순위 매출행도 동일 확장. 라이브 재검증 **5/5 sane**(035420=18.5%), pytest 1299.

## 학습
- **fixture-pass-live-fail의 교과서 사례: 합성 fixture가 라벨을 "매출액"으로 지어냈고, 실제 네이버는 "영업수익"이었다.** 회사마다 표기가 갈리는 항목(계정명·분모 라벨)을 파싱할 땐 **fixture를 합성하지 말고 실제 문서에서 떠올 것**, 그리고 **라이브 N종목 대조를 완료기준에 넣을 것**(이번엔 계획이 그렇게 적어둔 덕에 배포 직후 잡혔다).
- **같은 라벨편차 패턴이 여러 경로에 있으면 전부 고쳐야 한다** — 1순위(비율행 직접)와 2순위(비용÷매출 계산) 두 경로에 동일한 분모 매칭이 있었다. 한쪽만 고치면 다른 입력 형태에서 다시 None이 된다(가토 전수 스윕).
- **graceful None 설계 덕에 "부분 실패"가 배포 안전했다** — 오값 저장 0, 구 코드(전건 None) 대비 순개선이라 되돌릴 필요가 없었다. 다만 **DoD 미충족이므로 봉인은 막았다**: 순개선과 완료는 다르다.
- 초기 진단이 본 17.3%는 구 FY2024값이고 파서는 최신 보고서 당기값(18.5%)을 올바로 선택했다 — **라이브 기대값을 세울 때 보고서 연도/기수를 함께 확인**할 것.

## 문서 갱신
- 승급 없음 — CLAUDE.md가 외부소스 파싱 라이브 대조 DoD를 이미 다루고(fixture-pass-live-fail 가족), 이 건은 그 최신 실사례로 retro에 보존.
