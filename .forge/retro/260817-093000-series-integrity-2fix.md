# 2026-08-16 — 시장지표 시계열 영구 손상(B60) + Naver 재무 위치 인덱스 파싱(B61) 해소 (task#303)

> fg-loop 드라이브가 자동 스킵한 회고를 일괄 승급(batch promotion 2026-08-17). 원재료는 봉인 아카이브
> `.forge/done/260816-124111-series-integrity-2fix/run.md`. 배포 `64311ee`.

## 계획 대비 실제

- **계획대로 된 것**:
  - S0(라이브 baseline)·S3·S4·S5가 전부 계획대로. B61은 발산 0 — `_naver_row_val`을 title 매칭으로
    바꾸고 위치 인덱스 소비처 25곳(kr.py 22 + report_generator 3)을 전환, 잔존 0.
  - **8표본 전수 실측이 alias 목록을 배제한 판단을 지켰다.** 계획이 "은행 지주도 「영업수익」이 아니라
    「매출액」 — Naver가 라벨을 정규화한다"를 근거로 alias를 비목표로 뒀는데, 라이브 4개사 × 분기/연간
    × 12지표 **96건 대조에서 불일치 0**이었다. KB금융의 `revenue: None`도 title 부재가 아니라
    `매출액 = '-'`(은행이 그 연도 매출을 안 냄)이라 **옛 인덱스 코드와 동일한 정상 결측**이었다.
  - TDD가 값을 했다 — 4축 중 3축이 실제로 red였고(지속 이동에서 옛 판정이 **91점**을 잘랐다),
    그 red가 판정축 교체의 정당성을 수치로 증명했다.

- **이탈**:
  1. **⭐ 판정축을 전역→국소로 좁히자 옛 축의 부수 효과 2개를 함께 잃었다(승급).** 비양수 점 사각과
     「쓰레기 2점 연속」. 둘 다 적대적 리뷰가 잡았고 **pytest·프로브는 원리적으로 못 잡았다** — 신규
     4축이 *새* 판정을 검증하도록 짜였지 *옛* 판정이 무엇을 막고 있었는지는 묻지 않았기 때문이다.
     → CLAUDE.md 「무거운 복구 동작을 가벼운 것으로 바꾸면…」 가토에 **판정축판**으로 승급.
  2. **저장 전용 필드가 응답으로 샜다.** 라우터가 서비스 dict를 그대로 반환하는 구조라 **저장 blob ==
     응답이 기본값**이고, `_raw_history`를 저장 dict에 넣는 순간 계획의 명시적 비목표(「응답 shape를
     바꾸지 않는다」)를 위반했다. 게다가 기존 테스트 **5건을 `KeyError`로 파손**시켜 DoD ⓔ가 미충족이었다.
     (미승급 — `cache._public()`으로 해소했고, 「저장/표시 분리」라는 이번 설계 고유 함정이다.)
  3. **⭐ 정지조건 자체가 불충실했다.** C6(e) 「history 길이가 baseline 대비 미감소」는 `cache.py`의
     **366일 롤링 컷오프** 때문에 정상 동작에 거짓 FAIL을 낸다(같은 세션에서 `indices.gspc` 252→251,
     `ks11`·`kq11` 245→243 실측). 그릴링의 렌즈 4(in-scope reachability)가 이걸 못 걸렀다 —
     **그 축이 도달 가능한지는 코드를 읽어야 알 수 있는데, 체크를 핀할 땐 아직 안 읽었다.**
     날짜 집합차로 교체해 해소(DB 20시리즈 3660점 · API 16시리즈 2641점, 결측 0).
     (미승급 — 가토 ⑥·⑤⑦의 관측 수 증가에 그침. 다만 「롤링 창 저장소에서 길이 비교는 불변식이
     아니다」는 사실 자체는 위 ADR-0040 결과 절과 이 회고에 남긴다.)
  4. **baseline을 재는 행위 자체가 프로덕션 DB에 썼다.** `/api/market/*`는 요청경로 증분(fx 패턴)이라
     **GET 한 번이 라이브 fetch + `_mc_save`**를 탄다. 사용자가 시장지표 탭을 여는 것과 동일한 앱 설계
     동작이라 규약 위반은 아니지만, 함의가 컸다 — **B60 손상은 배치만이 아니라 아무 사용자의 페이지
     로드 1회마다 누적되고 있었다.** `kospi_signal`은 배치 백킹이라 이동하지 않아 두 부류가 실측으로 갈렸다.
  5. **run.md 초고에 검증하지 않은 주장을 썼다가 스스로 잡았다.** 「시드 인공물(usdkrw `1350.0`,
     oil `100.0` 등)이 새 선두 런 가드에 제거된다」고 적었으나, 실제 값을 읽으니 본문 대비 **1.07~1.71배**라
     옛 판정에도 새 판정에도 안 걸린다. S0 보고가 이들을 「원 커밋이 겨냥한 쓰레기 첫 점」이라 서술한 것은
     **육안 인상**이었고 실측 비율이 그것을 기각했다. → **에이전트 보고의 *해석*도 검증 대상**이다.

## 확인된 것

- **라이브 10 시리즈에 옛/새 판정을 둘 다 적용해 대조**하니 결과가 **전부 동일**(양쪽 0점 제거)이었다.
  즉 이 태스크의 가치는 전적으로 forward-looking이고, 배포 후 화면이 그대로인 것이 **예측된 정상**이다.
  이 대조를 배포 *전에* 돌린 덕에 「반영 안 됨」 오진과 불필요한 `deploy.sh` 재실행을 원천 차단했다.
- 프로브가 낸 FAIL 2건이 앱 결함이 아니라 **계측기 결함**이었고(카드 해소 루프가 `<button>`에서 멈춤 /
  경고 방출 계층 오선택), 둘 다 「이 현상이 사용자에게도 일어나는가」를 먼저 물어 갈랐다.

## 다음에 다르게 할 것

1. **판정축·필터·검증 기준을 좁히는 슬라이스는 「옛 축이 부수적으로 배제하던 입력」을 전수 열거**한 뒤
   각각을 새 축이 여전히 배제하는지 회귀로 못박을 것(승급됨).
2. **저장/표시를 분리할 때 「저장용 필드를 어디에 두는가」가 곧 응답 shape**임을 착수 시 확인할 것 —
   이 저장소의 라우터는 서비스 dict를 그대로 반환한다.
3. **정지조건을 핀하기 전에 그 축이 재는 값이 어떤 메커니즘으로 변하는지 코드를 1회 직독**할 것.
   롤링 컷오프·TTL·트림처럼 **정상 동작이 값을 줄이는 경로**가 있으면 길이·개수 비교는 불변식이 아니다.

## 문서 갱신

- CLAUDE.md 「무거운 복구 동작을 가벼운 것으로 바꾸면…」 가토에 **판정축 교체판** 승급(이탈 1).
- ADR-0040(저장/표시 분리 · 판정축 교체 · 복구 안 함)은 착수 전 작성분이 그대로 유효 — 변경 없음.
- `.forge/codebase/CONCERNS.md` §0 B60·B61 해소 + in-run 보강 3건 서술(태스크 중 반영됨).
- CONTEXT 신규 용어 없음 · API_SPEC·CLAUDE_COWORK_API·README 무변경(확인됨).
