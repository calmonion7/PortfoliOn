# 2026-07-23 — 리포트 심층분석 본질가치 재구성 2/2 (표시층: 시장전망 섹션·4그룹 재배치)

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: `MarketOutlookSection` 신설(시장규모·CAGR·점유율 + 매출 CAGR 대조), report 탭 4그룹 재배치, README 갱신.
- 이탈: ① CAGR 계산에서 **consensus(예측) 행 제외를 즉석 추가** — 라이브 payload에 2026/2027 예측 행이 있어 실적만으로 계산해야 정확(에이전트가 실데이터 열람 중 발견) ② 리뷰 nice-to-have 2건 미수정(잠재).

## 학습
- **파생 지표(CAGR·성장률)를 계산할 땐 입력 시계열에 "예측 행"이 섞여 있는지 먼저 본다.** 실적+컨센서스가 한 배열에 담기는 구조에서는 `is_consensus !== true` 필터가 정확성의 전제다. 라이브 payload를 한 번 열어보는 것이 이 함정을 잡았다(fixture만 보면 안 보인다).
- **미수정으로 남긴 잠재 결함은 조건까지 적어야 나중에 재발견 비용이 0이 된다.** 이번 2건: ① `market_outlook:{}`처럼 present-but-empty면 빈 그룹 헤더가 노출될 수 있음(형제 섹션도 동일 잠재 — 실제로 task#207 L1으로 발굴돼 #208에서 수정됨) ② `computeRevenueCagr`가 기간 갭이 아니라 **행 수로 지수를 계산**해 비연속 연도 시계열이면 오스케일(라이브 2샘플이 연속이라 미발현). ①이 실제로 다음 헌트에서 잡힌 것은 "잠재 기록"이 값을 냈다는 증거.

## 문서 갱신
- 승급 없음 — 잠재 결함 ②는 아직 미발현이라 retro에 조건과 함께 보존(발현 시 수정 후보).
