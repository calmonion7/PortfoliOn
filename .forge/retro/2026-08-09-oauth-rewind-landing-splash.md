# 2026-08-09 — 구글 로그인 착지 결함 수정 + 이탈~복귀 단일 로딩 화면 (task#285)

원자료는 `.forge/run.md`(봉인 시 `.forge/done/2026-08-09-oauth-rewind-landing-splash/run.md`).
커밋 `a66a817`(구현·문서) → `231f969`(라이브 프로브 신설 + uat283 스테일 축 보정).

## 계획 vs 실제

- **계획대로**
  - 8슬라이스 전부 착지. **원인 규명이 정확했다** — 사용자 진단 로그의 `rewind{base:2,len:3,delta:1}` + 6.4초 무기록 + 다른 코드에 `base:0`이라는 지문이 `markOAuthStart`의 기준값 과대계산 하나로 전부 설명됐고, 수정 후 라이브에서 그대로 사라졌다.
  - S7(커밋·푸시·빌드)을 S8(라이브 UAT)보다 **먼저** 배치(가토 ⑥ 3번째 관측의 처방). 그리고 빌드를 **메인 세션 단독 구간**으로 빼 라이브 측정 에이전트와 겹치지 않게 했다(task#283 이탈 3의 처방) — 이번엔 오염 0건.
  - TDD가 실질적으로 작동했다. S1이 기존 스텁(고정 `length`)을 미니 세션 히스토리 모델로 바꾸자 **바로 그 버그가 재현**됐다(`go(-1)` vs 기대 `-2`). 스텁이 상수였기 때문에 이 버그 클래스가 원리적으로 안 보였던 것이고, 모델을 바꾸는 것 자체가 수정의 절반이었다.
  - 대조군 설계가 라이브를 되돌리지 않고 성립했다 — `oauth_hist_len`을 `+1` tamper해 **처방만 무효화**하니 착지가 `fake-idp.test`로 재현됐다(가토 ⑧ⓚ②).

- **이탈**
  1. **가장 위험했던 순간은 적대적 리뷰가 `confirmed: false`로 내려놓은 발견이었다.** 렌즈3이 "`uat252`의 뒤로가기 단언이 이번 변경으로 스테일이 된다"를 HIGH로 올렸는데 `confirmed: false`였고, **내가 짠 워크플로우 스크립트가 `confirmed && (HIGH|MED)`만 수정 에이전트에 넘기도록 필터링**하고 있었다. 그대로 뒀으면 배포 직후 `uat252`가 FAIL하고 그게 **스테일 축이 아니라 앱 회귀처럼** 보고됐을 것이다. 그런데 그게 `confirmed: false`였던 건 렌즈3이 틀려서가 아니라 **확증할 수단이 없어서**다 — 그 시점엔 배포 전이고 서브에이전트에겐 빌드가 금지돼 있었다. → **가토 승급.**
  2. **적대적 리뷰가 HIGH 1건을 잡았고, 그 성질이 task#283 가토의 *역방향*이었다.** 우리가 창을 덮으려고 넣은 정적 스플래시를 **React `createRoot()`가 첫 커밋에서 지우고**, 그 순간 `authLoading`이 아직 true라 `App.jsx`가 `null`을 반환해 **덮으려던 바로 그 구간에 blank가 다시 생겼다**. 덮개는 있었는데 **인계 지점에 구멍**이 있었다. vitest도 라이브 프로브도 원리적으로 못 보는 자리였고(정적 노드→React 렌더 인계를 아무도 단언하지 않는다), auth·상태 표면에서 적대적 리뷰가 **3사이클 연속** 실질 결함을 잡았다. → **가토 승급.**
  3. **계획이 프로브 갱신을 `uat252` 하나로 특정했는데 `uat283`에도 같은 스테일이 있었다.** 이번 `pushState`로 히스토리가 한 칸 밀려 단일 `goBack()`이 **인증된 우리 오리진**에 착지하는데, 두 프로브가 모두 그걸 전제하고 있었다. 가토 ⑧ⓟ("프로브 갱신 지시를 *식별자*로 지목하면 같은 동작을 단언하는 다른 축이 시야 밖에 남는다")의 2번째 관측 — 지시를 "이 커밋이 바꾼 **동작**(히스토리 길이가 하나 늘어난다)을 단언하는 모든 축"으로 썼어야 했다.
  4. **Playwright 도구 한계 2건이 새로 실측됐다.** ⓐ `page.screenshot()`은 진행 중인 내비게이션과 겹치면 8초를 줘도 안 풀린다(never-resolve 라우트로 대조 확정) — raw CDP `Page.captureScreenshot`은 수십 ms. ⓑ `locator.click()`은 클릭이 트리거한 내비게이션이 끝날 때까지 반환되지 않으므로, 클릭 직후 DOM을 읽으려면 **클릭과 읽기를 같은 동기 `evaluate`에 묶어야** 한다. ⓑ가 특히 위험한 건 순진하게 짜면 프로브가 **"떠나기 직전 화면이 스플래시가 아니다"라고 앱을 무고**한다는 점이다. → **가토 승급.**
  5. **내가 프롬프트에 적은 `delta === 2` 추정치를 프로브 에이전트가 리터럴로 박지 않은 것이 옳았다.** 하니스가 가짜 IdP를 2클릭으로 밟아 홉 수가 달라 실측은 `uat285`=3 · `uat252`=4였다(실제 구글은 302 collapse로 또 다르다). 강제했으면 정당한 구현 위에서 거짓 FAIL이 났다.
  6. **육안 캡처가 자연 속도로는 스플래시를 못 잡았다 — 그리고 그건 좋은 소식이다.** 이 네트워크의 실제 왕복이 100ms 미만이라 스플래시가 지각 전에 사라졌다. 앱을 건드리지 않고 하니스에서 `route`로 메인 JS 응답만 4초 지연시켜 4조합(PC/모바일 × 다크/라이트)을 확인했다. 다만 **"실제 느린 모바일 네트워크에서 사용자가 지각 가능한 프레임으로 보는가"는 미측정**이다.
  7. **줄번호 drift 확인이 이번엔 지켜졌다 — 3번째 미준수를 막았다.** task#284 회고가 "완료기준으로 옮겨라"라고 했고 계획서 S6에 그대로 명시했더니 보정 20건이 실행됐다. 가토 ⑥("회고에 남긴 제약이 *지시*로 옮겨지지 않으면 무력하다")의 양성 사례다. 다만 **선재 drift 9건**은 HEAD 기준으로도 내용이 안 맞아 "무엇을 가리켰는지" 확정 자체가 불가능해 미해결로 남겼다(task#285발이 아니다).
  8. **task#264 절차가 값을 했다.** `uat252` 축을 뒤집기 전에 task#252 계획서를 읽어 판별하니, 라이브 완료기준은 **(a) IdP 아님 (b) 로그인 폼 없음 두 축뿐**이었고 'PROBE START 1회 정확 복귀'는 계획에 없던 부수 단언이었다. 그래서 (a)(b)는 보존하고 부수 단언만 의도 기준으로 교체했으며, IdP 축은 첫 착지만 보던 것에서 **모든 중간 착지**로 넓어져 오히려 엄격해졌다. 절차가 없었으면 통째로 완화하거나 정당한 수정을 스스로 막았을 자리다.
  9. **서브에이전트 금지 항목 위반 0건 — 4번째 연속.** 10에이전트 전원이 `git stash/checkout/restore/reset`과 프론트 빌드를 쓰지 않았고, red-first 실증은 전부 파일 내 편집→원복이었다.

## 학습

- **다음에 다르게 할 것**
  1. **적대적 리뷰 결과를 `confirmed`로 필터링하지 말 것.** `confirmed: false`는 "아니다"가 아니라 **"그 에이전트의 도구 범위 밖이다"**일 수 있고, 하필 그 범위 밖이 라이브·배포·타이밍처럼 **가장 늦게 터지는 표면**이다. 필터는 *심각도*로 걸고 미확증 발견은 오케스트레이터가 직접 확인할 것. 가토 ⑧ⓔ가 *프로브의 관측 한계*였다면 이건 **리뷰어의 확증 한계**다. → **가토 승급.**
  2. **덮개를 도입할 때는 "그 덮개가 언제·누구에 의해 걷히는가"를 함께 세라.** 초기 화면을 정적 HTML로 칠하면 프레임워크 마운트가 그것을 지운다. 인계가 끊기면 덮으려던 증상이 그 자리에서 그대로 재발한다. task#283 가토(무거운 복구 동작을 *제거*하면 그것이 덮고 있던 결함이 드러난다)의 **역방향 짝**. → **가토 승급.**
  3. **내비게이션과 겹치는 순간을 재는 Playwright 규약 2건**(위 이탈 4) + 왕복이 너무 빨라 육안 확인이 불가능하면 **앱이 아니라 하니스에서 응답을 지연**시킬 것(이탈 6). → **가토 승급.**
  4. **프로브 갱신 지시는 식별자가 아니라 *동작*으로 쓸 것**(이탈 3). 가토 ⑧ⓟ에 이미 있으므로 승급하지 않고 여기 기록만 한다 — 문제는 지식 부재가 아니라 계획 작성 시 이월 실패였다. 이번이 2번째 관측이니, 3번째가 나오면 계획 템플릿 쪽에 강제 장치를 넣을 것.
  5. **리터럴 대신 불변식**(이탈 5)·**기록된 결정 판별 절차**(이탈 8)·**줄번호 완료기준**(이탈 7)·**서브에이전트 금지**(이탈 9)는 전부 기존 가토의 재확인이라 기록만 한다. 문서를 늘리면 신호가 묻힌다.

## 문서 갱신

- CONTEXT.md 승급: **없음** — 스플래시·되감기·bfcache는 전부 구현 세부이지 도메인 용어가 아니다(task#283 회고의 판단 유지).
- ADR 추가: **없음**. 이번의 하드-투-리버스 결정(되감기 유지 · 되감기 폐기안 기각 · 팝업 OAuth 기각 · 스플래시 쌍둥이)은 실행 중 **ADR-0035 개정**으로 이미 들어갔다. 별도 ADR로 쪼개면 같은 결정이 두 문서에 흩어진다.
- 가토 승급: 학습 1·2·3을 CLAUDE.md에 반영 — **적대적 리뷰 `confirmed` 필터링 금지** · **덮개의 인계 지점** · **Playwright 내비게이션 타이밍 2제 + 하니스 지연 기법**. 더불어 task#278 줄번호 가토에 *"완료기준에 넣었더니 지켜졌다"* 양성 사례 한 줄 추가.
- 후속 큐:
  1. ~~**사용자 실기기 1회 확인**~~ → **✅ 닫힘(2026-08-09, 봉인 직전).** 관측: `로그인화면 → 로딩중 → 구글 로그인 화면 → 로딩중 → 리포트 화면` · `뒤로가기 문제 없음`. **로그인 폼이 다시 보이던 원증상이 사라졌고**(그 자리를 스플래시가 이어받았다) 뒤로가기 위생도 유지됐다. 이것이 이번 작업에서 **기계로 못 잰 유일한 축**이었다(bfcache 실복원은 Playwright 3엔진 전부 원리적 불가, task#246). 부수 확인: 구글로 **떠나기 전에도** 로딩중이 한 번 보이는데 이는 의도된 동작이다 — `flushSync`로 스플래시를 칠한 뒤 이동해야 bfcache가 복원할 프레임이 로그인 폼이 아니게 되므로, **그 프레임이 보이는 것 자체가 수정이 작동한다는 증거**다. → 실기기 확인이 설계 가정(“bfcache는 unload 시점 DOM을 복원한다”)까지 함께 검증했다.
  2. **`.forge/codebase/` 선재 drift 9건 → `fg-map` 재생성.** task#284 후속 큐 5번과 같은 대상이므로 함께 처리하는 게 싸다. 신규 파일(`oauthSplash.js`·`uat285`·테스트 4종)도 맵에 미등재다.
  3. **스플래시 테마가 `prefers-color-scheme` 기반**이라 앱 테마(`data-theme`/localStorage)와 어긋날 수 있다. 스플래시끼리는 동일하므로 이번 계약은 지켜지지만, 시스템 다크·앱 라이트 사용자는 스플래시(다크)→앱(라이트) 전환에서 한 번 튄다. 인라인 스크립트가 localStorage 테마를 읽어 `data-theme`을 세우면 해소된다(현재 YAGNI).
  4. **콜백 문서 2.8~4.1초의 성분 분해** — S5의 `resp`(responseStart)가 이제 실린다. 다음 실기기 로그에서 서버·리다이렉트 구간과 번들·마운트 구간이 갈리므로, 백엔드 OAuth 왕복 단축이 값이 있는지 그때 판단한다(계획 비목표 2). 현재 추정은 "지배분이 백엔드의 구글 서버간 토큰 교환"이고 근거는 같은 번들로 로그인 문서가 336~791ms에 뜬다는 대조뿐이다.
  5. **`SPLASH_HTML` 상수 전용 계약 주석** — `dangerouslySetInnerHTML` 소비처가 2곳이 됐다. 누군가 동적 값을 넣으면 XSS 벡터가 된다(구현 에이전트가 스스로 지적).
  6. **팝업/새 탭 OAuth** — 이론상 최선(히스토리 오염 0·문서 교체 0·SPA 재부팅 0)이나 안드로이드 PWA에서 `window.opener`가 null일 위험이 있어 리다이렉트 폴백 병행이 필요하다. 실기기 PWA 검증 선행(ADR-0035에 기각 근거 기록됨).
