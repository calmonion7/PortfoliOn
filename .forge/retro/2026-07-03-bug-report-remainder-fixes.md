# 2026-07-03 — 버그리포트 잔여 5건 수정 (이름 클로버 가드·마트 빈 CTE·FX 상수·단위 파싱·print→logger)

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: 5슬라이스 전부 수정, 전체 1092 passed + 프론트 빌드 green, 적대 리뷰 2렌즈 mustFix 0.
- 이탈: ① 계획이 print를 5곳으로 셌으나 실제 6곳(전부 전환) ② 적대 리뷰 minor 1건 in-run 반영 ③ S4 fixture 첫 시도 실패.

## 학습
- **테스트 단언이 항진식이면 버그가 있는 코드도 통과한다.** `test_storage.py`의 빈이름 테스트가 `A or B` 꼴이라 잘못된 SQL에서도 green이었다 — 적대 리뷰가 잡았다. 가드를 검증하는 단언은 **"고친 코드에서만 통과하고 되돌리면 실패하는가"**를 한 번 되짚을 것(위 task#99의 이빨 검증과 같은 원리).
- **파서 fixture는 파싱 대상뿐 아니라 그 앞단 *분류 조건*까지 충족해야 한다.** S4 fixture가 `_classify_table` 헤더 조건(수주잔고+기납품)을 놓쳐 파서에 도달하지 못했다 → 실제 표 구조로 보정. 합성 fixture를 쓸 땐 "이 입력이 파이프라인의 어느 단계부터 타는가"를 확인.
- 계획의 개수 추정(print 5곳)은 어긋나기 쉽다 — 착수 시 `grep -c`로 실수를 세고 전부 처리할 것.

## 문서 갱신
- 승급 없음 — 개별 가드 교훈으로 retro 보존. 라이브 스모크(개작 SQL) 필요성은 CLAUDE.md SQL 가토가 이미 다룸.
