# 2026-06-30 — FOMC 정책결정 일정 캘린더 추가 (정적 상수 + FRED 게이트 앞 배치)

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: `_FOMC_DATES` 정적 상수(2025~2027, 각 8회 2일차), FOMC 루프를 **FRED-key 게이트 앞**에 배치해 키 없어도 항상 노출, pytest 980 passed.
- 이탈 2건: ① additive 기능이 기존 캘린더 테스트 3건을 정당히 깨뜨려 갱신 ② 2027 날짜는 Fed 기준 **tentative**라 `2027-06-09`가 통상 중순 대비 이르다는 의심을 기록(먼 미래·비차단).

## 학습
- **기능을 additive로 추가하면 "그 달 이벤트가 없다 / FRED만 2건이다" 같은 *빈/정확수량 단언*이 깨진다.** 이건 회귀가 아니라 동작 적응이므로 (a) `empty` 단언은 "그 타입이 없음"으로 완화, (b) `len==N` 단언은 타입별 카운트로 분해, (c) 외부 실패 테스트는 "신규 항목은 생존" 단언으로 개명하는 게 정답. CLAUDE.md additive 가토의 *테스트 단언판*.
- **정적 상수로 외부 일정을 박제할 땐 "만료"를 코드 주변에 적어라** — 커버리지 끝 연도, 갱신 주기(연 1회), 소진 시 동작(미표시), 그리고 **tentative 구간 표시**. 이번엔 주석으로 남겼고, 2027 사용 시점 재대조를 회고에 남긴다.
- 키 없이도 동작해야 하는 항목은 **게이트보다 앞에** 배치해야 한다 — 이 순서 한 줄이 "FRED_API_KEY 미설정 환경에서 FOMC가 통째로 사라짐"과 갈린다.

## 문서 갱신
- 승급 없음 — CLAUDE.md 캘린더 절이 이벤트 타입을 이미 열거하고, additive 가토가 상위 가족을 다룸.
