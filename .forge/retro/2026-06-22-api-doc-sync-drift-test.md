# 2026-06-22 — API 문서 ↔ 라이브 라우터 drift 자동검출 테스트

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: `test_api_doc_sync.py` 신설(라이브 `app.routes` ↔ 두 문서 헤더 대조, `KNOWN_UNDOCUMENTED` 23개 동결), 전체 866 passed. 이탈 거의 없음 — 그릴링에서 알고리즘·정규화·베이스라인을 코드로 사전검증한 덕.
- 미세 이탈: 단언 1개를 `new_drift` / `stale_allowlist` 2메시지로 분리(실패 시 "새 drift냐 allowlist 정리냐"를 즉시 구분).

## 학습
- **가드(테스트가 산출물)인 작업의 완료기준은 "테스트가 통과함"이 아니라 "고의로 위반을 심었을 때 잡히는가"다.** 이번엔 3종 시뮬레이션(① 미문서 엔드포인트 추가 ② allowlist에서 빼고 미문서화 ③ spec에 죽은 엔드포인트)을 전부 돌려 CAUGHT를 확인했다. 이 단계를 빼면 항진식 가드(항상 통과)를 green으로 착각한다 — 이빨 검증이 가드의 UAT다.
- 실패 메시지를 원인별로 쪼개면 나중에 그 테스트가 깨졌을 때 진단이 0초다. 단언 1개로 뭉치는 것이 항상 더 깔끔한 건 아니다.

## 문서 갱신
- 승급 없음 — CLAUDE.md doc-sync 가토에 자동가드 존재·범위(존재만·스키마는 수동 DoD)가 당시 이미 반영됨.
