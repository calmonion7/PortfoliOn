# 2026-07-24 — 버그 헌트 4차 (4렌즈, 원시 5건 → 전건 CONFIRMED)

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: diff + blast radius 대상, finder 4렌즈 → 적대적 refute 검증. 9 에이전트·~140만 토큰·18.8분. 원시 5건 전건 CONFIRMED(HIGH 0·MED 4·LOW 1).
- 이탈: ① blast/계약 렌즈 0건(은밀 누락 아님을 교차 확인) ② 라이브 프로브가 계획보다 워크플로우 *내부*에서 선제 수행 ③ **검증기 refute 0건 — 2차와 동일 패턴 재현**.

## 학습
- **finder에 가토 시드를 주면 검증기가 보수적으로 기울어 refute가 0이 되는 경향이 있다**(2차·4차 연속 재현). 이건 "발굴이 완벽했다"는 뜻이 아닐 수 있으므로 **메인 세션이 전건 crux를 코드로 재확인**하는 단계를 고정하라(이번엔 5건 전부 재확인해 유지, 5차에서는 같은 습관이 심각도 과대 1건을 잡았다).
- **라이브 프로브가 판정에 필요한 건과 아닌 건을 구분하라.** "결함이 존재하는가"가 문서화된 결정적 동작(React primitive bail-out, Postgres `DISTINCT ON` 비결정성, 모듈 참조 부재)이면 정적 확정이 충분하고, 프로브는 "지금 얼마나 자주 터지는가"만 알려준다 → 생략 가능. 반대로 **외부 API 응답에 의존하는 건(#3 KR R&D)은 프로브가 유일한 확정 수단**이었고 실제로 fixture-pass-live-fail을 라이브로 못박았다.
- 빈 결과를 내는 렌즈가 있어도 정상이다 — "빈 findings 유효" 지시가 있어야 억지 발굴이 안 나온다.

## 문서 갱신
- 승급 없음 — 헌트 파이프라인 운영 교훈으로 retro 보존(5차가 계승).
