# 2026-08-19 — 주요기술 리포트 진입·복귀 동선: 목록 카드 「해부」 버튼화 + 해부 화면 목록 복귀 pill (task#309)

- 슬러그: `tech-anatomy-nav-ux` · 배포 `96fa0b5`(코드·테스트·프로브 한 커밋)
- 레인: fg-loop 무인 드라이브(회고 자동 스킵 → 2026-08-19 일괄 승급)
- 봉인: `.forge/done/260819-214202-tech-anatomy-nav-ux/` · `verified: yes`

## 계획 대비 실제

- **계획대로 된 것**: 6슬라이스 전부 착지. S2·S3·S4·S6은 이탈 0.
  프론트 vitest **70파일 695테스트 green**(신설 `TechAnatomy.test.jsx` 3케이스 포함) ·
  라이브 프로브 **218/218 ALL PASS**(커버리지 275 = 배포 전과 동일) · 육안 6장 · 줄번호 drift 0건.
  replan 라운드 **0** — fix-forward가 한 번도 필요하지 않았다.

- **이탈 3건** (전부 착수·실행 중 자체 포착, 목표·슬라이스는 불변):

  1. **⭐ 계획의 탭 타깃 산술이 원리적으로 미달이었다 — 지시대로 구현하면 완료기준을 못 넘는 자리.**
     결정 2가 「목차 칩 스타일 + padding만 `4px 10px` → `7px 12px`로 키워 ~32px 확보」였는데,
     인라인 박스의 content 높이는 `line-height`가 아니라 **폰트 메트릭**에서 나온다(`mono` 11.5px의
     `normal` ≈ 14px) → `14 + 14 + 2 = 30px < 32`. `lineHeight: '18px'` 명시로 34px을 만들었고
     배포 후 라이브 실측이 3뷰포트 전부 **정확히 34px**이라 계산이 확인됐다.
     **이건 가토 ⑥(「지시만으로 완료기준이 충족되는지 착수 시 역산하라」)의 양성 사례다** —
     역산을 안 했으면 배포 후 프로브 `card-tap-h` FAIL로 돌아와 fix-forward 1라운드를 썼을 것이다.
     부수로, 이 `<a>`가 `display:flex` 컨테이너의 자식이라 **blockify**되는 덕에 `clientWidth`가
     유효했다는 점도 확인했다(순수 인라인이었다면 `clientWidth === 0`이라 넘침 축이 무의미).

  2. **`nowrap`이 걸린 라벨에서 계획이 지정한 `scrollWidth` 축은 「1줄」이 아니라 「넘침 없음」을 잰다.**
     접힘이 원리적으로 불가능하므로 「1줄」 축은 지금 공허하고, 반대로 누군가 `nowrap`을 떼면
     접힘이 되살아나 **높이 하한 축이 접힌 판에서 높이 증가로 통과**한다(계획이 「이 단언이 통과하면서도
     깨질 수 있는 방식」에 적어둔 바로 그 구멍의 재등장 경로). 프로브에 이미 있던 `window.__lines`로
     줄 수 축을 분리하고 넘침 축과 **쌍으로** 뒀다(축 3 → 4).

  3. **프로브가 측정 대상 화면(목록)을 캡처하지 않고 있었다.**
     기존 캡처는 `{view}-anatomy.png`·`{view}-control.png`뿐이라, ⓙⓚ가 측정하는 목록 화면은
     **단언은 통과하는데 증거가 없는** 상태였다. ⓙⓚ를 재는 그 지점에서 `{view}-list.png`를 찍도록
     추가했다. 직전 회고(`tech-exposure-crosslink`)의 「스크린샷 *시점*을 단언 시점과 같이 설계하라」와
     **같은 규칙의 다른 얼굴**(저쪽은 시점, 이쪽은 대상)이다.

## 확인된 것

- **배포 전 red-first baseline을 직접 잰 것이 값을 했다.** 218단언 · FAIL **21건이 전부 신규 축**
  (pill 12 · card-anchors 3 · card-tap-h 3 · nav-roundtrip 3)이고 **선재 축 197건은 green**이었다.
  ⓚ의 실측(`카드마다 앵커 3개 — /tech-report/<slug>가 두 번`)이 계획의 진단을 **문자 그대로 확인**해 줬고,
  배포 후 `ALL PASS 218/218`이 회귀 없음의 증거로 읽힌다(선재가 미상이면 218/218도 아무 말을 못 한다).
- **task#264 판별 절차가 옳게 적용됐다.** `card-link-report`를 단언하는 기존 테스트를 뒤집기 전에
  task#306 계획서를 읽어 그 축이 완료기준·비목표 어디에도 없음을 확인했다(= 부수적 단언, 기록된 결정 아님).
- **task#301 처방(픽스처가 그 분기를 *타는가*)을 두 테스트 파일 모두에 적용**했다 —
  `composition == null` · `deriveAxes(fixture).length`를 직접 단언해 이빨과 커버리지를 분리해 뒀다.
- **워크플로우 없이 직접 실행한 판단**(프론트 4파일 + 프로브 1파일)은 적절했다 — 슬라이스 간 의존이
  선형이라 병렬화 이득이 없었고, 서브에이전트 오염 위험도 0이었다.

## 다음에 다르게 할 것

- **높이·탭 타깃을 수치로 약속하는 계획은 `line-height`까지 지시할 것**(승급됨). padding만 지시하면
  인라인 박스에서 산술이 조용히 어긋난다.
- **판정축을 고를 때 「현재 CSS가 그 실패 모드를 가능하게 하는가」를 먼저 물을 것**(승급됨).
  가능하지 않으면 그 축은 지금 공허하며, 그렇다고 빼지 말고 쌍으로 둬야 CSS가 바뀌어도 살아남는다.
- **육안 캡처는 「각 축을 측정하는 바로 그 지점에서」 찍을 것**(승급됨 — 시점·대상을 한 규칙으로 합쳤다).
- **정지조건을 쓸 때 그 도구가 실제로 쓰는 문자열을 확인할 것.** C7을 `verified: passed`로 적었는데
  forge의 어휘는 `verified: yes (<evidence>)`다(sealable 3값 = `yes`/`skipped`/`n/a`). 이번엔 의미로
  판정 가능해 무해했지만, 가토 ⑧ⓜ 4번째 관측(「정지조건에도 *어떤 자격으로 등장해야 통과인가*를 적어라」)의
  재발이다. **승급하지 않는다** — 기존 항목이 이미 이 클래스를 덮고 있고, 새 항목을 만들면 중복된다.

## 문서 갱신

- **`frontend/CLAUDE.md`** — 신규 가토 「인라인 요소의 세로 padding은 레이아웃 높이를 늘리지 않고,
  content 높이도 `line-height`가 아니라 폰트 메트릭에서 나온다」(+ flex blockify 짝 · 실천 3항 ·
  재발 토양으로 `TechReport.jsx` 목차 칩). **루트가 아니라 프론트 파일에 둔 이유**: 루트 CLAUDE.md가
  「프론트 전용 규약은 `frontend/CLAUDE.md`」로 라우팅하고, 그 파일은 `frontend/` 작업 시 자동 로드돼
  **필요한 순간에 읽힌다**.
- **`.claude/skills/live-uat-probes/SKILL.md`** — 3곳 보강: ⓐ 상단 육안 규칙에 「대상 화면」(⑨ 위 D3)
  ⓑ ⑨에 ⭐ 거울상 항목(`nowrap`이면 1줄 축이 공허하고 넘침 축이 실질 — 쌍으로 둘 것)
  ⓒ ⑰의 baseline 표 규율에 ⓒ항(「선재 FAIL이 0건이어도 **0이라고 명시**하라」).
- **승급하지 않음**: ADR(「하단 리포트 링크 제거」는 ADR-0042 결정 6 범위 안 + 되돌리기 쉬움 → 3조건 미충족) ·
  `CONTEXT.md`(신규 도메인 용어 0개) · `README.md`(:124 서술이 변경 후에도 문자 그대로 유효) ·
  `API_SPEC.md`/`CLAUDE_COWORK_API.md`(엔드포인트·스키마·게이팅 무변경) · fg-loop C7 문구 건(위 참조).

## 후속 후보

- **`TechReport.jsx`의 목차 칩이 아직 `padding: 4px 10px`(~30px)** — 같은 탭 타깃 미달이 남아 있다.
  다만 한 줄에 8~11개가 흐르는 밀집 UI라 같은 규칙을 적용하면 **세로 예산**(task#296의 128px 실측)을
  다시 건드리므로 별도 사이클이 필요하다.
- **카드 하단 여백은 여전히 죽은 클릭 영역** — 카드 전체를 클릭 영역으로 넓히려면 앵커 중첩 금지
  제약(task#306) 때문에 구조 변경이 필요해 이번 비목표였다.
- **미작성 분기는 라이브에서 dormant** — 6종 전부 해부를 보유해 `card-anatomy-pending`은 픽스처에서만
  렌더된다. 「실측엔 해당 없음」을 면제 근거로 쓰지 않고 vitest 두 분기 + 게이트 식으로 닫아 뒀다(task#249).
- **`retro: skipped`로 봉인된 태스크가 이 건 포함 13건** — 나머지 12건은 예전 fg-loop/fg-next all
  드라이브의 잔여분으로, 원하면 일괄 승급 모드로 따로 훑을 수 있다.
