# 2026-07-26 — 버그 헌트 5차 (5렌즈 14 에이전트, dedup 9 → 8 CONFIRMED / 1 REFUTED)

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: 기준선 11커밋, 5렌즈 fan-out(원시 11건), 적대적 검증 후 리포트 5차 교체(1~4차 이력 승계), 코드 미수정. 검증 상한 10 대비 9건이라 **dropped 0**.
- 이탈 6건 — 아래 학습 참조.

## 학습
- **검증기가 CONFIRMED한 건도 메인 세션이 재확인해야 한다 — 이번엔 심각도 과대 1건을 잡았다.** finder·검증기 *양쪽*이 M1을 HIGH로 봤으나(둘 다 "2행 카테고리까지 사라져 형제 탭 전환 불가") 실제 코드는 `sections`를 무조건 렌더하므로 링크는 남고 하이라이트만 꺼진다 → 고립이 아니라 컨텍스트 상실(MEDIUM). **판정문에 코드 인용이 붙어 견고해 보여도 결론의 스케일은 과할 수 있다.** 존재 판정은 5렌즈 모두 정확했으니, 다음 사이클엔 finder에 "심각도 근거를 UI/데이터 영향으로 구체화하라"를 넣을 만하다.
- **dedup으로 건수가 줄면 "유실"인지 "수렴"인지 원본 전사로 확인하고 리포트에 명시할 것.** 이번엔 3개 렌즈가 독립적으로 같은 지점을 지목한 수렴이었다(조용한 병합 금지).
- **워크플로우 사후 감사를 계획하면 필요한 원시 데이터를 스크립트 `return`에 담아야 한다** — `journal.jsonl`의 `value`가 전 에이전트 `null`이라 결과 복원에 쓸 수 없었고 `agent-*.jsonl` 전사에서 StructuredOutput 입력을 직접 파싱해야 했다. 다음엔 `rawAll`을 return에 포함.
- **기각 건의 반증 논리도 감사하라** — 잘못된 기각도 미발굴이다(R1의 반증 근거를 직접 확인하고 기각 유지).
- 라이브 프로브는 "8건 전부 정적 확정"이면 최소로 족하다(배포 번들에 문자열 실재·라우터 마운트 401 2종만).
- 도구 관찰: zsh에서 `grep --include=*.jsx`는 글롭 확장으로 실패 → 따옴표 필요. `sed`는 프로젝트 규칙상 차단(Read로 대체).

## 문서 갱신
- 승급 없음 — 헌트 파이프라인·워크플로우 감사 교훈으로 retro에 보존(다음 사이클이 읽을 fuel).
