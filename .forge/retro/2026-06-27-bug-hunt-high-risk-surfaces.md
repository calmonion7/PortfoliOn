# 2026-06-27 — 버그 헌트 고위험 표면 (62 에이전트, 원시 50 → 확정 42건)

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: 12 finder 렌즈 fan-out → 원시 50건 → 적대적 검증 45 통과/5 기각 → (file,line) 병합 42건(HIGH 9·MED 18·LOW 15)을 `.forge/bug-report.md`로.
- 이탈(낮음): 대상 표면 산출을 별도 scope 에이전트 대신 워크플로우 스크립트에 정적 인코딩(기능 동일·산출 파일 없음). 직전 cleanup 루프가 남긴 orphan 1건(vite.config 死 pwaAssets)도 부수 발견.

## 학습
- **적대적 검증 단계는 실제로 10%를 걷어낸다(50→45).** 기각된 5건은 "market_cache NaN은 DB가 거부", "OAuth id_token 서명검증은 이미 있음" 등 *코드를 더 읽으면 무너지는* 주장들이었다 — finder 산출을 그대로 리포트에 싣지 말 것.
- **메인 세션의 헤드라인 spot-check가 필수다.** 이번엔 보안 클러스터(무인증 엔드포인트)를 라이브 코드로 직접 재확인해 검증기 판정과 일치함을 확인했다. 검증기 판정문이 그럴듯해도 헤드라인 급은 사람이 crux를 다시 본다.
- **같은 결함을 여러 렌즈가 독립 발견하는 것은 노이즈가 아니라 신호다** — (file,line) 동일이면 병합하되, 인접하지만 distinct한 것(같은 파일 다른 실패 모드)은 분리 유지. 병합 사실을 리포트에 남겨야 "왜 렌즈 수보다 건수가 적나"를 나중에 의심하지 않는다.

## 문서 갱신
- 승급 없음 — 헌트 파이프라인 운영 교훈으로 retro에 보존(후속 사이클 #207·#221이 이 규약을 계승).
