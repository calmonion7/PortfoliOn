# 2026-07-25 — 이벤트 구동 분석 파이프라인 (배치 완료 → 루틴 fire, verified failed → 같은 날 fix-and-re-run)

일괄 승급 회고(2026-07-28) — 학습 출처: 아카이브된 `run.md`.

## 계획 대비 실제
- 계획대로: `cowork_trigger.py`(env 미설정 휴면·best-effort), `_generate_all` 말미 자동 fire, admin fire 엔드포인트. 테스트 10종 포함 1323 green.
- 이탈: 1차(클라우드 루틴)가 `verified: failed` → 같은 날 fix-and-re-run으로 **로컬 리스너** 방식으로 전환.

## 학습
- **전송처를 추상화해 둔 덕에 클라우드→로컬 전환에서 백엔드 코드가 무변이었다(URL만 교체).** 외부 트리거를 붙일 땐 "어디로 보내는가"를 한 지점(env + 얇은 모듈)에 모아두면, 전송 방식이 틀렸을 때 fix-forward가 설정 교체로 끝난다 — 이번 실패가 재작업이 아니라 재배선이 된 이유.
- **`verified: failed`는 재앙이 아니라 정상 경로다** — 같은 날 fix-and-re-run으로 닫혔고 새 `run.md`가 그 이력을 담았다. 실패를 `skipped`로 우회하지 않은 것이 이력의 정확성을 지켰다.
- env 미설정 시 **휴면(dormant)** 이 기본이라 코드를 먼저 머지해도 무해했다 — 외부 연동 신규 배선의 안전한 기본값.

## 문서 갱신
- 승급 없음 — 파이프라인 구조는 CLAUDE.md/ADR이 이미 다루고, 추상화 이득은 실행 교훈이라 retro에 보존.
