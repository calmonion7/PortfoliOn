# 2026-08-10 — 사용자 경계 확정 버그 4건을 닫으며: 배포 모델의 빈칸이 계획의 완료기준을 무너뜨렸고, 리뷰 발견의 *원인 귀속*이 한 칸 어긋나 주 도달 경로가 열린 채 봉인될 뻔했다

## 계획 대비 실제

- **계획대로 된 것**
  - S1~S4 4건 전부 red-first로 착지(`expected {access_token:'t'} to be null` · `assert 200 == 403` · `assert 2 == 1` · 모듈 부재). 의존성 교체 후 **전체 스위트 첫 실행이 그대로 green**(backend 1614 · frontend 573) — 계획이 "선제 override 추가 금지, 실제로 깨지는 것만" 이라 못박은 그대로 **실제로 깨진 것 0건**이었다. `_nonadmin_app`이 `require_admin_or_api_key`를 override하지 *않는* 상태가 오히려 403 판정을 실제 의존성으로 태워 줬다.
  - 착수 시 계획의 전제 4종을 재확인해 **전부 일치**(`§0` 20건 · `API_SPEC` 넓은 패턴 `불필요` 1건 · README PWA 서술 0건 · `_nonadmin_app` override 2종 + 기존 403 패턴). "계획이 전제한 상태를 착수 시 1회 재확인하라"를 계획이 스스로 지시해 둔 것이 값을 했다.
  - 서브에이전트 금지(git 되돌리기·commit/push·프론트 빌드) **12 에이전트 전원 0위반**, 결함 주입 렌즈를 파일별로 배타 분할해 **형제 오염 관측 0건**(task#288 처방의 2번째 양성 사례).
  - S7 줄번호 drift: 보정 0 · 표본 3 · 빈 표본 9 · 선재 0. 완료기준에 grep을 박아 둔 것이 그대로 이행됐다(task#285가 세운 처방의 2번째 준수).

- **이탈**
  1. **⭐ 계획의 S6 순서 전제가 사실과 달랐다 — 그리고 원인은 계획이 아니라 이 저장소의 *배경지식*이었다.** 계획은 `commit+push → npm run build(=즉시 라이브) → 프로브`로 짜고 "원한다면 **빌드 전에** 프로브를 1회 돌려 라이브 red-first를 확보할 수 있다"고 적었다. 실제로는 **`deploy.sh:25`가 `(cd frontend && npm install && npm run build)`를 돌린다** — 푸시 20초 뒤 배포가 프론트를 빌드해 라이브에 올린다(실측 4종: `gh run list` success `12:18:38Z` · `frontend/dist/*` mtime 21:18 · 라이브 번들 `index-DQPpY8WN.js` == 로컬 `dist` · 라이브 `sw.js`의 `api-cache` 0건). 결과 ⓐ 내가 실행할 별도 빌드 단계는 **불필요**했고(실행하지 않았는데도 라이브는 새 번들) ⓑ "빌드 전 라이브 red-first"는 **push-first 순서에서 원리적으로 불가능**했다. 계획이 그렇게 쓰인 건 `CLAUDE.md` 배포 절에 **`deploy.sh`가 프론트를 빌드한다는 사실이 빠져 있어서**다 — "프론트=즉시 라이브 / 백엔드=재배포 후"라는 비대칭 서술만 있어 푸시 한 번이면 둘 다 간다는 게 안 읽힌다.
     한 번 오진할 뻔했다: 빌드 전이라 믿고 돌린 프로브가 ⓐ·ⓑ 모두 PASS해서 **"앱이 안 고쳐졌거나 프로브가 못 본다"로 읽힐 자리**였는데, 추정으로 귀속하지 않고 `deploy.sh`·`gh run list`·dist mtime·번들 해시로 확정하니 **이미 배포가 끝나 있었던 것**이었다.
  2. **⭐ 적대적 리뷰 MED의 현상은 맞고 *원인 귀속*이 한 칸 어긋나 있었으며, 그 어긋남이 "스코프 밖" 분류를 만들었다.** 렌즈 2가 「SW 등록이 `window.load`에 게이팅돼 ADR-0036의 '보호창이 매 방문마다 좁아진다'가 낙관적」을 MED로 냈고, 그 귀속대로면 "이번 diff 4파일 밖"이라 수정 에이전트가 이월했다(리뷰어 자신도 "인지만 해두면 된다"고 적었다). 파고드니 진짜 결함은 그 옆이었다 — **`App.jsx::doLogout`이 SPA 전용**(코드 직독: `localStorage.removeItem` 2회 + `setSession(null)`, 리로드·내비게이션 없음)이라 **부팅 1회 퍼지가 재실행되지 않고**, 하필 그게 B47의 문서화된 **주 도달 경로**("A 로그아웃 → 5분 내 같은 브라우저에서 B 로그인")다. 즉 옛 SW가 살아 있는 전환 창에서 주 경로가 열린 채였고, ADR-0036 결정절의 "부팅 1회 삭제가 전 경로를 단일 지점으로 덮는다"는 근거가 그 창에서 성립하지 않았다(그 근거의 전제 "아무도 재적재하지 않는다"가 전환 창에서 거짓). 해소: `doLogout`에 `purgeApiCache()` **1줄** + ADR-0036 결정절 보정·결과절 재작성 + `CONCERNS §7.6`에 전환 창 단서.
  3. **심각도 필터가 값 있는 LOW 2건을 수정 페이즈에서 떨어뜨렸다.** 워크플로우는 `confirmed`로 필터하지 않았지만(task#285 준수) HIGH/MED로는 필터했다. 남은 LOW 4건 중 2건이 실질 — 「부팅 퍼지가 거절을 로그 없이 삼킨다」(전환 창의 **유일한 방어선**인데 실패가 관측 불가 → `console.warn` + 테스트 단언 추가)와 「`ARCHITECTURE.md §6.2`가 삭제된 4번째 분기를 여전히 서술」(계획의 S5 문서 목록에 그 파일이 **없었다** — task#274 가토의 재현). 나머지 LOW 2건(API_SPEC 표기)은 리뷰가 문서 페이즈 *전에* 돌아 이미 S5가 닫았다.
  4. **S4의 red-first가 단언 실패가 아니라 모듈 로드 파손**(`Failed to resolve import "../apiCachePurge"`)이었다. 신규 모듈이라 불가피하지만 "구현 없이는 통과 못 한다"만 증명하고 **단언의 이빨은 증명하지 않는다**. 워크플로우 종료 후(=유일한 편집자, 형제 오염 위험 0) 결함 주입 2회로 사후 검증 — 주입 A(try/catch·warn 제거) → 1 failed, 주입 B(`google-fonts`까지 삭제 + 침묵) → 3 failed(`정확히 1회` · `warn 1회` · `google-fonts 미접촉`). 원복 후 잔존 0.
  5. **신규 확정 버그 1건 발견 → `§0` 16 → 17건.** 렌즈 3의 형제 감사가 `report.py::refresh_analyst`(`:514`)·`::backfill_consensus`(`:566`)이 `user_id`를 받고도 본문에서 안 쓴다는 것을 잡았다(형제 `generate_one`은 `find_ticker(storage.get_all_stocks(user_id), ticker)`로 소유권 검사). 메인 세션이 코드 직독으로 재확인(리뷰어 줄번호도 검증 대상 — 이번엔 정확). B45와 동형이지만 별개 위치라 **B50으로 등재만** 하고 스코프 밖에 뒀다.
  6. **프로브 1차 실행의 2 FAIL은 프로브 결함이었다.** `ⓗ 프로브건전성`의 "비-GET/HEAD 0건" 축이 `POST /cdn-cgi/rum?` 3건을 세서 정상 구현을 거짓 FAIL시켰다. 임계를 건드리기 전에 성분을 분해하니 3건 전부 같은 경로였고 `grep -rn 'cdn-cgi' frontend/src backend` = 0건으로 **우리 앱 요청이 아님**(Cloudflare 엣지 주입)을 확정 → 판정 *범위*를 `/cdn-cgi/` 제외로 교정(완화 아님).
  7. **S5 docs 에이전트의 지시 외 자체 판단 1건 (정당).** `§14`의 구 7번(`put_backlog`·`clear_dashboard_cache`를 `require_admin`으로)도 함께 제거 — `§5.7`을 "닫힘"으로 재작성한 채 `§14`에 같은 항목을 미실행 후속 후보로 남기면 자기모순이라는 근거. 게다가 그 처방(`require_admin`)은 **애초에 틀렸다**(프론트가 일반 사용자 경로로 호출하므로 admin 게이트면 정상 새로고침이 403).
  8. **B45 라이브 검증에 무인증 대조군을 추가**했다(계획은 API키 200만 요구). API키 **200** / 무인증 **401** / `backlog_history` **96 → 96**. 대조군이 없었으면 200이 "게이트 통과"인지 "게이트 부재"인지 구별되지 않는다.
  9. **셸 cwd 잔존으로 거짓 에러 1회** — `cd frontend && …` 다음 호출에서 상대경로가 `frontend/frontend/…`로 풀렸다(task#288 이탈 6과 동일 함정). 절대경로로 해소.

## 학습

- **다음에 다르게 할 것**
  1. **⭐ 계획이 "X를 하면 Y 상태가 된다"를 근거로 완료기준을 걸면, 그 상태 전이를 실제로 만드는 스크립트를 1회 직독할 것.** 이번엔 순서 처방(task#253의 "빌드가 UAT보다 앞")을 **정확히 따랐는데도** 완료기준이 달성 불가였다 — 계획이 틀린 게 아니라 그 계획이 읽은 **배경지식(배포 절)이 불완전**했기 때문이다. 가토 ⑥의 역산("지시만으로 기준이 충족되는가")은 **배경지식이 옳다는 전제 위에서만** 작동한다. 이 저장소의 상태 전이는 전부 읽을 수 있는 파일(`deploy.sh`·`scripts/auto-deploy-poll.sh`)에 있으니 비용이 1분이다. → **가토 승급**(배포 절 보정 + ⑥ 5번째 관측).
  2. **⭐ 리뷰 발견의 이월·기각 판정은 발견의 *문장*이 아니라 그것이 가리키는 *코드*를 열어 확인할 것.** "스코프 밖"은 리뷰어의 원인 귀속에 의존하는 2차 결론이고, 귀속이 한 칸 어긋나면 그 결론도 어긋난다. task#285가 세운 "`confirmed`로 필터링하지 말 것"의 형제인데, 이번에 걸린 축은 **심각도 필터**와 **리뷰어의 스코프 판정** 둘이다. 값싼 LOW는 오케스트레이터가 직접 훑는다(이번 비용 ~10분, 수확 2건). → **가토 승급**(⑧에 병합).
  3. **신규 모듈의 red-first는 이빨 증명이 아니다** — 파일 부재로 인한 로드 파손이라 단언이 무엇을 잡는지는 못 보여준다. 구현 후 결함 주입 1~2회로 각 단언이 실제로 FAIL하는지 확인할 것. → **가토 승급**(⑥ 5번째 관측에 부수로).
  4. **줄번호 drift 클래스는 `ed98f72`(fg-map 전체 Refresh) 이후 구조적으로 사라졌다** — 맵이 `파일::심볼`·`§N` 앵커만 쓴다. grep은 계속 돌리되 **0건이 정상 기대값**이고, 맵을 재생성할 때 `파일:줄번호` 포인터를 되살리지 말 것. → **가토 승급**(drift 항목에 병합).
  5. **계획의 문서 목록은 "고칠 파일"이 아니라 "이 변경이 stale하게 만드는 파일"로 뽑아야 한다**(이탈 3). `ARCHITECTURE.md`가 목록에 없어서 §6.2가 남았고, 그걸 잡은 건 계획이 아니라 적대적 리뷰였다. task#274 가토("고칠 필드만 보지 말고 그 절 전체를 실응답과 대조")의 2번째 관측이지만, 이번엔 *필드*가 아니라 *파일*이 빠진 형태다. 기록만.
  6. **`wrong < missing`과 "무음 실패"는 다른 축이다**(이탈 3의 LOW 1). 퍼지가 거절을 삼키는 것은 `wrong < missing` 관점에선 합격(틀린 값을 안 만든다)이지만, 그것이 **유일한 방어선**일 때는 실패가 관측되지 않는 것 자체가 손실이다. 삼키되 침묵하지 않기(`console.warn`)가 정답. task#249의 "「틀리지 않았다」가 「괜찮다」를 뜻하지 않는다"와 같은 가족. 기록만.
  7. **대조군 하나가 판정을 만든다**(이탈 8). B45 in-container 검증에서 무인증 401을 함께 잰 덕에 200이 게이트 통과임이 확정됐다. 이빨 단언 규율의 저비용 사례. 기록만.
  8. **auth 표면의 적대적 리뷰가 6연속으로 값을 했다**(#283 HIGH · #284 LOW · #285 HIGH · #288 HIGH · #289 프로브 결함 2건 · 이번 MED→실제 HIGH급 1건 + LOW 2건 + 신규 버그 1건). 이번엔 **워크플로우 안의 조건부 리뷰**로 붙였고(별도 `fg-adversarial-review` 없이) 그것으로 충분했다. 기록만.

## 문서 갱신

- CONTEXT.md 승급: **없음** — 「SW 런타임 캐시」·「세션 경계」는 구현 세부이지 도메인 용어가 아니다(계획의 판단 유지, `grep -nE '캐시|세션' .forge/CONTEXT.md` 4건 전부 무관 문맥으로 확인). task#283~#289 회고의 일관된 판단과 동일.
- ADR 추가: **없음** — 이번 결정(`/api/*` 런타임 캐시 제거)은 **ADR-0036**이 계획 단계에서 이미 발행했고, 실행 중 발견된 `doLogout` 결함은 그 ADR의 **결정절 보정 + 결과절 재작성**으로 흡수했다(새 결정이 아니라 같은 결정의 근거 정정). 신규 ADR 3요건 미충족.
- 가토 승급(CLAUDE.md): 학습 1·2·3·4 반영 — **배포 절에 `deploy.sh:25`가 프론트를 빌드한다는 사실 + 라이브 red-first는 푸시 전에** · **⑥ 5번째 관측(배경지식이 불완전하면 역산도 통과시킨다 + 신규 모듈 red-first 이빨)** · **⑧에 심각도 필터·스코프 귀속 2축 병합** · **줄번호 drift 클래스 구조적 소멸**.
- 그 밖(실행 중 이미 반영): `.forge/codebase/CONCERNS.md` §0(B44~B47 제거, B50 등재)·§5.2·§5.7·§7.6·§14 · `ARCHITECTURE.md` §6.2 · `API_SPEC.md` 4곳 · `CLAUDE_COWORK_API.md` 1곳 · `scripts/uat288-oauth-boot-timing.mjs` 주석 stale 정정.

## 후속 큐

1. **B50 처방** — `refresh_analyst`·`backfill_consensus`에 `generate_one`과 같은 소유권 검사(또는 admin `scope=all` 정책에 맞는 멤버십 검사). `CONCERNS §0`·`§5.7` 등재됨.
2. **SW 등록을 `window.load`에서 앞당기기** — `injectRegister: 'auto'`의 `registerSW.js`가 등록을 `window.load`에 게이팅해 전환 창이 길다. `injectRegister: 'inline'` 또는 `DOMContentLoaded`/즉시 `register()` 검토. ⚠️ **창의 실제 길이는 미측정** — 추정으로 귀속하지 말고 계측부터(ADR-0036 결과절 기록).
3. **B48(에러 바운더리 신설)** — `§14`의 유일한 남은 후속 후보.
4. **`§0` 잔여 17건** — 이 문서가 이 저장소의 버그 백로그다. 다음 헌트는 여기서 고른다.
