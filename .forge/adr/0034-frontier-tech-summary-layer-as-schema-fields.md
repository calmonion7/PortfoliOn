# 0034 — 선도기술 리포트의 요약 레이어는 산문이 아니라 구조 필드로 싣는다 (그래프를 그리는 수치는 구조 데이터, 그리지 않는 요약 칩만 표시 문자열)

- 상태: 채택 (Accepted)
- 날짜: 2026-08-04
- 관련: task#281, [[ADR-0033]](0033-frontier-tech-report-non-ticker-resource.md)(비-티커 발행물·단위 규율), [[ADR-0027]](0027-analyst-report-separate-publication-resource.md)(발행물 모델·`points[]` 원형), [[ADR-0028]](0028-event-driven-routine-analysis-pipeline.md)(루틴이 콘텐츠 생산자), ADR-0006(기동 idempotent 마이그레이션), `CONTEXT.md`「선도기술 리포트」

## 맥락 (Context)

task#280이 상세 화면을 "지표·표 먼저, 산문은 하단"으로 재구성한 뒤에도, 이 문서에서 **가장 먼저 읽혀야 할 세 종류의 정보가 여전히 `description` 산문 안에만 있었다**:

1. **결론** — "이 기술의 요점이 무엇인가"가 4,700자 산문의 문장들에 흩어져 있다.
2. **연표** — "언제 무엇이 가동/착공/실증/계통연결됐나". SMR 실발행분에서 2020 로모노소프 가동 → 2023 HTR-PM → 2026 링룽 계통연결 → 2029 BWRX-300 → 2034 i-SMR이 **전부 문장 속에** 있었다. 이 기술의 핵심 정보가 정확히 그것인데도 화면 어디에도 축으로 존재하지 않는다.
3. **계보** — 업체가 어느 계열인가(SMR 노형: 경수형·비등수형·고온가스로·소듐냉각고속로). 표는 업체를 한 줄씩 나열할 뿐 계열로 묶지 못한다.

선택지는 둘이었다. **(A) 프론트가 산문을 파싱·요약한다** — 즉시 적용되고 재발행을 기다리지 않지만, 문장 추출 휴리스틱이 틀리면 화면이 발행물에 없는 사실을 말한다. **(B) 발행 스키마를 확장해 루틴이 채운다** — 정확하지만 다음 발행까지 빈 섹션이고, 컬럼 2개와 루틴 프롬프트 계약이 늘어난다.

여기서 [[ADR-0033]]과의 긴장이 생긴다. ADR-0033은 근거 항목에 **"표시용 문자열로 받기(심층 리포트 metrics 칩 방식) 기각 — 그래프를 못 그린다"**를 명시적으로 적어 뒀다. 그런데 이번에 추가하려는 `key_points[].metrics[].value`가 정확히 그 형태(`"1.1조원"`·`"22%"`)다. 경계를 적어두지 않으면 후임자가 **모순으로 읽고** 되돌리거나, 반대로 이 선례를 근거 삼아 시장 규모까지 문자열로 받는다.

## 결정 (Decision)

**1. 요약 레이어는 산문 파싱이 아니라 발행 스키마의 구조 필드로 싣는다.** (B)를 택한다 — `key_points[]`·`milestones[]`·`players[].category` 3필드를 추가하고, **프론트는 루틴이 쓴 것만 표시한다**. 산문에서 결론·연표를 추출하는 휴리스틱은 만들지 않는다(`wrong < missing`). 1/2가 도입한 `parseDescriptionSections`(대괄호 헤딩 → 접이식 소제목)는 *구조 분해*이지 *의미 추출*이 아니므로 이 금지선 안쪽이다.

**2. 세 필드는 전부 선택이고, 없으면 섹션째 생략한다.** 기술마다 채울 수 있는 것이 다르다 — 로봇·배터리엔 '노형' 같은 분류 축이 없고, 어떤 기술은 연표를 못 댄다. 빈 섹션(제목만 남은 유령)을 만들지 않으며, 게이트는 각 컴포넌트가 export한 채택 조건 순수함수를 페이지가 **그대로 호출**해 판정한다(`groupByCategory(...).length > 0` 등 — 느슨한 자체 판정을 쓰면 컴포넌트가 필터하는 입력에서 제목만 남는다).

**3. 경계: 그래프를 그리는 수치는 구조 데이터, 그리지 않는 요약 칩만 표시 문자열.**

- **구조 데이터로만 받는다(문자열 금지)** — `market.history/forecast[].size`(`{value, currency, unit}` enum), `market.cagr_pct`, `players[].share_pct`, `players[].tech_level`(1~5 이산), `players[].gap_years`(정수 년수), `difficulty.score`(1~5), **`milestones[].year`(정수)**. 이들은 곡선·축·밴드·타임라인 좌표를 만든다.
- **표시 문자열로 받는다** — `key_points[].metrics[].value` 하나뿐. 이 칩은 애초에 그릴 대상이 아니라 **읽을 대상**이고, 단위·통화·자릿수 표기를 루틴이 문맥에 맞게 고르는 편이 정확하다(`"2,700달러"`·`"134회"`·`"22%"`). 증감만 숫자(`change_pct`)로 받아 색을 결정론적으로 정한다.
- 따라서 **ADR-0033 결정 3이 기각한 것은 이 결정과 충돌하지 않는다** — 거기서 기각한 것은 *시장 규모를 문자열로 받아 성장 곡선을 못 그리게 되는 것*이었다. 판정 기준은 "문자열이냐"가 아니라 **"이 값으로 무엇을 그리는가"**다.

**4. `milestones[].status`는 추상 3단계 enum이다** — `done` | `in_progress` | `planned`. 구체 단계명("착공"·"계통연결"·"형식승인")은 기술마다 다르므로 자유 문자열 `event`가 담고, **색·마커는 이 3값이 결정론적으로 정한다**. 자유 문자열 상태를 허용하면 렌더러가 색을 고를 수 없거나 문자열 매칭 휴리스틱이 생긴다.

**5. 선택 필드는 `Optional[X] = Field(None)`으로 선언한다 — 예외 없음.** `x: float = Field(None)`은 키 생략만 통과시키고 **명시적 `null` 하나에 422**를 내 발행 요청 *전체*를 막는다(task#250 전체차단 실사례). 루틴이 "없음"을 `null`로 표현하는 것은 정상 입력이다. float 필드에는 `allow_inf_nan=False`를 함께 명시한다(task#211).

**6. 화면이 새 섹션을 보이려면 루틴이 채워야 하므로, 계약은 `scripts/cowork-routine-prompt.md`에 *지시*로 박는다.** 필드 의미·`status` 3값·`metrics` 상한 4개·"모르면 생략하되 산문에만 쓰는 것은 생략 사유가 아니다"를 프롬프트 본문에 쓴다. 설계 제약을 문서 한쪽에 배경지식으로만 남기면 무력하다.

## 근거 (Rationale)

- **(A) 프론트 산문 파싱** 기각: 문장 추출은 틀린 결론을 화면에 띄우고, 그 오류가 발행물에 근거가 없어 추적 불가다. 이 프로젝트가 반복해 택한 `wrong < missing`과 정면 충돌한다. 게다가 산문 형식은 루틴 프롬프트의 지시일 뿐 데이터 계약이 아니라(CONTEXT.md), 파서가 의존할 안정된 구조가 애초에 없다.
- **둘 다 채택**(파싱으로 즉시 + 스키마로 정확)도 기각: 같은 정보에 두 소스가 생겨 어느 쪽이 정본인지 화면이 답할 수 없다. 이 프로젝트가 이미 겪은 dual-source 가족(`tickers.name` ↔ `snapshots.data.name`)이다.
- **`key_points`를 `challenges`처럼 `{title, body}`로만 받기** 기각: 정량 근거가 다시 문장에 녹아 "한눈 구조화"(task#218)의 목적이 사라진다. 칩으로 분리해야 label/값이 스캔된다.
- **`metrics`에 구조 수치(`{value, unit, currency}`)를 요구** 기각: 이 칩이 담는 것은 금액만이 아니다(`"134회"`·`"1곳"`·`"48.9%"`). 단위 enum을 요구하면 표현 가능한 지표가 통화로 좁아지고, 실제로는 렌더러가 아무것도 계산하지 않으므로 얻는 것이 없다.
- **`status`를 자유 문자열로** 기각: 색·마커를 문자열 매칭으로 고르게 되어 `"완료"`/`"done"`/`"DONE"`이 각각 다른 렌더 결과를 낸다. 3값 enum이면 422가 발행 시점에 잡는다.
- **`category`를 enum·마스터 테이블로** 기각: 분류 체계가 기술마다 다르고(노형 ↔ 로봇 폼팩터 ↔ 전해질 계열) 4종이 고정도 아니다. 자유 문자열 + "없으면 전 업체 생략" 규율이 실질 비용 0이다.
- **계보를 DAG·트리 SVG로** 기각: 계보는 *묶음*이 본질이고 분류 간 방향 관계가 없다. 방향 없는 관계에 화살표를 그리면 그림이 데이터에 없는 사실을 말한다 — ADR-0033 결정 4가 `보완·경합`을 관계도에서 빼 칩 그룹으로 분리한 것과 같은 근거.
- **타임라인을 recharts로** 기각: ADR-0033 결정 4와 같은 근거(좌표가 순수함수면 vitest가 불변식을 직접 단언한다) + recharts 셀렉터 함정 3종(pie label이 별도 레이어 · 축 틱이 `.recharts-surface` 하위가 아님 · `.recharts-surface` 첫 매치가 범례 아이콘)을 원천 회피.
- **타임라인 SVG를 고정 `viewBox` + `width:100%`로** 기각: 컨테이너 폭에 비례해 **텍스트까지** 축소돼 350px에서 한글 라벨이 6~7px로 렌더된다(task#277 실측 — 기하는 전부 경계 안이라 넘침·잘림·겹침 축이 원리적으로 못 잡고 육안이 유일한 포착 수단이었다). 설계폭을 `minWidth`로 못박고 좁은 화면은 **자체 `overflow-x` 스크롤러**가 받는다(페이지 본문은 가로 스크롤하지 않는다).

## 결과 (Consequences)

- **컬럼 2개가 늘고 3표면을 쌍으로 유지해야 한다** — `app_schema.sql`(신규 설치) + `main._migrate`의 `ADD COLUMN IF NOT EXISTS`(라이브 DB, ADR-0006) + `save_report`의 INSERT 컬럼·VALUES·`ON CONFLICT DO UPDATE SET` **세 곳**. 한 곳만 고치면 배포 직후 발행이 깨지거나 upsert 경로에서 조용히 유실된다.
- **문서 5종이 계약에 묶인다** — `API_SPEC.md`·`CLAUDE_COWORK_API.md`·`scripts/cowork-routine-prompt.md`(박제본)·`README.md`(화면 구성)·`CONTEXT.md`. `test_api_doc_sync.py`는 엔드포인트 *존재*만 대조하므로 **이 필드들의 스키마 drift는 자동으로 잡히지 않는다** — 필드를 바꾸면 수동으로 5종을 함께 고쳐야 한다.
- **기존 발행물은 영구히 이 섹션이 없다.** 발행물은 불변이므로 구판의 `key_points`·`milestones`는 SQL NULL로 남고 `players[]`에는 `category` 키 자체가 없다. 소비자는 **배열 자리의 `null`과 `undefined`를 둘 다** 흡수해야 한다(`?? []`). 라이브 실데이터 2건(`smr`·`reusable-rocket`)이 정확히 그 상태이므로 구발행물 graceful은 이론이 아니라 현재 상태다.
- **새 섹션이 보이려면 루틴의 다음 발행을 기다려야 한다.** 30일 가드(ADR-0028) 때문에 자연 발행은 최대 한 달 뒤이며, 그전에 보려면 트리거 본문에 명시 범위 지정("smr을 재발행하라")으로 우선시켜야 한다 — 그것은 프로덕션 발행이므로 사람의 승인 사항이다.
- **경계가 문서로만 유지된다.** "그래프를 그리면 구조 데이터"는 코드가 강제하지 않는다 — 다음에 요약 칩이 아닌 값을 문자열로 받으려는 변경이 오면, 그것을 막는 것은 이 ADR을 읽는 사람뿐이다.
- **미커버로 남는 것**: 루틴이 세 필드를 *채울지*, 채운 내용이 정확한지는 어떤 코드도 검증하지 못한다. 스키마는 `status` enum·칩 상한·NaN 거부 같은 *형식*만 강제한다. 첫 실발행분은 사람이 한 번 읽어야 한다(ADR-0033과 같은 잔여 리스크).

## Amendment (2026-08-04, task#282) — 네 번째 요약 필드 `market.estimates[]`: 기관별 시장 추정 편차

같은 기술·같은 연도의 시장 규모를 조사기관마다 다르게 추정하는 편차(재사용 로켓 실측 3.4배 — `.forge/CONTEXT.md`「기관별 시장 추정 편차」)를 화면에 드러내기 위해 `Market.estimates: Optional[List[MarketEstimate]]`(최대 6건, `MarketEstimate = {institution, year, size, scope?, is_basis?}`)를 추가한다. 이 결정 자체가 새 필드는 아니지만(`key_points`·`milestones`·`players[].category`처럼 기존 결정 3의 연장), 아래 4가지는 그 세 필드에 없던 새 판단이라 이 amendment로 남긴다.

**① `estimates`는 새 최상위 컬럼이 아니라 `market` JSONB **안**에 둔다.** `save_report`(`services/tech_reports.py`)는 `market` 전체를 `json.dumps(payload.get("market", {}))`로 단일 컬럼에 쓴다 — `estimates`를 그 dict의 한 키로 추가하면 `app_schema.sql`·`main._migrate`·INSERT 컬럼 목록 어디도 건드릴 게 없다(변경 0, ADR-0006 관점에서 "컬럼 추가"가 아니다). 반면 `key_points`·`milestones`는 **최상위 컬럼**이다(이 ADR 본문 결과 절 "컬럼 2개가 늘고 3표면을 쌍으로 유지"). 이 비대칭은 자의적이지 않다 — 기준은 **"시장의 하위 사실이냐"**다. `key_points`·`milestones`·`players[].category`는 각각 결론·연표·업체분류로 **기술 전체**에 걸리는 독립 개념이라 자기 컬럼을 갖지만, `estimates`는 정의상 `market`(시장 규모)이라는 상위 개념의 **부분집합**(같은 지표를 보는 여러 관측)이라 그 JSONB에 자연히 속한다. 다음에 새 필드를 추가할 때 이 기준으로 최상위 컬럼 vs 기존 JSONB 내부를 가를 것.

**② `currency`·`unit`·`year` 동일 위반 시 422는 ADR-0033 결정 3("렌더러는 절대 추측·환산하지 않는다")의 직접 귀결이다.** `Market._estimates_consistency`(model_validator)가 배열 내 이 세 값의 집합 크기가 1을 넘으면 발행 자체를 거부하는 이유는, 렌더러(`MarketEstimates.jsx`)가 통화·단위를 환산하지 않고 `size.value`를 그대로 막대 폭 비율로 쓰기 때문이다. 통화가 섞이면(USD/KRW) 막대 길이가 환율 없이 비교되고, 단위가 섞이면(bn/tn) 1000배 오독이 나고, 연도가 섞이면 "같은 시점 편차"라는 이 필드의 존재 이유 자체가 무너진다(다른 연도끼리는 편차가 아니라 성장이다). 즉 이 검증은 새 규율이 아니라 **결정 3이 이미 정한 경계를 발행 시점(422)으로 앞당겨 강제**한 것 — 렌더러에 도달하기 전에 막아 "그리는 순간 발견"이 아니라 "쓰는 순간 발견"으로 옮긴다.

**③ 선택 배열은 예외 없이 `Optional[List[X]] = Field(None)`으로 선언한다.** `estimates: List[MarketEstimate] = Field([])`처럼 쓰면 루틴이 "없음"을 표현하려 보낸 명시적 `null` 하나가 필드 타입 검증에서 422를 내고, `market` 전체가 필수 필드라 이 422가 **발행 요청 전체**를 막는다(task#250 실사례 — 결정 5가 이미 정한 규율의 재확인). `estimates`가 결정 5 이후 추가된 필드라 이 함정을 원천 회피할 수 있었던 것은 우연이 아니라 **결정 5를 이 amendment 작성 시점에 다시 확인했기 때문**이다 — 다음 선택 필드도 착수 시 결정 5를 먼저 읽을 것.

**④ `scope`(집계 범위 차이를 적는 표시 문자열)가 문자열인 것은 결정 3의 경계 **안쪽**이다 — 결정 3을 어기는 예외가 아니다.** 결정 3의 판정 기준은 "문자열이냐"가 아니라 **"이 값으로 무엇을 그리는가"**다. `scope`는 막대·축·밴드 어느 것도 결정하지 않는다 — 순수하게 "왜 이 기관 숫자가 다른가"를 사람이 읽는 라벨(`MarketEstimates.jsx`가 `${institution} · ${scope}`로 이어붙일 뿐, 좌표 계산엔 쓰지 않는다)이다. 그래서 이 필드는 `key_points[].metrics[].value`(그래프를 그리지 않는 요약 칩)와 같은 부류이고, `size`(그래프를 그리는 구조 데이터)와는 다른 부류다 — 한 모델(`MarketEstimate`) 안에 구조 필드와 표시 문자열이 공존하는 것은 결정 3이 필드 단위가 아니라 **용도 단위**로 경계를 긋기 때문이다.

## Amendment (2026-08-12, task#296) — 산문 접기를 상시 노출 + 전역 목차로 교체, 표는 가로 스크롤 0

사용자 지시로 상세 화면의 **`<details>` 펼치기를 전부 제거**했다(산문 소제목 섹션 5개 + 업체별
`note` 9~12개). 아래 다섯 가지가 이 보정의 내용이다.

**① 접기 → 상시 노출 + 전역 목차 (뒤집은 결정)**

`task#280 S4`("섹션별로 접되 첫 섹션은 펼친다")·`task#282 S4`(전부 접힘)가 완료기준으로 못 박았고
이 ADR 본문도 "접이식 소제목"으로 서술했다 — 즉 **기록된 결정을 뒤집는 것**이므로 사람의 판단이
필요했고(task#264 절차), 사용자가 이번 요청으로 명시적으로 뒤집었다.

접기의 원래 목적은 **첫 화면의 산문 wall 제거**였다. 그것을 무엇이 대체하는가가 이 결정의 성립
조건이다 — ⓐ 산문은 여전히 본문 **끝**(출처 앞)에 있어 첫 화면에 오지 않는다 ⓑ 길어진 페이지의
항해는 **정적 전역 목차**(렌더된 섹션에서 파생한 앵커 칩)가 맡는다. 접기가 부수적으로 주던 것
(Ctrl+F가 닫힌 본문을 찾음 · `<summary>`가 키보드 포커스와 접근 이름을 공짜로 줌)은 상시 노출이
**더 강하게** 만족시킨다(닫힌 `<details>`는 브라우저의 `beforematch` 지원 여부에 검색이 갈렸다).
note의 접근 이름만은 `<summary>`가 사라지며 잃을 수 있어 `role="group" + aria-label`로 옮겼다 —
`aria-label`만 단 순수 `<div>`는 접근성 트리에서 이름을 갖지 못한다.

**② 목차의 위치는 KPI 스트립 *아래*다 (리드 문단 밑이 아니다)**

계획은 "리드 문단 바로 아래"였으나 라이브 실측이 그것을 기각했다 — 목차가 **128px**(높이 98 +
아래 여백 30)을 먹어 KPI 스트립이 첫 화면 밖으로 밀렸다(m390 스트립 bottom 686 > 가용 603 ·
m350 733 > 639). 「KPI 칩이 첫 화면에 보인다」는 `task#280`이 스트립을 압축해 얻은 **또 다른 기록된
결정**이고, 8~11칩은 278px에서 3줄이라 목차를 34px까지 줄이는 것은 원리적으로 불가능하다. 스트립
아래로 옮기면 스트립이 task#280 당시 좌표로 돌아가 **두 결정이 함께 성립한다**(m390 bottom 549).
목차는 여전히 모든 본문 섹션보다 위이므로 항해 목적을 잃지 않는다.

**③ 결정 1의 금지선은 그대로다**

산문에서 의미를 추출해 표·차트를 만드는 휴리스틱은 여전히 만들지 않는다(`wrong < missing`).
대괄호 헤딩 분해·문단 분리·소제목 승격은 *구조 분해*라 금지선 안쪽이고, 그 이상(노형 제원표·
확인지표 체크리스트)은 **루틴이 채우는 스키마 필드**여야 한다.

**④ 표의 가로 스크롤 제거는 「좌표계 축소 금지」와 충돌하지 않는다**

`task#277`이 확립한 금지 대상은 **폰트·좌표의 비례 축소**다(SVG `width:100%` + 고정 `viewBox`가
한글을 6~7px로 만들어 기하 축이 전부 통과하면서 읽을 수 없게 된 실측). HTML 표는 좌표계를 갖지
않고 **열 생략과 줄바꿈**으로 좁은 폭에 맞추므로 글자 크기가 줄지 않는다 → 표에는 그 금지가
적용되지 않는다. 그래서 표의 스크롤러(`overflowX:auto` + `minWidth:600`)는 제거하고, **SVG 표면
(진척 타임라인·연관 기술 관계도)의 스크롤러는 유지한다.**

부수 사실 하나가 이 경계를 실제로 시험했다 — `overflow-wrap: break-word`는 렌더에서 단어를 접지만
**스펙상 min-content 크기에 영향을 주지 않아** 표 자동 레이아웃의 최소폭이 "최장 단어"로 남는다.
그래서 스크롤러를 없앤 뒤에도 표가 좁아지지 못해 문서가 가로 스크롤했다(라이브 `reusable-rocket`
m350: 업체 열 181px → 4열 360px > 가용 278px). **`anywhere`**로 바꿔야 그 끊김 기회가 min-content
계산에 포함된다. 즉 "문자를 잃지 않는다"와 "표가 좁아진다"는 별개 축이고, 후자는 `anywhere`만
만족시킨다.

**⑤ 동적 열 생략의 판정식은 형제 게이트와 같은 식이어야 한다**

`playerColumns(players)`의 share 판정은 `Number.isFinite(p.share_pct) && p.share_pct >= 0`이며, 같은
페이지의 점유율 섹션 게이트·`ShareChart`의 채택 조건과 **문자 단위로 같다**. 갈리면 한 필드가 한
페이지에서 두 얼굴을 갖는다(`share_pct === 0`은 결측이 아니라 값이다). gap 열은 `gap_years != null`.

## Amendment (2026-08-12, task#297) — 다섯·여섯 번째 요약 필드: `variants[]`(계보 비교축)·`watch_items[]`(확인할 지표)

`.forge/CONTEXT.md`의 [[기술 계열]]·[[확인할 지표]] 용어가 먼저 정의됐다 — 두 용어 모두 발행물 4종
전수에서 확인된 축이라(SMR 노형·재사용 로켓 회수 방식·전고체 배터리 전해질 계열·로봇 기체 구성/형태,
그리고 4종 모두의 진척 판정 신호), 이번에도 결정 2("세 필드는 전부 선택")·결정 5(`Optional[X] =
Field(None)` 예외 없음)의 연장으로 스키마에 편입한다. 새 판단 4가지:

**① `variants`·`watch_items`는 `market.estimates`와 달리 최상위 컬럼 후보다 — Amendment(2026-08-04)
①의 기준을 그대로 적용한 결과다.** 그 기준은 "시장의 하위 사실이냐"였다 — `estimates`는 `market`(시장
규모)의 부분집합이라 그 JSONB 안에 자연히 속했다. `variants`(계보 비교)와 `watch_items`(진척 판정
신호)는 시장 규모의 하위 사실이 아니라 `key_points`·`milestones`·`players[].category`와 동렬로
**기술 전체**에 걸리는 독립 개념이므로, 같은 기준으로 최상위 컬럼(`app_schema.sql` + `main._migrate`의
`ADD COLUMN IF NOT EXISTS` + `save_report`의 INSERT 컬럼·VALUES·`ON CONFLICT DO UPDATE SET` 세 곳을
쌍으로 유지)이 맞다. **그 세 표면 배선은 task#297에서 함께 완료됐다**(라이브 실측: `tech_reports`
16컬럼 · `variants`·`watch_items` 둘 다 `jsonb`). 렌더(2섹션 표시)만 2of2 = task#298 소관이다.

**①-b 중복·빈 비교 방어를 스키마에 둔다(적대적 검토 CONFIRMED 2건).** `variants`는 처음에 중복 방어가
없어 `options[].name`이 같은 옵션 2개, `axis_label`이 같은 축 2개가 **201로 통과**했다 — 형제
`Market._estimates_consistency`(통화·단위·연도 동일성 + `is_basis` 중복)에는 있던 짝이 빠져 있었다.
중복이 통과하면 2of2가 같은 제목의 표를 두 개, 같은 이름의 행을 두 번 그려 독자가 서로 다른 두
계열로 읽는다 → `VariantAxis._option_names_unique` · `TechReportIn._variant_axis_labels_unique`.
또 `strength`·`tradeoff`가 **둘 다** 결측인 옵션도 통과해, 비교표의 그 행이 이름만 남았다 —
`options` 하한 2개가 *축* 수준에서 막으려던 「비교가 아니라 서술」 상태가 *행* 수준에서 되살아난
것이므로 같은 의도를 내려 `VariantOption._has_comparison_content`로 막는다.
⚠️ **최소 하나이고 둘 다 요구하지는 않는다** — 루틴이 한쪽만 아는 계열이 실제로 있고, 그때 발행
전체를 422로 막는 대가가 이득보다 크다(프롬프트는 여전히 "쌍으로"를 지시한다). 이 하한을 "쌍 강제"로
조이려는 다음 사람은 `test_publish_variant_option_one_side_only_201`이 왜 있는지 먼저 읽을 것.

**② `variants[].options[].examples`가 `{value, currency, unit}` 구조 데이터가 아니라 표시 문자열인
것은 결정 3의 경계를 그대로 적용한 결과다 — 새 예외가 아니다.** 결정 3의 판정 기준은 "문자열이냐"가
아니라 **"이 값으로 무엇을 그리는가"**다. `examples`(`"중국 ACP100 125MWe"`)·`strength`·`tradeoff`는
어느 것도 축·막대·곡선 좌표를 만들지 않는다 — 비교표의 셀에 그대로 표시되는 서술이다. 구조 데이터로
받으면(예: 각 example을 `{country, model, capacity_mw}`로 분해) 얻는 것이 없다 — 렌더러가 그 수치로
계산하는 것이 아무것도 없고, 오히려 계보마다 그 셀에 실을 수 있는 정보의 형태가 다르다(노형은 정격
출력, 회수 방식은 성공률, 전해질 계열은 이온전도도 — 공통 구조를 강제하면 표현 가능한 정보가 줄어든다).
`market.estimates[].scope`·`key_points[].metrics[].value`와 같은 부류다.

**③ `variants[].options`의 `min_length=2`가 `422`인 것은 "완화하면 안전"이 아니라 "완화하면 이 필드의
존재 이유가 사라진다"는 뜻이다.** 선택지 1개는 비교가 아니라 서술이고, 서술은 이미 `description`
산문이 담당한다. 하한을 1로 낮추면 루틴이 축 이름만 채우고 선택지를 하나만 넣는 퇴화 입력이 통과해,
화면의 "이점·대가 쌍" 비교 UI가 비교 대상이 하나뿐인 빈 표를 그린다 — `wrong < missing`이 아니라
`empty-comparison < missing`인 사례다. 완화 유혹이 드는 지점은 "루틴이 어차피 2개를 채우니 하한이
무의미하다"는 생각인데, 정확히 반대다 — **하한이 있어야 루틴 프롬프트가 "2개 미만이면 필드를 생략하고
산문에 써라"를 지시로 강제할 수 있다**(422가 그 지시의 이행을 발행 시점에 검증하는 것). 하한을 낮추면
이 강제가 사라지고, 축은 있는데 비교가 안 되는 섹션이 화면에 남는다.

**④ `axis_label`·`options[].name`의 상한(30자·40자)과 `strength`/`tradeoff`/`detail`/`not_signal`의
상한(120자·200자)은 결정 3의 근거 문단이 이미 설명한 것과 같은 이유다 — 이 필드들은 2of2에서 2열
비교표의 셀·체크리스트 카드에 들어간다. 상한이 없으면 모바일 278px 폭에서 한 셀이 수십 줄로 폭발한다
(`.forge/CONTEXT.md`가 이미 이 사실을 배경으로 깔고 "축 최대 2 · 축마다 계열 2개 이상"을 서술해 뒀다 —
그 상한들이 여기서 숫자로 확정된다). `watch_items[].not_signal`이 `label`/`detail`과 별개 필드로
분리된 것도 같은 근거의 다른 얼굴이다 — 한 문장에 신호와 오독 경고를 섞으면(가토 "한 문장이 두 역할을
겸하지 않게 쪼갠다") 화면이 그 둘을 시각적으로 분리해 보여줄 수 없다.
