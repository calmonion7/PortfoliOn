# PortfoliOn — Project Context

> **이 파일은 매 세션 항상 로드된다.** 작업별·디렉터리별 지식은 필요할 때만 읽히도록 아래로 분리돼 있다 —
> 해당 작업에 들어가면 **그쪽을 먼저 읽을 것**:
>
> | 어디 | 무엇 | 언제 로드되나 |
> |---|---|---|
> | `backend/CLAUDE.md` | 백엔드 모듈·외부 데이터소스(키움·KIS·DART·FRED) 인벤토리 | `backend/` 아래 작업 시 자동 |
> | `frontend/CLAUDE.md` | Vite·색 토큰·nav 단일소스·차트 규약 | `frontend/` 아래 작업 시 자동 |
> | `.claude/skills/live-uat-probes/` | 라이브 UAT 프로브 결함 9클래스·판정축·신뢰성 20규칙 | 프로브 저작·시각 검증 시 **호출** |
> | `.claude/skills/subagent-orchestration/` | 병렬 서브에이전트 오염 6방향 사례사 | 워크플로우 설계 시 **호출** |
>
> 코드 리뷰·구현 태도(가정 명시·최소 구현·수술적 변경·검증 가능한 완료기준)는 **조직 정책으로 매 세션 주입되므로**
> 여기 중복해 싣지 않는다.

## Commands

```bash
# Start both servers (Windows)
start.bat

# Start both servers (macOS/Linux)
./start.sh

# Backend only (from project root)
cd backend && python -m uvicorn main:app --reload --port 8000

# Frontend only
cd frontend && npm run dev

# Backend tests
cd backend && .venv/bin/python -m pytest
```

## Architecture

**Backend** — Python/FastAPI (port 8000) · **Frontend** — React 19 + Vite (port 5173), plain CSS (no TailwindCSS)

디렉터리·모듈 목록은 `ls`로 확인할 것. 코드만 봐서는 **틀리게 읽기 쉬운 것**만 남긴다:

- `backend/scheduler/`는 APScheduler 설정 **패키지**다(services 아님, 루트 레벨 — `__init__.py`(잡 배선·`_JOB_FUNCS`)·`jobs.py`·`schedule.py`·`_state.py`). 단일 `scheduler.py`가 **아니다**(task#120 정정).
- `backend/data/`는 **정적 참조 데이터만**(sp500_tickers.json·kospi_tickers.json). 런타임 데이터는 전부 Docker PostgreSQL.
- 캘린더 라이브 캐시는 PostgreSQL `calendar_cache` 테이블 — **레거시 파일 캐시는 task#167에서 제거**됐다.
- `backend/snapshots/`(gitignored)·`backend/reports/`(legacy, read-only)는 옛 스냅샷 JSON 폴백 경로다.
- 프론트 화면은 **허브 2종**으로 묶여 있다: Research(홈 `/` — 리포트·랭킹·다이제스트·캘린더, 리포트 탭이 보유/관심 종목 관리를 흡수), MarketHub(=Market, 시장지표·수급지표 2탭). 섹터·매크로 탭은 Portfolio 분석탭으로 통합됐다.

## Deployment

- **인프라**: Mac 로컬 Docker 4-컨테이너 (Render/Vercel/Supabase 제거)
- **nginx**: HTTP(80) 서빙, /api/* → backend:8000 프록시
- **프론트 서빙**: nginx가 `./frontend/dist`를 `:ro` 볼륨마운트로 직접 서빙. 로컬 `cd frontend && npm run build`가 **즉시 라이브**(서빙 번들 해시=로컬 빌드 해시로 검증 가능). `git push`는 폴러 재배포/영속용이며, **백엔드 변경은 폴러 재배포 후에야 라이브** 반영된다(프론트만 먼저 빌드하면 백엔드 의존 기능이 미동작). ⚠️ **단 그 비대칭은 *로컬 빌드를 했을 때*의 이야기다 — `deploy.sh:25`가 `(cd frontend && npm install --silent && npm run build --silent)`를 돌리므로 푸시 하나로 프론트도 자동 라이브가 된다(task#290 실측: 커밋 push → `gh run list` 배포 success 20초 → `frontend/dist/*` mtime이 그 시각 → 라이브 번들 == 로컬 `dist`).** 이 사실이 이 절에 빠져 있어서 **task#290 계획이 S6를 「commit+push → `npm run build` → 프로브」로 짜고 "빌드 전에 프로브를 돌려 라이브 red-first를 확보할 수 있다"를 적었는데, 별도 빌드는 불필요했고 라이브 red-first는 push-first 순서에서 원리적으로 불가능했다**(푸시 ~20초 뒤면 새 번들이 서빙돼 옛 번들을 잴 창이 없다). 실천 2가지: ⓐ **라이브 red-first가 필요하면 푸시 *전에* 돌릴 것** ⓑ 배포가 작업트리의 `frontend/dist`를 갱신하므로 **라이브를 재는 에이전트가 도는 동안 푸시도 배포성 행위**다(빌드만이 아니다 — 아래 ⑥ 4번째 관측의 자기적용판). **⚠️ 단 ⓐ는 충분조건이 아니다 — 푸시 *전에* 돌려도 red가 원리적으로 안 나오는 경우가 있다(task#318 실측)**: task#290이 「푸시 *후*엔 옛 번들을 잴 창이 없다」였다면 이건 「푸시 *전*에도 red가 없다」는 **다른 메커니즘**이다. 조건은 **프로브의 기대값이 그 변경으로 신설되는 백엔드 필드에서 파생될 때**다 — 푸시 전에는 프론트만 옛 번들인 게 아니라 **백엔드도 옛 코드**라 그 필드가 없고, 그러면 기대값이 0으로 degenerate해 **관측 0과 일치하며 통과한다**(task#318: 신규 9축이 전부 「표본 0 — 미검증」으로 PASS). 계획서는 「옛 번들이라 원리적으로 red여야 한다」고 적었지만 그 추론은 **백엔드가 이미 새 코드일 때만** 성립하고, 그 창(백엔드 새 + 프론트 옛)은 `deploy.sh`가 프론트를 먼저 라이브로 만들기 때문에 **이 저장소에서 자연 발생하지 않는다**(비대칭의 방향이 반대다). 실천: 프론트·백엔드를 함께 건드리는 변경의 라이브 판별력은 red-first에 기대지 말고 **배포 후 fault injection**으로 얻을 것(대상 셀렉터·기대값을 의도적으로 깨뜨려 축이 실제로 FAIL하는지 확인 → 원복). task#318이 그렇게 해서 **축 하나가 표본 0에서 공허하게 통과하던 것**을 찾았다(`.claude/skills/live-uat-probes` ⓨ).
- **backend**: FastAPI 컨테이너 (port 8000)
- **postgres**: PostgreSQL 16 컨테이너, pgdata 볼륨
- **certbot**: HTTPS 인증서 자동 갱신 컨테이너 (certbot/certbot) — docker compose 4번째 컨테이너
- **Cloudflare Tunnel**: portfolion.taebro.com → localhost:80 (cloudflared는 compose 컨테이너가 아니라 launchd로 실행)
- **launchd 자동실행**: cloudflared + docker compose
- **환경변수**: `backend/.env.docker` (DATABASE_URL, JWT_SECRET, SESSION_SECRET, OAuth, FRED_API_KEY, KOFIA_API_KEY) · `backend/.env` (로컬 venv·pytest용 DATABASE_URL) · `.env` (루트 — **`POSTGRES_PASSWORD`**, docker-compose 보간 전용). ⚠️ **DB 비밀번호를 바꾸면 이 3개를 함께 갱신해야 한다**(`scripts/rotate-postgres-password.sh`). `POSTGRES_PASSWORD`는 `backend/.env.docker`가 아니라 **루트 `.env`**에 있고, task#334에서 폴백을 제거해 **미설정이면 `docker compose`가 실패**한다(전엔 tracked 폴백값으로 조용히 떴고 그 값이 공개 저장소에 커밋돼 실운영 크리덴셜이었다 — B21).
- **배포**: `git push origin main` 시 자동 배포됨(GitHub Actions 러너 + 폴러 폴백). `docker compose build` / `docker compose up` 같은 *ad-hoc* 재빌드는 하지 말 것.
- **배포 메커니즘 주의 (검증된 사실, task#104·106)**: 배포 경로는 **GitHub Actions 러너(주) + 폴러(폴백)**다. 폴러(`scripts/auto-deploy-poll.sh`)는 **`LOCAL != origin/main`이면(앞서든 뒤처지든 양방향) 무조건 `git reset --hard origin/main` 후 `deploy.sh`**를 돌린다(스크립트 직접 확인, task#106 — task#104가 '미확정'으로 남긴 reset 조건 규명). 따라서 **commit→push로 `LOCAL==origin/main`이 되면 폴러는 skip(exit 0)**하고 배포는 *러너*가 처리하지만, **commit만 하고 push를 안 하면 로컬이 앞서(`!=`) 다음 폴(≤2분)에 reset돼 그 커밋이 통째 소실**된다 — `commit`만으론 불충분, **코드/문서 변경은 push 필수**(fg-map 지도 커밋이 이렇게 소실→cherry-pick+push 복구, task#106 reflog 확인). **백엔드가 옛 코드/이상 동작으로 보이면**: ① 도커 재기동 churn(컨테이너 uptime 제각각·certbot 등 일부 미기동)을 **먼저** 의심하고(`docker ps`로 uptime 확인 — backend는 `docker run`이라 `docker compose ps`엔 안 잡힘), ② 필요 시 정식 스크립트 **`bash deploy.sh` 1회**로 백엔드 컨테이너 재생성(working tree rebuild+stop/rm/run — 금지된 ad-hoc `docker compose`가 아님). 프론트는 nginx가 `frontend/dist` 직접 서빙이라 `npm run build`로 즉시 라이브(배포 무관). ⚠️ **그 비대칭이 만드는 창을 잊지 말 것 — 프론트는 즉시 라이브인데 백엔드는 러너/폴러 재배포 후라, 그 사이 "새 프론트 ↔ 옛 백엔드"가 실제로 돌아간다(task#267)**: 백엔드가 *새로 추가한* 응답 필드를 프론트가 읽으면 그 창에서 `undefined`로 렌더된다(구루 크롤 완료 문구가 `완료: undefined명`). **여기서 옛 필드로 폴백하면 고치려던 오값이 그대로 되살아난다** — `data.fresh ?? data.done`은 `done`이 시도 총계(=바로 그 버그)라 최악의 폴백이다. 정답은 **숫자를 빼는 것**(`fresh != null ? '완료: N명 갱신됨' : '완료: 갱신됨'`) — 틀린 값보다 없는 값이 낫다(`wrong < missing`). 관리자 수동 조작이라 창은 수 분이지만, 신규 백엔드 필드를 프론트가 소비하는 변경은 **그 창에서 무엇이 렌더되는지 한 줄 확인**할 것. ※ task#104에서 "in-checkout push는 절대 배포 안 됨"으로 단정했으나 그건 폴러 로그만 보고 러너를 놓친 과잉결론. **task#105에서 "미확정"이 규명됨 — 러너는 *dead*였다**(아래 러너 격리 항목): in-checkout 푸시는 러너에만 의존하는데 PortfoliOn 러너가 lab-taebro로 재등록돼 사라져 06-22~06-27 무음 미배포. **백엔드가 옛 코드면 폴러 footgun 단정 전에 러너부터 확인**: `gh run list`(잡이 `queued`/`cancelled(24h)`면 러너 부재)·`gh api repos/calmonion7/PortfoliOn/actions/runners --jq '.runners[]|{name,status}'`(online 러너 존재?).
- **⚠️ 배포 직후 백엔드가 `Up`이고 로그도 활발한데 API가 수 분간 무응답일 수 있다 — 기동 배치가 끝나야 포트가 서비스된다(실측, task#250)**: 위 "배포 메커니즘 주의"가 *배포가 안 된* 경우라면 이건 *배포는 됐는데 아직 못 받는* 경우다. 실측 타임라인 — 컨테이너 시작 `22:00:24` → lifespan 완료 `22:00:25`(**0.6초**; `main.py:241` lifespan = `_migrate()` + `sched.start()` + 데몬 스레드) → 그런데 포트 8000은 계속 `Connection refused`였고 기동 배치의 **마지막 로그 `22:05:41` 직후**에야 열렸다(분당 로그 49·19·22·20·20 → **4** → 2, 첫 요청 처리 `22:06:06`). 즉 **총 ~5분 15초**. 그동안 `docker ps`는 `Up`, `docker logs`는 활발해서 **완전히 건강해 보인다** — 여기서 "배포 깨졌다"로 오진해 `deploy.sh` 재실행 같은 불필요한 조치를 하기 쉽다. **배포 후 라이브 스모크는 포트 바인딩을 폴링한 뒤 실행할 것**: `docker exec <c> python -c "import socket;print(socket.socket().connect_ex(('127.0.0.1',8000)))"` 가 `0`이 될 때까지 대기(111=refused). 증폭 조건: **배포가 배치 예정 시각과 겹치면** 배치가 기동 즉시 뜬다(`daily_report_us` 07:00 KST · `daily_report_kr` 20:30 KST — task#250 배포가 정확히 07:00 KST에 떨어져 관측됐다) → 그 시간대 배포는 라이브 API가 5분+ 다운된다고 예상할 것(현재는 기록만, 조치 없음). ⚠️ **주의 — 이 항목은 원인을 두 번 고쳐 썼다**: 처음엔 "uvicorn이 lifespan을 끝낸 뒤에야 바인딩하므로 기동 배치가 그 시간을 먹는다"로 적었는데 **틀렸다**(lifespan 0.6초, 배치는 스케줄러 스레드라 비블로킹). 정확한 메커니즘(GIL/이벤트 루프 기아)은 **미확정**이며 그렇게 남긴다 — task#246의 "원인을 추정으로 귀속하지 말고 계측으로 확정하라"가 회고 기술 자체에도 적용된 사례다.
- **⚠️ 러너 (task#105)**: 자동배포 주 경로 = self-hosted GH Actions 러너(`deploy.yml`, `runs-on: self-hosted`). **PortfoliOn 전용 러너 = `~/actions-runner-portfolion`**(launchd `actions.runner.calmonion7-PortfoliOn.macbook-portfolion`). 06-22~06-27 무음 미배포는 이 러너가 lab-taebro 세팅 때 재등록돼 사라진 탓. **러너 격리 일반 규칙(새 프로젝트가 기존 러너 재사용 금지·타 프로젝트 자원 읽기전용)은 전역 `~/.claude/CLAUDE.md`의 "멀티 프로젝트 인프라 격리" 참조.**
- **⚠️ 자동 배포 폴러 (작업 시 주의)**: launchd `com.portfolion.auto-deploy-poll`이 2분마다 `scripts/auto-deploy-poll.sh`를 돌려 **`LOCAL != origin/main`이면(양방향) `git reset --hard origin/main` 후 배포**한다. → 메인 체크아웃에서 **커밋 안 한 tracked 편집뿐 아니라 push 안 한 로컬 커밋(로컬이 origin보다 앞선 경우)도 다음 폴(≤2분)에 reset으로 소실**된다(task#106 실사례). 코드/문서 변경은 **commit과 `git push origin main`을 묶어 즉시** 반영할 것 — commit만으론 안전하지 않다. ⚠️ **`.forge/`는 「untracked라 안전」이 아니다 — 혼합이다(task#296 실측 정정).** `git ls-files .forge/`가 **320파일**을 반환하며 **`.forge/CONTEXT.md`와 `.forge/adr/*.md`는 tracked**다(`loop.md`·`backlog/`·`done/`·`quick/`·`config.json`만 untracked라 안전). 실제로 ADR 보정 절과 CONTEXT 구성 서사를 편집하고 **커밋하지 않은 채 다른 파일만 커밋·푸시했더니 그 편집이 폴 reset에 소실돼 재적용해야 했다.** 게다가 **`reset --hard`는 해시가 같아도 작업트리를 되돌린다** — reflog가 내 커밋마다 `reset: moving to origin/main`(같은 해시)을 남겼고, "`LOCAL == origin/main`이니 폴러가 skip한다"는 추론이 그래서 틀렸다. **forge 스킬들은 `.forge/*`가 gitignored라고 전제하지만 이 저장소에선 거짓이다** → 코드와 **함께** `.forge/CONTEXT.md`·`.forge/adr/`**·`.forge/retro/`**도 같은 커밋에 담을 것. ⚠️ **`retro/`가 이 목록에서 빠져 있어 실제로 한 번 놓쳤다(task#322 회고)** — 실측 tracked 비율이 `retro/` **303/304** vs `done/` **0/313**이라, 회고는 커밋 대상이고 done 아카이브는 아니다. fg-learn 스킬은 「retro/ADR/CONTEXT가 gitignore 화이트리스트로 tracked」라고 전제하는데 그 전제는 `retro/`·`adr/`·`CONTEXT.md`에는 맞고 `done/`에는 틀리다. 새 회고를 쓰면 `git add`를 잊지 말 것(안 하면 폴러에 소실되지는 않지만 — untracked는 `reset --hard`에 안전하다 — 형제 303건과 달리 git에 남지 않아 다음 세션의 fg-ask 그릴링이 그 회고를 읽지 못한다). **⚠️ 단, 커밋 소실을 `git log -1`로 판정하지 말 것 — 2연속 오판했다(task#238 D6·#239 D9)**: `commit && push`를 한 셸 체인으로 묶어도 그 사이 2분 폴이 끼면 폴러가 **자기가 앞서 fetch해 둔 낡은 `origin/main`으로 reset**해 로컬 커밋·작업트리가 잠깐 되돌아간다. 그 순간 `git log -1`을 찍으면 **이전 커밋이 나와 커밋 소실로 보이지만**, `push` 자체는 이미 성공해 있고 다음 폴이 `origin/main`을 재fetch해 그리로 reset하므로 **`HEAD == origin/main == 내 커밋`으로 자기복구**된다(실제 손실 0, 두 사례 모두 reflog에 `commit → reset → reset`이 찍혀 있었다). 위 폴러 경고가 강해서 정황을 과대해석하기 쉬우니, 판정은 반드시 **`git rev-parse HEAD` vs `origin/main` + `gh run list`(배포 성공 여부)** 로 할 것.

## Key Files

- `API_SPEC.md` — full REST API reference (source of truth for endpoints)
- `CLAUDE_COWORK_API.md` — external API for Claude AI to read/write stock analysis
- `backend/auth_schema.sql` — Docker PostgreSQL 인증 스키마 (users, refresh_tokens); 반드시 app_schema.sql보다 먼저 실행
- `backend/app_schema.sql` — Docker PostgreSQL 앱 스키마 (tickers, user_stocks, snapshots, schedules, guru_managers, guru_schedules, digests, consensus_history, calendar_cache, market_cache, user_menu_permissions, user_events, market_leverage_indicators, market_lending_balance)
- `backend/.venv/` — Python virtual environment (macOS: `backend/.venv/bin/python`, Windows: `backend/.venv/Scripts/python`)

## Data Model

Docker PostgreSQL이 기본 저장소. 로컬 JSON 파일은 런타임 캐시 용도.
**테이블·컬럼의 정본은 `backend/app_schema.sql`(앱)·`backend/auth_schema.sql`(인증)이다** — 목록을 여기 복제하지 않는다.

- **스키마 실행 순서: `auth_schema.sql` → `app_schema.sql`** (역순이면 FK가 깨진다).
- 로컬 파일 캐시(gitignored)는 `backend/data/consensus/` per-ticker 컨센서스 하나뿐이다. **캘린더는 로컬 파일 캐시가 없다** — 라이브는 `calendar_cache` DB 테이블(task#167에서 파일 캐시 제거).

## Gotchas

- **라이브 UAT 프로브의 결함 클래스·판정축·신뢰성 규율은 `.claude/skills/live-uat-probes` 스킬로 분리됐다 (task#225~304 누적)** — 프로브(`scripts/uat*.mjs`)를 새로 짜거나 고칠 때, **시각·레이아웃을 바꾸는 변경을 검증할 때**, 프로브가 ALL PASS인데 화면이 깨져 보일 때는 **먼저 그 스킬을 호출할 것**. 담고 있는 것: 시각 결함 9클래스(넘침·잘림 ellipsis·flex 접힘·요소 간 간격·색 미적용·SVG 좌표계·AT 프루닝·min-content·이웃 열로의 비용 이전), 프로브 신뢰성 20규칙(ⓐ~ⓣ — 커버리지 카운터·정의역 sentinel·대조군·identity·성분 분해), 하니스 제약(SW가 `/api/*`를 가로채니 `serviceWorkers:'block'` 필수 / bfcache·탭 전환은 자동화로 관측 불가 → 핀 단언으로 박제), admin 표면 UAT 4대안, jsdom·recharts 한계. **핵심 2줄만 여기 남긴다:** ⓐ 시각을 바꾸는 변경은 프로브 PASS 후에도 **육안 스크린샷 1장**을 완료기준에 넣을 것(그것이 유일한 포착 수단이었던 사례 6회). ⓑ 완료기준은 대리지표가 아니라 **목표 자체**로 쓰고, "이 단언이 통과하면서도 깨질 수 있는 방식"을 계획 시점에 한 줄 적을 것.

- **⭐ 계획서·회고에 적힌 *수치와 상태 서술*은 착수 시점에 이미 틀렸을 수 있다 — 그것을 재는 슬라이스를 **첫 자리**에 두면 드러난다(task#313~316 드라이브에서 **5/5 반박**)**: 위 ⓑ가 「완료기준을 목표 자체로 쓰라」면 이건 그 **입력**의 이야기다. 연속 5개 태스크에서 계획·회고의 서술이 **다섯 번 모두** 실측에 반박됐고, 반박한 것은 전부 **첫 슬라이스의 baseline 측정**이었다 — ⓐ task#311의 「이 태스크에서 「해부 미작성」 분기가 라이브에서 **처음 활성화**된다」는 루틴이 `composition`과 *함께* 발행해 성립하지 않았다(그 분기는 아직 dormant다) ⓑ task#314가 인용한 직전 회고의 「`producers[]` 6종 전부 비어 있음」은 **틀렸다**(실제 9개·ticker 8개였고 진짜 공백은 `share_pct`가 30항목 전부 0인 것) ⓒ task#315의 「요약 블록 2줄 이내」는 278px 실측 **3줄**이고, 줄이려면 「대조할 수 없는 업체 N곳」을 빼야 해서 task#307 규율과 정면 충돌한다 ⓓ task#316의 「목차 칩이 `~30px`」는 실측 **27px**이었고 더 나쁜 것은 **높이 모델 자체가 틀렸다**는 점이다(content 높이의 출처가 폰트 메트릭이 아니라 상속 `line-height` — 그래서 옛 산술 `14+7·2+1·2=30`이 성립하지 않았다) ⓔ 같은 태스크에서 *방금 쓴* 주석의 「8~11칩은 278px에서 3줄」이 그 baseline 역산으로 **5줄**임이 드러났다(`nav 168 = 27.25×5 + 8×4`). **실천 3가지**: ⓐ 수치·상태를 담은 계획은 그것을 재는 **baseline 슬라이스를 S0/S1로** 두고 그 완료기준에 「현재 값이 계획이 적은 값과 같은지」를 명시할 것(5건 중 4건이 그렇게 짜여 있었고 그래서 잡혔다) ⓑ **회고를 인용할 때 그 수치가 *실측*이었는지 *인상*이었는지 구별**할 것 — task#314가 반박한 문장은 실측이 아니었다 ⓒ **계획이 「이 태스크에서 처음 관측된다」고 예고한 것은 관측되지 않을 수 있다** — 그 예고는 데이터가 협조해야 성립하고, 협조하지 않았다는 사실 자체가 산출이다(「미검증」으로 기록하고 「통과」로 위장하지 말 것). **⭐ 네 번째 표면이 가장 위험하다 — 낡은 서술이 *산문*이 아니라 `if` 조건의 **근거**로 쓰이고 있으면, 그 낡음은 문서 오류가 아니라 **버그**다(task#317)**: `scripts/uat282-tech-structure.mjs`의 헤더 주석이 「라이브 두 판 모두 `related` 4키가 빈 배열」을 근거로 관계도 축 **9개**를 `if (R.mode === 'inject')` 안에 가두고 있었다. 그 서술은 작성 시점엔 참이었지만 그 뒤 루틴이 `related`를 채워(실측 7 slug 전부 8~20개) **거짓이 됐고**, 그래서 그 9축은 **픽스처만 재고 라이브 렌더를 전혀 재지 않는 상태**로 조용히 전환돼 있었다 — 프로브는 계속 초록이었고 총계도 줄지 않았으므로 **어떤 자동 신호도 이것을 알리지 않는다**. 하마터면 「라이브 리플로우」를 목표로 하면서 라이브를 안 재는 게이트에 신규 축을 넣을 자리였다. **실천: 새 축을 넣기 전에 그 축이 들어갈 블록의 *진입 조건*과 그 조건의 *근거 주석*을 함께 읽고, 근거가 데이터 상태를 주장하면 그 상태를 1콜로 재라.** 일반화하면 — **주석이 코드 경로를 가두고 있으면 그 주석은 코드다**(테스트가 없는 코드다). `if (mode === …)`·`if (feature)`·조건부 스킵의 근거 주석이 전부 이 부류다. **⭐⭐ 그리고 이 항목이 5/5에서 7/7로 갱신됐다 — task#326~332 드라이브에서 **7파트 전부** 반박됐고, 그중 셋은 위 네 표면이 *안 덮는* 방향이다(task#332 회고).** **ⓐ 반대 방향 — 계획이 「이것은 결함이다」라고 경고한 것이 *이미 고쳐져 있었다*(task#329)**: 계획 S1이 「`commodities.get_treasury()`는 백필 → 판정 순서라 판정이 영원히 발동하지 않는다. 그대로 베끼면 완료기준이 원리적으로 달성 불가」라고 경고했으나 실측은 **판정이 이미 백필 앞에 있고 판정 대상도 raw fetch 결과**였다(`e88e9c2`, task#269가 고쳤고 이유가 코드 주석에 박혀 있다). 위 네 표면은 전부 「계획이 *참이라 한 것*이 거짓」인데 이건 「계획이 *결함이라 한 것*이 이미 해소」다 — **경고를 액면가로 믿고 없는 결함을 고치려 들면 멀쩡한 순서를 뒤집는다.** ⚠️ **이 CLAUDE.md의 「④ 「동형 이식」은 순서까지…」 항목이 바로 그 stale한 경고이므로 인용 전에 코드를 볼 것.** **ⓑ 부분적으로 맞음 — 옳은 결함을 *틀린 줄*에서 찾았다(task#330)**: 계획의 「컨센서스 `report_date`가 UTC 변환으로 하루 밀림」에서 그 경로는 **이미 `today_kst()`로 해소**돼 있었고, 진짜 결함은 같은 파일의 **`idx.tz_convert(None)`**이었다(미 동부 저녁 타임스탬프가 다음 달력일로 밀린다 — 실측 `2026-08-21 20:00 ET` → `date(2026,8,22)`, 그 값이 `_MART_SQL` 90일 창과 `DISTINCT ON … report_date DESC`에 쓰인다). **「지목한 줄이 이미 옳다」를 「결함이 없다」로 읽으면 진짜 버그를 놓친다** — 계획이 *증상*은 맞게 봤고 *위치*만 틀린 경우가 있다. 실천: 계획이 지목한 줄이 이미 옳으면 **그 증상이 성립하는 다른 경로를 같은 파일에서 찾을 것**(기각하기 전에). **ⓒ 수치·상태가 아니라 *순서*가 틀린 계획(task#332)**: S3(`STRUCTURE.md`에 셀프체크 규칙 남기기) → S4(`fg-map` 재생성) 순서였는데 **재생성이 그 파일을 덮어쓴다** — 계획의 어떤 *주장*도 틀리지 않았고 **의존 순서만** 자기파괴적이었다. 실천: 슬라이스 의존 그래프를 볼 때 「A가 만든 산출을 B가 덮어쓰지 않는가」를 물을 것(특히 **재생성·재작성·전량 교체**를 하는 슬라이스가 있으면 그것을 *앞*에 두고 세부 보정을 뒤에 둔다).

- **⭐ 감사·검증 패턴은 *양방향*으로 틀린다 — 좁으면 위반을 놓치고 느슨하면 **없는 회귀를 만든다**(task#326~332 드라이브에서 자기 게이트가 3회 오도됨)**: 이 문서의 여러 항목이 「감사 패턴을 좁히면 그 감사는 통과해도 무의미하다」를 반복하는데, **반대 방향은 기록돼 있지 않았다.** 한 드라이브에서 *정지조건 검사 자신이* 세 번 틀렸다 — ⓐ **너무 좁아서**: §0 열린 행을 볼드 표기만 잡는 `^\|.*\*\*B[0-9]+\*\*`로 세어 **41행을 24행으로**, 이월 6건을 2건으로 오측정했다(§0 행은 볼드와 평문 `| B9 |`이 혼재한다) ⓑ **또 너무 좁아서**: bare 포인터 감사가 **범위형 `:261-262`**를 못 봐 2건을 1건으로 셌다 ⓒ **너무 느슨해서**: `[^0-9]:[0-9]+`가 **`totalCount:0`**(JSON 필드 참조)에 걸려 진짜 포인터 3건을 **9건으로** 세고 **없는 「+2 회귀」를 만들었다**. **느슨한 오탐이 더 위험한 이유** — 좁은 누락은 「통과했지만 무의미」인데, 느슨한 오탐은 **멀쩡한 상태를 회귀로 판정해** 없는 문제를 쫓게 하거나(비용) 진짜 회귀를 그 노이즈에 묻는다. 실천 3가지: ⓐ 감사 정규식을 쓰면 **그 히트를 한 번 눈으로 열거**할 것(3건이면 3건을 다 본다 — 「몇 건」만 보면 실체를 모른다) ⓑ 판정은 **「0이면 통과」가 아니라 「HEAD와 같으면 통과」**로(내가 무엇을 늘렸는지가 판정 대상이다) ⓒ **오탐 후보를 배제 목록에 명시**할 것(포인터 감사라면 포트 번호·필드 참조·파이썬 슬라이스 `[:20]`·시각 `06:10`).

- **⭐ 감사·게이트의 *baseline과 판정 형태* 자체가 틀린다 — 한 드라이브에서 내 정지조건이 3번 틀렸다(task#335~337)**: 위 항목이 「감사 *패턴*이 좁거나 느슨하다」였다면 이건 **그 감사가 무엇과 비교하고 무엇을 세는가**의 이야기다. 세 형태 전부 「목표는 달성됐는데 축이 FAIL」이거나 「축이 거짓 통과」였고, 매번 **느슨화가 아니라 등가·강화로** 다시 썼다. ⓐ **불충실한 프록시** — 「`logDiag('doc')` payload에 `location.search` 0건」으로 썼는데 **옳은 구현이 쿼리 *키*를 뽑기 위해 그것을 정당하게 읽는다**(`URLSearchParams(...).keys()`) → 축의 전제(「보이면 값이 기록된다」)가 거짓이고 도달 불가였다. 「검색 문자열을 *값으로* 싣지 않는다」로 등가화하고 **옛 코드로 이빨을 확인**했다(현재 0 / 수정 전 1). ⓑ **세션 상수 baseline** — 「`dist/index.html` mtime이 `1787387896` 불변」으로 썼는데 **내 푸시의 정당한 배포**(`deploy.sh:25`의 `npm run build`)가 그 상수를 무효화했다. 감사 대상이 「특정 값 유지」가 아니라 **「내 행위가 그것을 건드리지 않음」**이면 판정은 **직전·직후 비교**여야 한다(남의 정당한 이벤트는 그 축의 대상이 아니다). ⓒ **개수 판정은 양방향으로 틀린다** — 「`CONCERNS §0`의 B행 수 == 6」은 ⓐ 드라이브가 **새 결함을 정직하게 기록하면**(`B82`) 7이 되어 영원히 FAIL하고 ⓑ 반대로 **형제를 하나 지우고 새 행을 하나 더해도 6이라 통과한다.** 원장·목록 감사는 **개수가 아니라 행의 정체성**으로 쓸 것(「선재 형제 N행이 *전부 존재*하고, 추가분은 이 작업의 신규 발견분이다」) — 정체성은 정직한 추가를 허용하면서 은밀한 교체를 잡으므로 **강화**다. **부수 2가지**: ⓘ **볼드 마크업이 감사 정규식을 깨뜨린다** — `^\| *(\*\*)?B[0-9]+ *\|`가 `| **B49** |`의 **뒤쪽 `**`** 때문에 매치하지 않아 **B49가 사라진 것처럼 보였다**(거짓 경보). `\*{0,2}B[0-9]+\*{0,2}`로 넓히고 **행을 눈으로 열거**해야 확정된다. 같은 라운드에서 `ls .forge/retro | grep -c resilience`도 무관한 옛 회고 2건을 잡았다. ⓙ **적대 검토 프롬프트에 「기록된 트레이드오프를 결함으로 재발견하지 말 것」을 넣을 때는 「단 그 *전제*가 거짓이 된 경우는 결함이다」를 반드시 쌍으로 넣을 것** — task#337에서 그 한 줄이 **최대 발견**을 만들었다(ADR이 「공개 경로는 Cloudflare 전용」을 근거로 쓰는데 `nginx.conf`가 `CF-Connecting-IP`를 지우지 않고 포트가 `0.0.0.0`에 게시돼 그 전제가 거짓 → `B82`). 쌍으로 넣지 않으면 검토가 **전제 오류까지 「기록된 트레이드오프」로 보아 침묵**한다.

- **⭐ 죽은 코드·해소 판정은 **비대칭**이다 — 열린 결함을 놓치면 버그가 남지만, **산 코드를 죽었다고 적으면 기능이 삭제된다**(task#332 fg-map 재생성)**: 맵 증분 갱신이 「사실과 *반대*를 말하던 서술」 4건을 잡았다 — §3.3 「`set_status`를 쓰는 곳은 구루 크롤 2경로뿐」(실측 `as run` **25곳**/호출 41) · §6.6 「런타임 캐싱 3종에 `/api/*` NetworkFirst 포함」(ADR-0036이 **제거**했다) · §7.8 「용어집 importer 0건·도달 불가」(실측 **24파일** — 확장자 명시 import를 좁은 패턴이 못 봤다) · §7.10 `role="img"` 사이트(파일이 **삭제**돼 있었다). 부수로 워커 상한을 정당화하는 **주석 3곳이 `maxconn=10`**을 근거로 드는데 `db.py` 실제는 **20**이다(「주석이 상한을 가두면 그 주석은 코드다」의 또 한 사례). **그래서 「이건 죽었다/해소됐다」는 판정에는 「이건 열려 있다」보다 *높은* 증거 기준을 적용할 것** — `TechLevelBand.css`는 `PlayerTable.jsx`가 여전히 import하므로 **고아가 아니다**(지우면 스타일이 깨진다). 부수 2가지: ⓐ **`last_mapped_commit`은 신선도의 *하한*이지 정확한 표식이 아니다** — in-place 편집이 스탬프를 올리지 않으므로, 스탬프만 믿고 「전부 stale」로 가정하면 **이미 옳은 서술을 중복 수정**한다(실측: 「반영 대상」 6항목 중 3항목이 이미 baseline에 있었다) ⓑ **맵을 재생성할 때 `Refresh`(처음부터)를 고르면 `CONCERNS §0` 원장이 옛 상태로 돌아가 거짓이 된다** — 반드시 `Update`(증분, in-place)를 쓰고, concerns 문서에는 **전체 재작성 탈출구를 금지**하고 보존해야 할 행을 항목별로 열거해 넘길 것(`git diff` hunk가 §0 범위에 0개인지로 검증한다).

- **⭐ 병렬 wave에서 파일이 안 겹쳐도 **계약이 겹치면** 오염된다 — 그리고 적대 검토의 LOW를 자동 수복에서 빼는 것은 비용 절감이지 무해가 아니다(task#326·#329)**: `.claude/skills/subagent-orchestration`이 기록한 6방향(git 되돌리기·부모의 배포·in-file 주입 충돌·전체 스위트 공유·문서의 미완 박제·리뷰가 바꾼 계약) 위에 **일곱 번째**가 있다 — **공유 함수의 반환 계약**. task#329에서 B40이 `market_indicators/cache.py::_mc_load`의 반환 계약을 바꿀 수 있었는데, 그 함수는 **이 wave가 소유하지 않은 모듈 다수**(commodities·exports·indices·econ·sentiment)와 **동시 편집 중인 형제**(`earnings.py`)가 호출한다. 처방: 그 그룹에 **「변경은 additive여야 한다 — 비-additive가 불가피하면 *하지 말고* 보고하라」**를 하드 제약으로 걸고(엄격 로더를 *추가*하고 관용 로더를 보존했다), 검토 렌즈 하나를 그 결합에 겨눴다. **실천: 병렬 그룹을 나눌 때 파일 소유권만 보지 말고 「이 그룹이 바꿀 *공유 심볼의 계약*을 누가 호출하는가」를 grep으로 셀 것.** 부수 — **LOW 필터의 대가**: task#326에서 `severity !== 'LOW'`로 수복 대상을 좁혔더니 **제외된 LOW 5건 중 3건이 실재**했고 그중 하나는 `main.py`의 bare `os.environ["SESSION_SECRET"]`이 **빈 문자열을 통과**시켜 「기동은 되는데 신규·재로그인만 전면 불가」한 무음 고장 경로였다(`docker ps`·헬스체크·기존 세션 전부 정상으로 보인다). 이후 파트는 LOW도 수복 단계에 넘겼고 실재율이 계속 높았다.

- **⭐ 항상 FAIL하는 축은 게이트가 아니다 — 선재 결함을 프로브에 수입하면 그 프로브의 exit 코드가 영구히 죽는다(task#316)**: 프로브에 새 축을 넣었더니 `over <= 0`이 m278에서 **원리적으로 FAIL**했다 — 대상 slug의 `players` 표가 task#311이 이미 baseline으로 박제한 **선재 넘침 3/7종** 중 하나였기 때문이다. 더 나쁜 것은 프로브 주석이 *"선재 FAIL 2건 — 이 태스크와 무관하다"*를 **적어 두고 축은 실패로 남긴** 점이다: 그러면 「ALL PASS」가 원리적으로 불가해지고 이후 누구도 그 프로브의 exit 코드를 신호로 쓸 수 없다(`uat280`이 정확히 그 상태로 선재 19건을 안고 있다). **처방은 축을 「이 변경이 깨뜨릴 수 있는 것」으로 좁히는 것**이다 — ⓐ 넘침의 **범인 집합에 내가 만진 요소가 없다** ⓑ 넘침량이 **baseline 이하**. 그러면 내가 깨뜨리면 ⓐ가, 선재가 악화되면 ⓑ가 잡고, 선재 자체는 FAIL을 만들지 않는다. 같은 라운드의 두 번째 사례는 **기준 상자를 틀리게 잡은 축**이다(`overlay-covers-card` — 절대배치 `inset:0`은 padding box 기준인데 카드 `getBoundingClientRect`는 border box라 테두리 폭만큼 어긋난다, `frontend/CLAUDE.md` 참조). 축을 정확화하되 **느슨해진 대가를 되받는 이빨 축**(안 덮인 띠가 *정확히* 테두리 폭이어야 한다)을 쌍으로 둘 것. **부수 — 계측 실패를 판정 실패로 읽지 말 것**: 정지조건 체크가 **전체 실행에서만** `fetch failed`로 FAIL하고 단독 실행은 3/3 PASS였다(앞선 축이 프로브 5종을 뷰포트마다 로그인시킨 직후라 새 로그인이 거부됐다). 「대상에 닿지도 못한 것」을 FAIL로 읽으면 없는 버그를 쫓는다 — 라이브 fetch엔 **유계 재시도**를 두고 재시도가 다 실패할 때만 FAIL로 볼 것(task#232의 「0을 성공으로 읽는 게이트」·task#300의 「0바이트 로그가 죽음과 정상 진행을 동시에 뜻한다」와 같은 가족의 **세 번째** 사례). **⭐ 그리고 이 교훈에는 *반대편 절반*이 있다 — 위가 「축을 *넣을 때*」라면, 게이트를 **정의할 때**도 같은 함정이 있고 그쪽이 더 은밀하다(task#317)**: fg-loop 정지조건을 「`node scripts/uat282-…mjs` **exit 0**」으로 못박았는데 그 프로브는 착수 시점에 이미 **선재 FAIL 16건**을 안고 있었고 그중 4건(`overflow-leaf`·`caption-lines` — 업체 표 셀과 시장 캡션)은 그 태스크와 **무관**했다. 즉 **대상을 완벽히 고쳐도 도달 불가한 값**을 완료 조건으로 삼은 것이다. 가장 아픈 부분은 **그 교훈을 알고 있었고 같은 사이클의 다른 판단에서 이 항목을 인용까지 했다는 점**이다 — 「알고 있음」은 재발을 막지 못했다. 더 나쁜 것은 **계획서의 원래 완료기준이 더 정확했는데**(「기존 축 PASS 수가 배포 전 baseline **이상**」) 내가 그것을 「exit 0」이라는 *더 단순하지만 더 나쁜 프록시*로 바꿨다는 것이다. **처방은 하나뿐이고 30초짜리다 — 게이트를 쓰는 그 자리에서 대상을 1회 돌려 현재 FAIL 수를 세라.** 그러면 정지조건이 「exit 0」이 아니라 「이 변경이 책임지는 축 FAIL 0 **+** 선재 FAIL baseline 이하 **+** PASS 수 baseline 이상」 세 항으로 자연히 갈라진다(느슨해진 것이 아니다 — 내가 깨뜨리면 1항, 선재 악화는 2항, 커버리지 붕괴는 3항이 잡는다). **같은 절차가 계획서의 완료기준을 정지조건으로 옮길 때도 필요하다**: 계획이 「baseline 이상」처럼 *상대적*으로 쓴 것을 *절대적*으로(「0건」·「exit 0」) 단순화하지 말 것 — 그 단순화가 곧 이 결함이다. **⭐ 그 30초 실행은 *두 방향*을 본다 — 「선재 FAIL 수」만 세면 절반만 막는다(task#318)**: task#317이 「도달 불가한 값」을 게이트로 삼는 결함이었다면, 그 **거울상은 「이미 도달한 값」**이다. task#318 초기 문답에서 계획서 DoD의 `node scripts/uat311-…mjs exit 0`을 정지조건 후보로 올렸는데, 같은 자리에서 1회 돌려 보니 **착수 시점에 이미 40 PASS · 0 FAIL · exit 0**이었다 — 그대로 넣었으면 **첫 체크 실행에서 「전부 통과 → 목표 달성 → `loop.md` 삭제」가 되어 구현 0줄로 루프가 닫혔다.** 그래서 그 축을 `exit 0` **AND 단언 총계 > 40**(baseline)으로 강화했고, 실제로 총계 40 → 49로 늘어 신규 축이 들어갔음을 그 조건이 증언했다. **즉 착수 시 1회 실행에서 물어야 할 것은 두 개다 — ⓐ 지금 FAIL이 몇 건인가(도달 불가 방지) ⓑ 지금 이미 통과하고 있는가(구동력 0 방지).** ⓑ가 참이면 그 게이트는 정지조건이 아니라 **회귀 가드**이므로, 목표를 구동할 축을 따로 세워야 한다(여기선 「대기 칩이 실측 표본으로 렌더된다」가 그 축이었고, 그것만이 배포 전 FAIL이었다). 같은 이유로 회귀 가드는 `exit 0`이 아니라 **「FAIL 0 유지 **AND** PASS 수 baseline 이상」**으로 쓸 것 — 전자만 보면 축이 조용히 사라져도(커버리지 붕괴) 통과한다. **⭐⭐ 그리고 「계측 실패를 판정 실패로」의 *역방향*이 더 위험하다 — 계측 실패를 판정 **성공**으로 읽는 것(task#318)**: 위 부수 3사례(#316 로그인 거부 · #232 총계 0 · #300 0바이트 로그)는 전부 「닿지도 못한 것을 FAIL로 읽는」 방향이었다. 반대 방향의 실측 — fault injection 사본을 `$CLAUDE_JOB_DIR/tmp`(프로젝트 **밖**)에 두고 돌렸더니 node가 `playwright`를 해석하지 못해 `ERR_MODULE_NOT_FOUND`로 즉사했고, `| grep -E '✗|단언 총계'`가 **빈 출력**을 냈다. 그 빈 출력은 「FAIL 0」과 **글자 하나 다르지 않게** 보였고, 파이프 때문에 `exit=0`까지 찍혔다(파이프라인의 exit는 마지막 명령의 것이다). 전체 출력을 확인하지 않았다면 「주입했는데도 통과 → 축에 이빨이 없다」는 **정반대 결론**을 냈을 자리다. 실천 3가지: ⓐ 프로브 사본·주입본은 **`node_modules` 해석이 되는 위치**(여기선 `scripts/`)에 두고 끝나면 지울 것 ⓑ **「FAIL 0」을 신뢰하기 전에 단언 총계가 기대치인지** 볼 것(총계가 0이거나 없으면 그것은 통과가 아니라 미실행이다) ⓒ 판정을 grep 결과에 걸 때 `| grep`은 **exit 코드를 삼킨다** — 원본 명령의 exit를 따로 받아 둘 것(`out=$(cmd); ec=$?`).

- **⭐⭐ 그 가족의 세 번째 짝 — 축이 FAIL할 때 *축을 의심하기 전에 등가로 좁혀 보라*. 좁힌 축이 여전히 FAIL하면 그건 축의 결함이 아니라 **구현의 결함**이다(task#319~321 fg-loop 드라이브, 실측 2사례)**: 앞의 둘이 게이트를 *정의할 때*(#317 도달 불가한 값 · #318 이미 도달한 값)의 이야기라면, 이건 게이트가 **실제로 FAIL했을 때 그것을 어떻게 읽는가**의 이야기다. 그리고 오독의 방향이 정해져 있다 — FAIL을 보면 「내 축이 너무 빡빡한가」로 먼저 기울고, 그렇게 느슨하게 고치면 **축이 방금 잡아낸 진짜 결함을 스스로 지운다.** ⓐ **실측 1(도달 불가형)**: 루프 프로브의 링크 칩 축이 「`related` 항목이 있으면 링크가 있어야 한다」였는데 발행 7종 중 **4종은 발행물 이름 일치가 원리적으로 0**이라 영원히 FAIL했다. 여기서 「축을 빼자」가 아니라 **「발행물 일치 수 == 링크 수」 등가**로 좁혔더니 도달 가능해지면서 *동시에* 이빨이 늘었다(과잉 링크도 누락 링크도 잡는다). ⓑ **실측 2(거짓 전제형, 이쪽이 값지다)**: 「스크롤 0에서 플로팅 바가 없다」가 모바일 전 조합에서 FAIL했다. 그 축은 「정적 목차가 첫 화면 안에 있다」를 전제하는데 실측은 **m390 vh 664 · 목차 bottom 768**로 그 전제가 거짓이었다(그 사실은 `TechReport.jsx` 주석에 「변경 *전부터* 그렇고 요구사항이 아니다」로 **이미 적혀 있었다**). 축을 느슨하게 고쳤다면 그대로 끝났을 텐데, **「바 존재 == 목차가 화면 밖」 등가**로 좁히니 **여전히 FAIL**했고 그 FAIL 메시지가 원인을 실어 왔다(`목차 가시=true(bottom=768/vh=664)`) — 모바일에서 **목차와 바가 함께 떠 두 항해 UI가 겹치는** 실제 UX 결함이었다. **느슨한 축은 자기가 놓친 것을 알려주지 않는다.** 실천: FAIL을 보면 ① 그 축이 주장하는 전제를 한 줄로 적고 ② 그 전제를 1콜로 재고 ③ 전제가 거짓이면 축을 **버리거나 완화하지 말고 등가로 다시 쓴다**(느슨화는 판별력을 줄이지만 등가화는 늘린다). ④ 그러고도 FAIL이면 그때가 구현을 볼 시점이다. 부수 — **이 정정은 사람에게 올렸기 때문에 일어났다**: `loop.md`가 「정지조건 프로브는 드라이브 중 수정하지 않는다 — 계약이 틀렸으면 fork 벽」을 못박아 둬서 두 번 다 승인을 거쳤고, 그 규칙이 없었으면 조용히 완화하고 지나갔을 것이다(자기가 짠 축으로 자기를 판정하는 것을 막는 장치는 *문서에 적힌 금지*였지 의지가 아니었다).

- **⭐ 병렬 서브에이전트에게 `git stash`·`checkout`·`restore`·`reset`과 프론트 빌드(`npm run build`·`vite build`)를 금지할 것 — 작업트리 전역 변형은 형제의 *측정 대상*을 바꾸고, 그 오염은 "재현 불가 flake"로 오귀속된다.** 프론트 빌드는 nginx가 `frontend/dist`를 직접 서빙하므로 이름이 "문법 확인"이어도 **배포 행위**다. **그리고 오케스트레이터 자신도 같은 규칙의 적용 대상이다** — 라이브를 재는 에이전트가 도는 동안 메인 세션도 배포성 행위(빌드·`deploy.sh`·컨테이너 재기동)를 하지 말 것. 나머지 4대 원칙: ① 파일 내 fault-injection은 **파일당 주입자가 1명일 때만** 안전하다 ② 병렬 wave의 완료기준은 **자기 소유 파일의 표적 테스트**로 좁힐 것(전체 스위트는 파일이 안 겹쳐도 형제의 미완 상태를 본다) ③ **문서·프로브 갱신 슬라이스는 코드 wave와 적대 검토가 끝난 뒤 직렬 단계**로 둘 것(형제의 미완 상태가 박제되고, 리뷰가 계약을 바꾸면 프로브가 통째로 스테일해진다) ④ 프롬프트에 코드 예시를 인용할 땐 `${`를 `\${`로 이스케이프할 것(평가되면 thunk가 죽고 **`agents_error: 0`으로 조용히** 보고된다 — 완료 시 `<failures>`와 결과 키의 null을 반드시 확인). **6가지 오염 방향의 실측 사례사와 처방 근거는 `.claude/skills/subagent-orchestration` 스킬에 있다.**

- **백엔드 모듈·외부 데이터소스 인벤토리는 `backend/CLAUDE.md`로 분리됐다** (키움·KIS·DART·FRED·KOFIA·관세청·yfinance 연동, `market_indicators/` 패키지, 캐시 6종, 라우터·서비스 지도 — 21항목). `backend/` 아래 파일을 만질 때 자동 로드된다. **외부 소스의 파싱 규약이 필요하면 그 파일을 먼저 읽을 것** — 응답 봉투·필드 스케일·필수 파라미터 함정이 거기 있다.

- **프론트 전용 규약은 `frontend/CLAUDE.md`로 분리됐다** (Vite 8 = rolldown 청크 분할, KR 색 관례 토큰(가격 up/down ↔ 의미 success/danger 교차 사용 금지), `navSections.js` nav 단일 소스, market 차트 dual-axis·`krFmt` 단위, Vite proxy — 5항목). `frontend/` 아래 파일을 만질 때 자동 로드된다.

- **로깅 방출 규약 (버그를 드러내기 위한 로그 패턴, task#162/#163)**: 백엔드 진단/경고/에러는 **모듈 `logger`로 통일**하고 `print`는 신규 금지(앱 코드 `print(`=0, `tests/test_no_print.py`가 단언). 루트 로거는 `main.py:_configure_logging()`이 기동 시 1회 배선(`basicConfig(level=INFO)` + urllib3/yfinance/apscheduler/asyncio는 WARNING 억제 + uvicorn `propagate=False`로 중복 emit 방지) — **config 없으면 lastResort=WARNING+라 `logger.info`가 docker logs에 안 뜬다**. 레벨: warning=graceful 담화, error=예상치 못함·데이터손실(아껴), info=배치/라이프사이클. 포맷 `logger.x(f"[Component] <무엇> (<ids>): {e}")` — `[Component]`는 PascalCase·개념당 1스펠링(formatter 프리픽스 없어 메시지 내 마커가 유일 grep 앵커). **프론트(`console.*`)도 §4에서 다룸**(task#185): `console.warn`=graceful/`console.error`=예상외, 마커는 소스 모듈/훅명 실명(`[usePortfolioData]` 등, 백엔드 개념명과 다름), 자동 가드 없음(lint 미연결). 전체 규약: `.forge/codebase/CONVENTIONS.md` §4.

- **신규 DB 컬럼은 `app_schema.sql`만으론 배포에 반영 안 됨 — `main.py _migrate`에 `ADD COLUMN IF NOT EXISTS`를 쌍으로 추가 필수(DoD)**: 스키마 파일은 신규 설치용이고 라이브 DB는 기동 idempotent 마이그레이션(ADR-0006)만 탄다. 한쪽만 고치면 배포 직후 그 컬럼을 쓰는 INSERT/SELECT가 컬럼 부재로 깨진다(`stock_recommendations.name`이 app_schema.sql에만 추가돼 배치 INSERT 파손 직전 — 배포 전 포착, task#130). 컬럼 추가 슬라이스의 완료기준에 두 파일 쌍을 명시하고, 리뷰도 변경 파일 밖 배선 계층(main._migrate·include_router·batch_registry)까지 봐야 한다.

- **API 변경 시 명세서 2개 모두 갱신**: 엔드포인트 추가/삭제·요청/응답 스키마·인증 게이팅을 바꾸면 `API_SPEC.md`(전체 REST 레퍼런스)와 `CLAUDE_COWORK_API.md`(외부 Cowork API)를 **항상 함께** 업데이트(DoD에 포함). 한쪽만 고치면 다른 쪽이 stale돼 Cowork/소비자가 잘못된 명세로 호출한다. **엔드포인트 *존재* drift(method+path 추가/삭제/개명)는 `backend/tests/test_api_doc_sync.py`가 자동검출**(라이브 `app.routes` ↔ 두 문서 `### \`METHOD /path\`` 헤더 대조, task#99) — 새 엔드포인트를 `API_SPEC.md`에 안 적으면 테스트 실패. 미문서화 기존 23개는 `KNOWN_UNDOCUMENTED` exact-match 베이스라인으로 동결(문서화하면 거기서 빼야 통과). 단 **요청/응답 스키마·인증 게이팅 동기는 여전히 수동 DoD**(테스트는 존재만 검증, prose 파싱 안 함). **단, "2문서 모두"는 Cowork 관련 엔드포인트에 한한다** — `CLAUDE_COWORK_API.md`는 외부 Cowork의 enrich/backlog 워크플로우 전용 스코프라, 사용자 대면 read 엔드포인트(`/api/portfolio/*`·admin 배치 refresh 등)는 `API_SPEC.md`에만 넣는다(형제격 `/api/portfolio/rebalance`도 Cowork 문서에 없음). 신규 엔드포인트가 Cowork 소비 대상인지 먼저 판별해 DoD를 좁힐 것(기계적 "둘 다"는 과함 — task#149·#150·#151에서 노출·베타·F&G 모두 API_SPEC만). **인증 게이팅을 바꾸는 슬라이스는 착수 시 `grep -n '불필요' API_SPEC.md`(⚠️ **콜론 무관 — 패턴을 좁히지 말 것, 아래 재발 참조**)를 먼저 돌려 "곧 틀릴 표기"를 세고, 문서 갱신을 *슬라이스로 명시*할 것(task#230·231·232)**: 3부작에서 `**Auth:** 불필요` **8곳**(1/3:3 · 2/3:4 · 3/3:1)이 오표기로 남아 게이팅 직후 곧바로 틀린 문서가 됐는데, doc-sync 테스트는 엔드포인트 *존재*만 보므로 이 auth 산문 drift를 잡지 못한다. 더 나쁜 건 **세 계획 모두 문서 슬라이스가 아예 없었다**는 점(fg-ask 그릴링이 이 DoD를 계획에 옮기지 못했다) — 그래서 계획을 믿지 말고 착수 시 직접 grep해야 한다. 남아도 되는 `불필요`는 `GET /api/auth/oauth/token` 등 `auth.py` 공개 엔드포인트뿐이다(ADR-0029). **⚠️ 감사 패턴을 좁히면 그 감사는 통과해도 무의미하다 — 실측 재발 1건(task#263 → 7차 버그헌트 L3)**: 3부작이 도구로 못박은 `grep -n '\*\*Auth:\*\* 불필요'`(콜론 독립 줄)는 **문장 중간형 `**Auth 불필요.**`(콜론 없음)를 원리적으로 볼 수 없다.** 실제로 그 형태 3곳(`/backlog`·`/disclosures`·`/insider-trades`)이 같은 섹션 안에서 `**Auth:** Bearer token 필요`와 **문자 그대로 모순**인 채 생존했고(task#263이 삭제), `git show e148592~1:API_SPEC.md`에 좁은 패턴은 **1건**·넓은 패턴은 **4건**이 걸린다. 이건 "프로브 판정축이 부족하면 ALL PASS가 무의미"(아래 ⑧ⓐ·④)의 **문서 감사판**이고, `test_api_doc_sync.py`는 엔드포인트 *존재*만 대조하므로 스위트 초록 상태로 무기한 생존한다. **더 중요한 메타 교훈 — 승급하지 않은 교훈은 지시 문서로 번진다**: task#263 run.md가 "패턴을 넓힐 것"을 승급 후보로 *적어만 두고* 승급하지 않은 사이, 같은 날 생성된 `.claude/agents/doc-sync.md:35`가 **좁은 패턴을 에이전트 지시로 박제**했다(카드 `4e204a3` 00:13 < 발견 `e148592` 10:45). 같은 사이클에서 실증된 교훈은 회고를 기다리지 말고 **가토·카드 같은 *지시 문서*에 즉시 전파**할 것. **⚠️ 구체 사실 — `CLAUDE_COWORK_API.md`는 한 엔드포인트를 *두 곳*에 적는다(task#318)**: `GET /api/tech-reports`가 **① 워크플로우 0단계**(「(조건 확인) GET /api/tech-reports → …」, 파일 앞부분의 발행 절차 블록)와 **② 엔드포인트 절**(`### \`GET /api/tech-reports\``, 예시 응답 포함) 양쪽에 서술돼 있다. task#318 계획서는 ①만 지목했고 **적대적 리뷰가 ②를 찾았다** — 계획대로 했으면 엔드포인트 절의 예시 응답이 `{"reports": [...]}`만 보이는 채로 stale하게 남았을 자리다. `test_api_doc_sync.py`는 엔드포인트 *존재*만 대조하므로 이 응답 스키마 drift는 자동 게이트가 원리적으로 못 잡는다. 실천: Cowork 문서를 고칠 땐 그 method+path를 **파일 전체에서 grep해 히트 수를 세고** 전부 갱신할 것(절 제목만 찾으면 워크플로우 서술을 놓치고, 워크플로우만 찾으면 예시 응답을 놓친다). **⭐ 그리고 그 판정을 「무엇이 바뀌었나」로 하면 문서를 *열지도 않는다* — 계약이 전부 무변경이어도 산문은 stale해진다(task#324)**: 위 항목들이 「문서의 *어디*를 볼지」였다면 이건 **「문서를 볼지 말지」**의 오판이다. 화면 개명(enrich 탭 「심층분석」 → 「사업분석」)에서 엔드포인트·요청/응답 스키마·인증이 **정말로 전부 무변경**이었고, 그래서 계획서가 「`API_SPEC.md`·`CLAUDE_COWORK_API.md`는 갱신 대상이 아님을 **확인**한다」를 완료기준으로 적었다. 그런데 두 문서는 enrich 필드 3종(`key_resource`·`competitor_edge`·`market_outlook`)의 **화면 위치를 서술한다** — 「저장 후 리포트 **심층분석 탭** … 섹션에 표시」(API_SPEC 3건) · 「리포트 "…" 섹션(**심층분석 탭**)에 표시」(COWORK 3건). 개명 순간 6건이 통째로 거짓이 됐고, `test_api_doc_sync.py`는 엔드포인트 *존재*만 대조하므로 **스위트 초록 상태로 무기한 생존**한다. **더 넓게 새어나간 곳이 있다** — 개명 대상을 「`frontend/src` 8곳」으로 셌는데 실제는 **20곳**이었다: frontend 8 · API_SPEC 3 · COWORK 3 · `.forge/CONTEXT.md` 2 · **레거시 캡처·프로브 스크립트 4종 9건**(`capture-report-detail.js`·`capture-ux.js`·`uat-79.js`·`uat79-reports.js`가 `'심층분석'` 라벨을 **클릭**한다 → 개명 후 조용히 못 찾는다, task#317의 「문자열이 코드 경로를 가둔다」 계열). 실천 2가지: ⓐ **사용자 대면 이름·화면 위치를 바꾸는 변경의 감사 단위는 「계약」이 아니라 「그 문자열」이다** — `grep -rn '<옛 이름>' .` 를 **프로젝트 전역**으로 한 번 돌려 히트를 전부 분류할 것(코드·테스트·문서·스크립트·`.forge`). ⓑ **「갱신 대상이 아님을 확인한다」류 완료기준은 그 확인을 *어떻게* 하는지까지 적어야 한다** — 「계약이 안 바뀌었으니 무관」은 확인이 아니라 추론이고, 이번엔 그 추론이 틀렸다. 남겨도 되는 히트는 「그 개명 자체를 서술하는 문장」과 `.forge/done/` 아카이브(봉인된 과거 기록)뿐이다.

- **기능 표면을 바꾸면 `README.md` 해당 절도 같은 PR에서 갱신(DoD)**: README가 문서화하는 표면 — ① 화면 구성(nav 탭/화면 기능) ② 환경변수(env/데이터 소스 키) ③ 기술 스택(라이브러리/외부 연동) ④ 아키텍처(router/service/table) ⑤ 배치 — 중 하나라도 추가·삭제·개명되어 기존 절이 stale해지는 변경은 README의 그 절을 함께 손본다. README는 **overview 레벨**이라 엔드포인트/요청·응답 스키마 세부는 여기 중복하지 말고 `API_SPEC.md`/`CLAUDE_COWORK_API.md`에만 둘 것(위 doc-sync 규칙과 역할 분담). 안 지키면 README가 드리프트해 신규 합류자·외부 소비자가 옛 화면구성/키/스택으로 오인한다(task#47 README 전수 재조정이 누적 드리프트를 한 번에 청소한 사례).

- **엔드포인트 응답을 비-additive로(배열→객체 등) 바꾸면 그 엔드포인트를 fetch하는 *모든* 프론트 소비처를 전수 grep**: `grep -rn '<엔드포인트 경로>' frontend/src/`로 독립 fetcher까지 찾아 전부 갱신할 것. 한 소비처만 고치면 다른 곳(예: `Analytics.jsx`가 훅과 별개로 `/api/stocks/dashboard`를 직접 fetch)이 `res.data`를 옛 형태(배열)로 취급해 조용히 깨진다(task52: 대시보드 배열→`{holdings,totals}` 변경 시 상관관계 탭이 항상 "보유종목 없음" — 워크플로우·적대적 리뷰 둘 다 놓치고 메인 세션 grep이 포착). 가능하면 **additive(필드 추가)**를 선호하고, reshape가 불가피하면 소비처 전수 감사를 DoD에 포함. (배치 id 4표면 grep의 프론트-계약판.)

- **엔드포인트에 read/외부호출을 additive로 *추가*하면 `mock.call_args`(마지막 호출)를 단언하는 기존 테스트가 조용히 오염된다** — additive는 응답 *shape*뿐 아니라 *호출 시퀀스*도 늘린다. 기존 테스트가 단일 호출을 전제로 마지막 호출 인자(`exclude_tickers`/`limit` 등)를 `mock.call_args`로 단언하면, 두 번째·세 번째 호출이 끼는 순간 마지막 호출이 신규 호출로 바뀌어 거짓통과/오류가 난다. 대응: ① 기존 단언은 **호출별 `call_args_list[i].kwargs`**로 마이그레이션(인덱스로 해당 호출 명시), ② 신규 호출은 **`if <조건>:`로 입력 비면 생략**(예: 빈 watchlist/holdings면 추가 read 안 함)해 기존 테스트를 보존, ③ 신규 테스트가 **`call_count`로 시퀀스를 못박음**. (위 비-additive reshape 가토의 *테스트판 사촌* — 깨지는 건 응답이 아니라 모킹된 호출 시퀀스. `GET /api/recommendations`에 watchlist→holdings read를 순차 additive 추가하며 두 번 재현·검증, task#66·67.)

- **엔드포인트에 auth `Depends`(get_current_user/require_admin/require_admin_or_api_key)를 추가하면 그 엔드포인트를 호출하는 *자체-app 테스트*가 401/403로 깨진다** — 다수 테스트가 `conftest`의 `client`가 아니라 모듈 상단에서 `FastAPI()`를 직접 만들어 `app.dependency_overrides[...]`로 auth를 우회한다(예: `test_stocks_router.py`·`test_consensus_router.py`). conftest는 `main.app`의 `get_current_user`만 override하므로 자체-app 테스트엔 안 걸린다. 대응: 엔드포인트에 auth 의존성을 추가/변경하면 **그 경로를 호출하는 자체-app 테스트를 전수 grep해 새 의존성의 override를 추가**(`app.dependency_overrides[require_admin_or_api_key] = lambda: "test-user-id"` 등). 무인증 거부(401/403)는 **override 없는 fresh app**으로 별도 검증(`tests/test_security_auth_gaps.py` 패턴). 위 additive-mock 가토의 *dependency-override 사촌* (auth 추가가 응답이 아니라 의존성 그래프를 늘림, task#108). **단 "전수 grep해 override를 추가"를 *선제적으로* 하지 말 것 — 감사 대상 파일 수는 작업량이 아니다(task#230·231·232 3연속)**: ADR-0029 3부작에서 계획이 지목한 감사 대상은 4·5·14파일이었는데 실제 변경은 3·0·0파일이었다. 형제 read가 먼저 인증돼 있으면 그 테스트 앱이 **이미 override를 등록해 둔** 경우가 많고(3/3에선 계획이 경고한 "`_or_api_key`는 별개 키" 함정까지 선재 대응돼 있었다 — `test_report_router.py`가 4개 키를 모두 override), 선제 추가는 중복이 된다. **순서: 의존성을 붙인 뒤 전체 스위트를 먼저 돌리고 *실제로 깨지는 것만* 고친다.** grep은 "어디를 볼지"를 좁히는 용도이고 게이트는 스위트다. **단 그 규칙엔 보완항이 있다 — 스위트는 *안 깨지는* 오류를 못 잡는다(task#249)**: 판정축·계산식을 바꾸면 결과가 같아 **통과하는 테스트의 주석·docstring이 거짓이 된다.** 피어 멀티플 가드의 기준 표본을 leave-one-out에서 "peer 전체+자사"로 바꿨을 때, 계획이 그릴링에서 1건(`never_judges_self_row`의 "표본에서도 빠짐")을 지목했지만 실행 중 **2번째**(`excludes_none_from_target_and_sample`의 `median(10,11,12)=11 → 90.9×` → 실제로는 `median(10,11,12,1000)=11.5 → 87.0×`)가 나왔다 — 코드 변경 없이 초록으로 통과하므로 스위트에 원리적으로 안 걸리고, 그대로 두면 **반대 사실을 증언하는 테스트**가 남아 다음 사람이 옛 판정축을 사실로 읽는다. 실천: 전수 확인 대상은 "깨지는 테스트"가 아니라 **"그 축을 *서술하는* 테스트"** 이고, `git grep`으로 축의 **옛 표현**(여기선 `나머지 peer`·주석에 박힌 `median(` 산식)을 훑어야 잡힌다. 위 규칙과 방향이 반대이니 둘을 같이 돌릴 것 — 스위트는 *깨지는 것*, grep은 *안 깨지면서 거짓이 된 것*. **⭐ 세 번째 사각이 있다 — *그 축을 정확히 서술하면서도 새 분기를 안 타는* 테스트다: 이빨 단언은 판별력을 보장하지 분기 커버리지를 보장하지 않는다(task#301)**. 위 두 사각이 「깨지는 것」과 「거짓이 된 것」이었다면 이건 **「옳고 초록인데 새 코드 경로를 지나가지 않는 것」**이다. `TechReport.test.jsx`의 「표 행 순서 == 밴드 행 순서」는 그 계약을 정확히 단언하고 **픽스처의 판별력까지 이빨로 못박아** 뒀는데(`expect(tableNames).not.toEqual(rep.players.map(p => p.name))` — API 순서와 렌더 순서가 같은 픽스처였다면 정렬을 통째로 지워도 통과한다는 경고까지 주석에 있었다), 두 픽스처 **모두 `category` 필드가 없어** `groupByCategory`가 `[]`를 반환 → **그룹핑 분기를 아예 타지 않았다.** 그래서 그룹핑이 표·밴드 순서를 갈라놓는 회귀를 **이빨 단언을 갖춘 테스트가 초록으로 통과**시켰고, 라이브 프로브만이 잡았다(fixture-pass-live-fail의 새 형태 — 기존 사례들은 *값*이 라이브와 달랐지만 이건 *분기 진입 자체*가 없었다). **실천**: 새 분기를 만들면 「기존 테스트의 픽스처가 이 분기를 *타는가*」를 **별도로** 확인할 것 — 확인은 싸다. 그 분기의 **게이트 함수를 픽스처에 직접 적용**해 보면 된다(`expect(groupByCategory(FIXTURE)).toEqual([])`가 곧 「이 픽스처는 그 경로를 안 탄다」의 증명이며, 그 한 줄이 없어서 놓쳤다). 이빨(`not.toEqual`)과 커버리지(`분기 진입`)는 **다른 축**이고, 전자를 갖췄다는 사실이 후자의 알리바이가 되지 않는다.

- CORS origins: `localhost:3000`, `localhost:5173`, `FRONTEND_URL` env var (`backend/main.py`). 배포 시 `FRONTEND_URL`을 `.env.docker`에 설정.

- `start.bat` runs both servers in hidden PowerShell windows; use `stop.bat` to kill them.

- 백엔드 리포트 생성(`report_generator`)은 시장 데이터 스냅샷만 만든다 — **백엔드에 LLM/Anthropic 호출 없음**(`requirements.txt`에 anthropic 없음). AI 분석 텍스트는 외부 Cowork 클라이언트가 enrich API(`CLAUDE_COWORK_API.md`)로 작성한다. `ANTHROPIC_API_KEY`는 `.env.docker`에 남아있으나 현재 백엔드에서 미사용.

- **캘린더 KR 실적발표일(`earnings`)은 yfinance `.KS`/`.KQ`가 *유일한* forward 소스다(task#121)** — `t.calendar["Earnings Date"]`(`{code}.KS`(KOSPI)/`.KQ`(KOSDAQ)). **Naver는 forward 실적일 미제공**(`m.stock.naver.com/api/stock/{code}/integration`의 `irScheduleInfo`=null, `/finance`·`/consensus` 404), **DART도 forward 스케줄 없음**(공정공시 013 무데이터; `잠정실적`은 *과거* 발표일이라 캘린더 가치 marginal). **`yf.Ticker`엔 반드시 `_yf_sym(ticker, market, exchange)`로 접미사를 붙일 것 — raw ticker면 KR 0건**(캘린더 `_collect_earnings`가 raw ticker로 호출해 KR 실적이 통째 비던 잠재버그였음). `_yf_sym`은 US=bare ticker(무영향)·KR=`{ticker}.{exchange||KS}`. KOSDAQ 커버리지는 patchy(yfinance가 일부 `.KQ`에 404) — best-effort, 빈 결과 graceful. "stale"(다음 분기 미공시 시 과거 날짜만)은 월범위 필터가 자연 처리.

- **대시보드 빌드는 "holdings=N → 항상 N카드" 불변식 — 500-to-empty 금지(task#102)**: `GET /api/stocks/dashboard`의 `_build_all`(`routers/stocks.py`)은 `get_quotes_batch`(try/except→{}) + 카드당 `_safe`(throw→`_minimal_card`)로 감싸 **일괄시세·per-card enrichment(snapshot/consensus/배당/수급/내부자) 실패에도 전체 500을 안 낸다**. 콜드 첫 호출에 **10-워커 ThreadPool×카드당 다중 DB read가 풀 경합(PoolError, CONCERNS §4)/throw → 500 → 프론트 `usePortfolioData.fetchDashboard` catch가 silent로 삼킴 → 빈 그리드**였고, 헤더(`/api/portfolio`=단일 쿼리·ThreadPool 없음)는 N 정상이라 "헤더 N·그리드 빈"이 됐다(첫 로딩만, 재네비=풀 warm면 정상). 프론트도 방어: `Portfolio.jsx` `DashboardGrid`는 `stocks>0`이면 빈상태 대신 Skeleton(헤더↔그리드 모순 제거), self-heal은 one-shot이 아니라 **bounded 재시도(최대 3)** — 첫 콜드 실패에 한 방 헛쓰고 재마운트 전까지 stuck하던 회귀 차단. **헤더 N인데 그리드 빈** 증상을 보면 dashboard 빌드 throw(풀 경합 등)와 **프론트 silent catch**를 의심할 것. (풀 sizing 자체 튜닝은 별도 후속 — 이 가드는 증상 차단.) **단, 실제 근본은 per-card throw가 아니라 `_portfolio_totals` NaN→직렬화 500이었다(task#104)**: `_usdkrw_rate()`가 저장 FX의 비유한값(nan)을 그대로 반환→`if fx is None` 가드를 통과(NaN≠None)→US 카드 totals=NaN→starlette `allow_nan=False` 직렬화 500. cold·warm 무관 결정적 500(per-card 가드 *위* 단계라 task#102가 못 막음). 수정: ① `_usdkrw_rate`에 `math.isfinite` 가드(비유한→None, US totals graceful 제외) ② `_build_all` 반환을 `services.utils.sanitize`로 감싸 NaN/inf→None(출처 불문 직렬화 500 안전망, CONCERNS §3). 외부시세서 흘러든 NaN은 응답 dict 어디든 들어갈 수 있으니 **시세/합산을 응답에 싣는 엔드포인트는 sanitize 또는 소스 isfinite 가드 필수**. **세 번째 minimal-card 트리거 — 배당 `float/Decimal` TypeError(commit d666cdd2)**: `_build_card`의 `yield_on_cost = annual_div / avg_cost`·`expected_income = annual_div * qty`에서 `avg_cost`/`qty`는 DB NUMERIC→**Decimal**, `annual_div`(stock_dividends)는 **float**이라 `float/Decimal` 연산이 `TypeError`→배당 있는 *모든* 보유종목 카드가 throw→`_minimal_card` 폴백→RSI·컨센서스·매물대·배당이 통째 blank(스냅샷·시세는 정상). task#102(풀 경합·cold-only)·#104(NaN 직렬화 500)와 달리 **결정적이고 500도 안 나서(graceful 폴백) 더 은밀** — 에러 토스트 없이 enrichment만 조용히 사라진다. **헤더/시세는 정상인데 enrichment(RSI·컨센서스·매물대·배당)만 일괄 blank면 `docker logs portfolion-backend-1 | grep '최소카드 폴백'`로 per-card 예외부터 확인**(minimal-card 가드가 근본원인을 마스킹하므로 로그가 유일한 단서). 수정: 배당 계산 양변 `float()` 정규화. 회귀 테스트는 **Decimal** avg_cost로(기존 fixture가 float만 써 못 잡던 fixture-pass-live-fail — DB는 NUMERIC→Decimal). DB NUMERIC 컬럼(avg_cost·quantity 등)을 float·외부값과 산술하는 경로는 어디든 동일 위험.

- **`enriched_at`(AI 분석 존재) 정본은 `tickers` 테이블 컬럼 — 스냅샷 `data` JSON엔 없다**: 신규 판정/표시 필드를 붙일 땐 정본 저장 위치를 *가정하지 말고* 그 필드를 이미 읽는 기존 소비처(`report.py:447` 등)를 grep으로 확정하고, 테스트가 실구조(테이블·컬럼)를 단언할 것. 스냅샷 JSON에서 읽도록 가정하면 fixture는 green인데 라이브는 항상 False가 된다(추천 watchlist enriched가 정확히 이렇게 죽어 task#132로 재수정 — 종목명 dual-source 가토와 같은 '이중 저장소 혼동' 가족, fixture-pass-live-fail의 내부 저장판).

- **종목명은 dual-source** — `tickers.name`(공유 마스터, 종목관리 목록이 live로 읽음) vs `snapshots.data.name`(리포트 생성 시 박제, 리서치 목록·상세가 읽음). 이름 변경 시 **둘 다** 갱신해야 목록↔상세가 일치한다(`storage.refresh_snapshot_names` 단건 / `reconcile_snapshot_names` 전체 동기화). DB만 바꾸면 리포트 목록 캐시(`cache.get_list`)·스냅샷 LRU 탓에 화면 미반영 → `cache.invalidate(ticker)`+`invalidate_list()` 필수(storage→cache는 함수 내 지연 import로 순환참조 회피). 추가/관심/승격 시 실명은 `market.resolve_name`이 quote(KR 키움 stk_nm/Naver·US yfinance shortName)에서 채운다(이름칸 비거나 티커면). 백필: `POST /api/stocks/names/backfill`(admin) — 단, 시세 일시실패 시 그 종목을 **재시도 없이 조용히 스킵**(`updated:0`)하므로 결과 0이면 재실행할 것(task#77; 응답 `skipped` 목록으로 어느 종목이 빠졌는지 확인 가능 + 서버에 skip 진단 로그, task#88). **클로버 방지 가드**(task#77): `save_holdings`/`save_stocks`의 tickers UPSERT는 들어온 name이 NULL/빈값/티커와 같으면 기존 `tickers.name`을 보존(`name=CASE WHEN EXCLUDED.name IS NULL OR EXCLUDED.name = EXCLUDED.ticker THEN tickers.name ELSE EXCLUDED.name END`)하고, 일일 배치(`report_generator`)는 `resolve_name`으로 ticker형 이름이면 quote 실명을 박제해, "고쳐도 다음날 또 종목번호"로 되돌아오는 재발을 막는다. (task 41 retro)

- **admin `scope=all` 리포트 목록은 비소유 종목(`is_mine=false`)에도 `category`("holdings"/"watchlist")를 무조건 붙인다** (`report.py:_mk_entry`, 글로벌 포트폴리오 멤버십 기준) — 그래서 category로만 게이트된 관리/액션 버튼(수정·승격·삭제)이 **남의 종목(리서치 "그외" 탭)에도 노출**되는데, 그 핸들러는 호출자 본인 user_stocks만 검사하는 user-scoped 엔드포인트(`/api/watchlist|portfolio/{ticker}`)라 **404로 조용히 깨진다**(증상="관심 목록에 없다"). 대응: ① **액션 버튼 가시성은 category가 아니라 `is_mine`으로 게이트**(`is_mine===false`면 삭제만 — 수정/승격은 타인 데이터라 무의미), ② 관리자의 교차-사용자 동작은 `/api/admin/*` 전용(`require_admin`, ticker 단위 전 사용자 `user_stocks` 삭제, 스냅샷은 고아로 유지)으로 분리, ③ 액션 버튼 블록은 **단일 `StockActions` 컴포넌트**(`frontend/src/components/reports/StockActions.jsx`, `layout="card"|"list"`)로 통합됨(task#103) — `StockCard`(그리드)·`TickerListItem`(사이드바) 양쪽이 이걸 쓰므로 **액션버튼/게이트 변경은 거기 한 곳만**(과거엔 두 렌더러에 byte-identical 중복이라 "항상 둘 다 수정" 푸트건이었고, 그게 task#97 그외탭 삭제 404의 재발 토양이었다). (그외 탭 삭제 404 task#97 → 버튼 중복 제거 task#103)

- **응답에 NaN/inf 가능 float를 싣는 엔드포인트는 NaN/inf를 가드할 것** — starlette `JSONResponse`는 `allow_nan=False`라 응답 dict에 `NaN`/`inf`가 있으면 직렬화에서 **500**(`Out of range float values are not JSON compliant`)이 난다. 특히 위험한 건 폴백이 *다르게* 가린다는 점: PostgreSQL은 `json` 컬럼에 NaN을 거부(저장 실패)하지만 **파이썬 `json.dumps`는 기본 `allow_nan=True`라 파일 폴백은 통과**해, DB저장 실패→파일 성공→응답 직렬화 실패로 증상이 엇갈려 진단이 늦어진다. 외부 시세(yfinance `Close`가 NaN, FX/usdkrw가 NaN 등)에서 흘러든 NaN이 합산값(`total_value` 등)을 오염시키는 게 전형. 가드는 **소스에서**(예: `math.isfinite` 체크 후 "시세 없음" 처리) 하는 게 출력 일괄 sanitize보다 깨끗하다. (다이제스트 생성 500 사례 8cd70a42.)

- **매크로 신호(FRED 거시 시계열, 시장지표 탭, `macro.py`/`GET /api/market/macro-signals`) ≠ 매크로 상관(MacroTab/`analysis_service.py`, 보유 종목-매크로 자산 90일 수익률 상관, `GET /api/analysis/macro-correlation`)** — 둘 다 "매크로"지만 전자는 FRED 거시지표 자체의 절대 신호, 후자는 내 보유 종목과 TLT/UUP/USO/^VIX의 상관계수 행렬. 서로 다른 서비스·엔드포인트·화면이니 혼동 금지.

- **`get_or_refresh`는 "fetch 실패 시 직전 저장값 폴백"을 하지 않는다 — 취약·비공식 외부소스는 VIX식 수동 폴백을 쓸 것(task#151)**: `cache.get_or_refresh(key, fetch_fn, ttl)`는 "캐시/저장값이 있으면 fetch를 *스킵*"할 뿐, fetch가 실패(418·타임아웃·파싱)했을 때 직전 `_mc_load` 값을 반환하지는 않는다(실패를 전파). 그래서 CNN Fear&Greed(`sentiment.py`)처럼 언제든 막힐 수 있는 소스엔 `fx.py`의 VIX식 **수동 폴백**(`_get_cache→try fetch→성공 시 _mc_save+반환, 실패 시 _mc_load 직전값 반환, 없으면 None`)을 써야 빈 화면 대신 stale 값이 뜬다. FRED/yfinance처럼 안정적인 소스는 `get_or_refresh`로 충분. 요청경로 응답에 저장값의 `timestamp`를 실어 프론트가 stale을 인지하게 할 것. **⚠️ 그리고 `ttl` 인자는 *저장값에 안 걸린다*(task#234 ①)** — `_mc_load`가 행을 주면 **나이 불문 그대로 반환**하고 `ttl`은 **인메모리 캐시 수명만** 지배한다. 즉 한 번 `market_cache`에 저장되면 `force=True`가 올 때까지 **영구 서빙**이다(라이브에서 3일 지난 `m7_earnings`가 2ms에 반환돼 확인; `econ_indicators`·`kr_exports`가 오래된 채 서빙되는 것도 같은 이유). 이름과 인자가 "TTL이 지나면 갱신된다"는 오해를 부르므로 **"TTL 만료 → 요청 경로가 재조회한다"를 전제로 심각도·설계를 판단하지 말 것** — 실제 재조회자는 `force=True`를 주는 배치와 (테스트에서) `_mc_load`가 None을 반환하는 경로뿐이다. 15개 키 전체에 적용되는 구조적 사실.

- **중복 제거·단일 소스화는 "같아 보이는 필드"를 합치는 작업이다 — 합치기 전에 그 필드가 *겸하던 역할 수*를 세라(task#251)**: 위 nav 단일 소스화에서 `MobileNav`의 `key`는 **권한 필터**(`menuPermissions.includes(t.key)`) 겸 **이벤트명 소스**(`trackEvent('nav_' + key)`)로 두 역할을 겸직 중이었고, 그래서 '일정·인컴' 탭이 `nav_research`를 쏘고 있었다(섹션 key로는 `schedule`). 공유 모듈에서 파생할 때 자연스러워 보이는 `section.key`를 골랐다면 이벤트명이 `nav_schedule`로 바뀌어 백엔드 `events.py`의 **`VALID_EVENTS` 화이트리스트에서 조용히 탈락**했을 것이다(요청은 성공하고 이벤트만 사라진다). 실제 수정은 `section.perm`으로 파생해 5탭 전부 기존 이벤트명과 바이트 동일하게 유지했다. **vitest·빌드·라이브 프로브 어느 것도 이걸 안 잡는다** — 이벤트명을 단언하는 테스트가 없고, 화면은 정상으로 보인다. 실천: 필드를 합치는 리팩터에선 **각 필드의 소비처를 grep해 역할 수를 세고**(`key`가 필터·이벤트명·React key·라우팅 목적지 중 몇 개를 겸하는지), 파생 소스는 "이름이 비슷한 것"이 아니라 **소비처가 요구하는 값**으로 고를 것. 같은 가족의 다른 표면: `batch_registry`의 `source`(fetch 출처) vs `usage`(소비 UI) 혼동, `tickers.name` vs `snapshots.data.name` dual-source.

- **레이스 가드를 한 함수에 넣으면 *그 파일·그 훅의 형제 비동기 경로*를 전수 세라 — 같은 결함이 이웃에 그대로 남는다(task#273)**: 위 "세라" 가족(task#251 *역할* 수 · task#266 *소비처* 수 · task#170 같은 증상 재발 시 *writer* 수)의 새 표면으로, 이번에 세는 대상은 **형제 비동기 경로**다. B27(랭킹 마켓 토글 레이스 — 옛 응답이 새 market으로 렌더돼 원화가 `$`로 고착)을 닫는 슬라이스가 `Ranking.jsx:fetchPage`에 세대번호 가드를 넣었는데, **바로 그 슬라이스가 새로 소비하기 시작한 `useTrackedStocks`의 `reload()`에 같은 결함이 그대로 있었다** — 마운트 자동 reload(R0)와 `toggle` 후 reload(R1)가 동시 in-flight일 때 R0가 늦게 도착하면 낡은 응답이 최신 `stockMap`을 덮는다(적대적 리뷰 렌즈2가 포착 → red-first로 `{AAPL:'watchlist'}` 대신 `{}`로 회귀함을 확인한 뒤 수정). **원인은 이름이었다** — 계획이 B27을 "`fetchPage`의 결함"으로 *이름 지어* 두었고 그 이름이 시야를 그 함수 하나로 좁혔다(가토 ⑧ⓘ "판정 축이 대상과 독립이면 틀린 대상에서도 통과"의 *구현판* — 여기선 가드가 옳은 함수에 들어갔는데 **대상 집합이 하나 모자랐다**). 실천: 가드를 넣기 전에 **"이 레이스가 성립하는 조건"을 한 문장으로 적고**(= 같은 상태를 쓰는 비동기 호출이 2개 이상 겹칠 수 있다), 그 조건을 만족하는 형제를 `grep`으로 셀 것 — 같은 훅/모듈 안의 다른 `async` 진입점 · 마운트 이펙트 · 이벤트 핸들러가 후보다. 부수 2가지: ① 세대 가드는 `.then`뿐 아니라 **`.finally`도 게이트**해야 한다(낡은 응답이 `loadingRef`를 열면 다음 무한스크롤 트리거와 겹쳐 이중 append) ② 뮤텍스(`if (loadingRef.current) return`)는 레이스 가드가 아니다 — 리셋 호출을 no-op시켜 **오히려 옛 응답이 화면을 소유하게 만든다**(reset은 뮤텍스를 우회하고 세대를 올려야 한다). **⭐⭐ 이 「이름이 시야를 좁힌다」가 *명시 경고 + 실측 열거를 거친 뒤에도* 모자랐다 — 3연속(task#328·#331), 그리고 세대 가드의 의미론에 빠진 절반이 있다(task#331 회고).** **ⓐ 절차를 지켜도 모자랐다**: task#331에서 계획이 B49를 단수로 서술 → 오케스트레이터가 실측으로 **5곳**을 지목해 S1에 넘김 → S1이 전수 확인 → **그런데도 적대 검토가 「주 인스턴스 `pages/Reports.jsx`가 무수정」임을 잡았다**(CONCERNS §7.3이 HIGH로 못박은 그 경로이고 `.catch` 자체가 없었다). 추가로 `AnalystReport.jsx`에 3경로가 더 있었다(`ReportDetailTabs`가 `key` 없이 렌더해 **같은 마운트 내** 레이스 — `key` 보호를 못 받는다). task#328에서도 계획의 형제 2건이 실제 **5건**이었다(3번째는 `investor_service.py::_parse_signed_int` Naver 폴백 — 안 고치면 **같은 컬럼이 소스에 따라 0/None으로 갈려** 어느 쪽이 결함인지 코드로 판정할 수 없다). **실천: 「결함의 이름」이 아니라 「성립 조건」을 한 문장으로 적고 그 조건으로 grep할 것 — 그리고 그 결과를 *적대 검토에 다시 세게 할 것*(전수를 셌다는 자기 보고는 3회 연속 부족했다).** **ⓑ 세대 가드는 「늦은 착지」만 막고 「보존」은 막지 않는다** — 옛 데이터가 *이미* 착지한 뒤 prop만 갈리면 **경합이 없어** 가드가 발동하지 않고 옛 데이터가 **결정적으로** 새 화면을 소유한다(`ConsensusChart`가 실사례: 티커를 갈면 옛 컨센서스가 그대로 남는다). 그래서 식별자 변경 시 **상태를 `null`(미조회)로 되돌리는** 것이 가드와 쌍으로 필요하고, ⚠️ **`[]`로 되돌리면 「0건」이라는 거짓 진술**이 된다(task#307의 3상태 규율과 같은 가족).

- **입력 경로의 NaN은 3중으로 통과한다 — Pydantic float은 기본 `allow_inf_nan=True`, NaN 비교는 항상 False, 그리고 422 detail이 NaN을 echo해 500이 된다(task#211)**: 위 "응답에 NaN 가드" 가토가 *출력* 방어라면 이건 *입력* 방어다. raw JSON 본문의 `NaN` 토큰은 ① `json.loads`가 허용하고 ② Pydantic v2 float 필드가 기본 `allow_inf_nan=True`로 통과시키고 ③ `low <= high` 같은 범위 검증은 NaN에서 **항상 False**라 조건을 뒤집어도 안 걸려서, **불변이어야 할 문서(발행물 등)에 NaN이 조용히 저장**됐다(적대 리뷰가 라이브 재현). → **float 필드엔 `allow_inf_nan=False`를 명시**하고 범위 검증 전에 NaN을 배제할 것. 연쇄 함정: NaN을 거부하면 FastAPI 기본 검증 에러 응답이 **입력 NaN을 그대로 echo**하고 starlette는 `allow_nan=False`라 422가 **500**으로 터진다 → `main.py`의 `RequestValidationError` 커스텀 핸들러(+`sanitize`)가 앱 전역에서 막는다(엔드포인트별 문제가 아님). **⭐ 그리고 위 3중은 *요청 본문* 경로의 이야기다 — 외부소스 문자열 파싱에는 네 번째 통로가 따로 있다: `float("nan")`·`float("inf")`·`float("Infinity")`는 **ValueError를 던지지 않는다**(task#292 B52). 그래서 `try: float(s) except (ValueError, KeyError): pass`로 감싼 숫자 파싱은 그 토큰들을 **예외 없이 통과시켜** 비유한값을 컬렉션에 넣고, 뒤이은 `sum(xs)/len(xs)`·`max(xs)`가 통째로 NaN이 된다(`market/kr.py::get_analyst_data_kr`의 `TARGET_PRC`·`AVG_PRC`가 실사례). 연쇄로 더 위험한 것은 **진리값 가드가 NaN을 막지 못한다**는 점이다 — `bool(float('nan')) == True`이므로 `if x:`는 NaN을 통과시킨다(`consensus_pipeline.py::run_daily`의 KR override가 같은 파일이 도입한 `math.isfinite` 초크포인트 *옆을* 지나갔다). PostgreSQL `numeric`은 `NaN`을 저장하므로 UPDATE는 성공하고 행이 **영구 오염**된다. 실천: ⓐ 외부 문자열→float 파싱은 `except`만으로 부족하고 **`math.isfinite` 검사가 쌍으로** 필요하다 ⓑ 비유한값 게이트는 `if x:`가 아니라 `if x is not None and math.isfinite(x):`로 쓸 것 ⓒ 파일 안에 초크포인트를 만들었으면 **같은 파일의 다른 쓰기 경로가 그 옆을 지나가지 않는지** grep할 것. **⭐ 그리고 네 번째 통로의 *반대편* — 방어가 겹치면 「이빨 검사」가 거짓 음성을 낸다(task#305)**: 위 세 통로가 「NaN이 새어 들어오는」 이야기라면 이건 **「막긴 막았는데 무엇이 막았는지 모르는」** 경우다. `composition.share_pct`에 `allow_inf_nan=False`를 넣고 task#250 관례대로 그것을 **일시 제거해 이빨을 확인**하려 했는데 **테스트가 계속 통과했다.** 원인은 방어가 **셋**이었기 때문이다 — ⓐ `allow_inf_nan=False` ⓑ 5% 그리드 검증자의 `round(nan)`이 **ValueError**를 던져 pydantic이 422로 바꿔줌 ⓒ **`ge=0, le=100` 자체**(NaN은 모든 비교가 False라 범위 검증에서 탈락). 셋을 **전부** 걷어내고 `Infinity`를 보내야 비로소 실패했다. 여기서 두 사실이 나온다: **(가) 두 비유한값의 운명이 갈린다 — `round(nan)`은 ValueError(→422)인데 `round(inf)`는 **OverflowError**라 pydantic이 잡지 않아 **500**이 된다.** 그러니 이 경로를 암묵 의존으로 두지 말고 검증자 첫 줄에 `math.isfinite` 명시 가드를 둘 것(그러면 두 값이 같은 422로 수렴한다). **(나) 「이빨이 안 잡힌다」는 테스트가 무의미하다는 뜻이 아니라, 방어가 중복돼 *어느 하나도 단독으로 load-bearing이 아니라는* 뜻일 수 있다** — 그 구별을 계측 없이 추정했으면 멀쩡한 가드를 "쓸모없다"며 지웠을 것이다. 실천: 이빨 확인이 실패하면 **그 축을 지키는 방어를 전부 열거해 하나씩이 아니라 *모두* 제거**해 보고, 그때도 통과하면 그제야 테스트를 의심할 것. **⭐ 그리고 그 *반대 방향* — 이빨을 **체계적으로** 재보면 「아무도 지키지 않는 축」이 드러난다(task#315·#316)**: 위 (나)가 「방어가 중복돼 이빨이 안 잡힌다」였다면 이건 **「방어가 0이라 이빨이 안 잡힌다」**다. 두 경우는 **같은 관측**(제거해도 통과)에서 나오므로, 구별하려면 **여러 축에 같은 fault-injection을 돌려 비교**해야 한다 — 한 축만 재면 어느 쪽인지 알 수 없다. 실측 2건: ⓐ 순수 함수에 4종을 주입했더니 3종은 각각 1~2건 실패했는데 **전문가 축 필터 제거만 0 fail**이었다(= 그 축을 아무 테스트도 지키지 않았다) → 대조군 테스트를 추가하고 방어 2겹을 *모두* 제거해야 깨짐을 확인했다 ⓑ 오버레이 신규 6축이 전부 *선언*만 재서 `pointerEvents:'none'` 주입에 **6축 모두 초록**이었다(오버레이를 무력화하는 가장 자연스러운 변경이 통째로 안 잡혔다) → 그 핀을 신설했다. **실천**: 새 가드의 이빨을 **한 축만** 확인하지 말고 같은 파일의 인접 축에도 같은 주입을 돌려 볼 것 — **0 fail이 나오는 축이 곧 「선언만 재는 테스트」**다.

- **선택 필드를 `x: float = Field(None)`으로 쓰면 *키 생략은 통과하고 명시적 `null`만 422*가 된다 — 그 비대칭이 요청 **전체**를 죽인다(task#250)**: 위 task#211 가토의 형제(둘 다 Pydantic v2 입력 검증). pydantic v2는 **`validate_default=False`가 기본**이라 기본값 `None`은 검증을 안 타지만, 클라이언트가 **명시적으로 보낸 `null`** 은 선언 타입(`float`)의 검증을 탄다 → `float_type` 422. 그래서 **생략한 요청은 잘 되는데 `"change_pct": null`을 보낸 요청만 죽는** 형태가 되고, 그 필드가 중첩 배열(발행물 지표 칩) 안에 있으면 **칩 하나 때문에 발행 요청 전체가 422**로 막힌다(버그리포트 H1 전체차단). 생략 케이스만 단언하는 테스트는 이걸 **원리적으로 못 잡는다**(`test_publish_with_point_metrics`가 정확히 그랬다). → **선택 필드는 반드시 `Optional[X] = Field(None, ...)`**(로컬 `.venv`가 py3.9라 PEP604 `X | None` 금지 — 위 가토 참조). **`Optional`로 바꿔도 `allow_inf_nan=False`의 NaN 차단은 유지된다**(로컬 py3.9 + 컨테이너 py3.12/pydantic 2.13.4 양쪽 실측: 생략→None · `null`→None · `7.5`→7.5 · `NaN`/`Infinity`→422 `finite_number` · `"abc"`→422). 회귀 테스트는 **명시적 null 케이스**와 **NaN 거부 핀** 두 개를 쌍으로 둘 것 — 후자는 red-first가 원리적으로 불가하니(수정 전에도 통과) 대신 `allow_inf_nan=False`를 일시 제거해 실제로 실패하는지 **이빨 검증**하고 원복할 것. 동종 패턴 탐지: `grep -rn "= Field(None\|= Field(default=None" backend/routers/ backend/services/`. **⚠️ 그런데 그 탐지 grep은 *배열 변형*에 원리적으로 블라인드하다 — 실측 위반 1건이 그것 때문에 25일간 생존했다(task#218 → 2026-08-20 일괄 승급에서 발견)**: 같은 함정의 배열형은 `List[X] = Field(default_factory=list)`·`Field([])`이고, 위 grep은 **그것을 한 글자도 잡지 못한다**(실측: 38건 히트 중 대상 0건). 배열형 규약은 이 가토가 아니라 **ADR-0034 보정 ③**에 「선택 배열은 **예외 없이** `Optional[List[X]] = Field(None)`」으로 따로 적혀 있어서, **한쪽만 읽으면 한쪽만 고친다** — 실제로 `analyst_reports.py`의 `PointMetric.change_pct`(스칼라)는 이 가토를 인용한 주석과 함께 올바르게 고쳐져 있는데 **바로 다음 줄**의 `ReportPoint.metrics`(배열)는 옛 형태로 남았다(`:36`). 로컬 실증(pydantic 2.13.4): 키 생략 → OK · **명시적 `null` → 422 `list_type`** · `[]` → OK. `points`는 발행 본문의 배열이므로 **포인트 하나의 `"metrics": null`이 발행 요청 전체를 막는다**(형제 `tech_reports.KeyPoint.metrics`는 `Optional[...]`이라 통과한다). 현재는 dormant다 — 루틴 프롬프트가 `metrics`를 항상 배열로 넣도록 지시하지만 그 절엔 **「없으면 생략하라」가 없다**(형제 tech 절은 `change_pct`에 그것을 명시한다). **실천 2가지**: ⓐ 「선택 필드」 규약을 적용할 땐 **스칼라와 배열을 한 목록으로 세라**(같은 `validate_default=False`에서 나오는 같은 함정이다) ⓑ **탐지 grep을 처방문에 적을 때는 그 grep이 함정의 *모든 표현형*을 덮는지 한 번 돌려 확인할 것** — 덮지 않는 처방은 「감사했다」는 거짓 안심만 만든다. 넓힌 패턴: `grep -rnE "List\[.*\] = Field\((default_factory=list|\[\])" backend/routers/ backend/services/`. 이것은 인증 문서의 「감사 패턴을 좁히면 그 감사는 통과해도 무의미하다」와 **같은 결함의 세 번째 사례**이고, 이번엔 **가토 자신이 그 좁은 패턴을 처방문으로 박제**하고 있었다.

- **⭐ 신규 구조 필드를 만들 땐 「가장 닮은 형제」의 `model_validator`를 전부 열거해 각각 "이 필드에도 필요한가"를 물을 것 — 그 목록이 곧 체크리스트다(task#297, 적대 리뷰 HIGH)**: 위 두 가토가 *타입·Optional* 규약이라면 이건 **교차필드 불변식**이다. `variants[]`(계보 비교축)를 신설할 때 형제 `market.estimates[]`에는 있던 중복 방어(`Market._estimates_consistency` — 통화·단위·연도 동일성 + `is_basis` 최대 1개)의 **짝을 만들지 않아**, `options[].name`이 같은 옵션 2개와 `axis_label`이 같은 축 2개가 **201로 통과**했다(리뷰가 TestClient로 재현, 무쓰기 확인). 중복이 통과하면 렌더러가 같은 이름의 행을 두 번, 같은 제목의 표를 두 개 그려 **독자가 서로 다른 둘로 읽는다.** 같은 라운드에 두 번째 결함도 나왔다 — `strength`·`tradeoff`가 **둘 다** 결측인 옵션이 통과해 비교표 행이 이름만 남았다(배열 하한 `min_length=2`가 *축* 수준에서 막으려던 「비교가 아니라 서술」이 *행* 수준에서 되살아난 것). **어느 자동 게이트도 이 클래스를 못 잡는다** — 타입은 맞고 상·하한도 지켜지므로 pydantic이 통과시키고, `test_api_doc_sync.py`는 엔드포인트 *존재*만 본다. 부수 교훈 2가지: ⓐ **문서가 검증 강도를 과대 서술할 수 있다** — 「이점과 대가를 **반드시** 쌍으로」가 ADR·CONTEXT·Cowork 문서 **세 곳**에 있었는데 스키마엔 그 강제가 없었다. 문서의 강한 단어(반드시·항상·최소)는 실제 `Field(...)`·validator와 대조할 것. ⓑ **완화 판단은 양성 테스트로 못박아야 되돌려지지 않는다** — 「최소 하나이고 **둘 다는 아님**」은 음성 테스트만으론 표현되지 않는다(쌍을 강제해도 음성 테스트는 그대로 통과한다). 한쪽만 있는 입력이 **201임을 단언하는 테스트**를 쌍으로 두고, 왜 하한을 하나로 뒀는지(한쪽만 아는 계열에서 발행 전체를 422로 막는 대가가 이득보다 크다)를 그 테스트에 적을 것.

- **⭐ 스키마가 있는 쓰기 API를 호출할 땐 규약 산문이 아니라 *검증기*를 직독하고, 호출 전 로컬 교차검증을 만들 것 — 재시도 예산보다 압도적으로 싸다(task#322 실측 422 **0회 / 7종**)**: 위 세 가토(task#211 NaN 3중 통과 · #250 `Optional` 비대칭 · #297 형제 `model_validator` 열거)가 전부 「스키마를 *만들* 때」의 이야기라면 이건 **그 스키마에 *쓸* 때**의 이야기다. 주요기술 리포트 7종을 발행하는 태스크에서 계획은 「422 지뢰밭」을 예상해 파일럿 1건으로 지뢰를 다 밟게 하고 나머지 6종에 slug당 3회(총 21회) 재시도 예산을 뒀는데, **실제 422는 0회**였고 예산은 한 번도 쓰이지 않았다. 갈림은 하나였다 — 규약 문서(`scripts/cowork-routine-prompt.md` §3)만 읽지 않고 **`backend/routers/tech_reports.py`의 Pydantic 모델과 `model_validator`를 전부 직독**해 `max_length`·`min_length`·`ge/le`·`Literal` enum·교차필드 검증을 그대로 옮긴 것이다. **규약 산문은 상·하한의 정본이 아니다** — 사람이 읽을 문서라 「최대 4개」류는 적히지만 `min_length=1`·`max_length=40`·`Literal["done","in_progress","planned"]` 같은 것은 전부 적히지 않거나 일부만 적힌다(그리고 위 #297 가토가 기록한 반대 방향도 있다 — 문서가 스키마보다 *강하게* 서술하는 경우). 그 위에 **호출 직전 로컬 사전검증**을 붙였다: 축별 합 100 · `share_pct` 5의 배수 · 배열 항목수 상·하한 · `composition.tech[].leaders` ⊆ `players[].name` · `minerals[].used_in` ⊆ `tech[].name` · `estimates` 전 원소의 year/currency/unit 동일성 · `metrics` 개수 · `status` enum. **이 사전검증은 한 번 만들면 형제 대상 전체에 그대로 이전된다** — 병렬 서브에이전트 6명 전원의 프롬프트에 「POST 전에 이 검증을 반드시 돌려라」를 필수 게이트로 박았고 6/6이 첫 호출에서 201을 받았다. 실천 3가지: ⓐ **재시도 예산을 짜기 전에 검증기를 직독**할 것(예산은 실패를 전제하는 설계이고, 사전검증은 실패를 없애는 설계다 — 후자가 싸다) ⓑ 교차필드 참조(`leaders`⊆`players` 류 dangling reference)는 **로컬에서 집합 차집합 한 줄로** 잡히니 왕복에 맡기지 말 것 ⓒ 되돌릴 수 없는 쓰기(이 저장소의 발행물은 `slug`당 1행이고 삭제가 admin 세션 전용이다)에서는 사전검증이 안전장치까지 겸한다 — 422는 아무것도 쓰지 않으므로 **검증 실패가 곧 무해한 실패**이고, 그 성질을 활용해 위험을 왕복에서 로컬로 옮기는 것이다.

- **Admin 역할 설정**: `UPDATE users SET role = 'admin' WHERE email = '...'` (Docker postgres 직접). admin만 리포트 생성·Guru 크롤 가능.

- **user_menu_permissions**: 사용자별 메뉴 표시 제어. `PUT /api/admin/users/:id/permissions`로 관리. 프론트 `AuthContext`가 로그인 시 로드해 nav 필터링. 허용 메뉴 목록은 `admin.py`의 `ALL_MENUS`에 정의.

- **로컬 `.venv`는 Python 3.9.6 (Docker 컨테이너는 3.12) — 런타임 평가 어노테이션에 PEP604 `X | None` 금지, `Optional[X]` 사용**: Pydantic 모델·FastAPI 시그니처처럼 어노테이션이 런타임 평가되는 자리에 `float | None`을 쓰면 로컬 pytest/실행에서 `TypeError`(3.9는 union 연산자 미지원). 문자열 주석(`"dict | None"`)은 평가 안 돼 통과하므로 더 헷갈린다 — 실사례: 사용자 목표가·손절가 작업에서 로컬 pytest가 배포 전 포착(2026-07-04 retro). 프로덕션 3.12에선 동작하지만 로컬 테스트가 게이트이므로 사실상 하드 제약.

- **로컬 `.venv` ≠ Docker 의존성**: `lxml`은 `requirements.txt`에 있고 Docker 이미지엔 설치되지만 **로컬 `backend/.venv`엔 없다**. 로컬 pytest로 검증할 코드/테스트에서 HTML 파싱은 `BeautifulSoup(html, "lxml")` 대신 stdlib `BeautifulSoup(html, "html.parser")`를 쓸 것(로컬·프로덕션 모두 동작). 로컬에서만 필요하면 `.venv`에 직접 설치.

- **로컬 `.venv` ≠ Docker는 *패키지 유무*뿐 아니라 *같은 패키지의 버전차*로도 갈린다 — 그 차이가 API 형태를 바꿔 스크립트가 조용히 빈 결과를 낸다(task#232)**: 배포 이미지의 **FastAPI 0.138.1은 `app.include_router()`로 들어온 라우트를 `_IncludedRouter`로 감싸 `.path`도 `.routes`도 노출하지 않고 `original_router`만** 준다. 반면 로컬 `.venv`(구버전)는 `app.routes`에 평탄하게 들어온다. 그래서 `app.routes`를 순회해 엔드포인트를 열거하는 코드는 **로컬에선 138개를 세고 컨테이너에선 0개를 센다** — 인증 감사(`scripts/audit_unauth_endpoints.py`)가 처음 그렇게 `전체 0 / 무인증 0`을 내며 **거짓 통과처럼 보였다**(총계 0을 "문제 없음"으로 오독하기 쉽다). 대응: 라우트 트리를 열거할 땐 `routes`와 `original_router` **양쪽을 재귀 하강**할 것(`_walk` 패턴). 일반화하면 **"라이브 게이트"를 자칭하는 스크립트는 배포 환경에서도 돌려 *숫자가 실제로 나오는지* 확인해야 완성**이다(0/빈 결과를 성공으로 읽는 게이트는 게이트가 아니다). 위 fixture-pass-live-fail 가족의 *검증도구판*이며, `test_api_doc_sync.py`가 `app.routes`를 쓰는 것도 같은 위험 표면이다(현재는 로컬 pytest 전용이라 무해).

- **DART `fnlttSinglAcntAll`(전체 재무제표, 현금흐름표 포함)은 `fs_div`(CFS/OFS)를 *요청 필수값*으로 받는다** — 누락 시 `status 100 "필수값(fs_div)이 누락되었습니다"`. 반면 `fnlttSinglAcnt`(주요계정, `backlog.get_financials`)는 fs_div 없이 호출해 응답에서 행별 `fs_div`로 필터한다(둘을 헷갈려 복붙하면 깨짐). **그래서 fnlttSinglAcntAll은 ① 요청에 fs_div를 넣고(연결 CFS 우선→없으면 OFS 폴백), ② 응답을 행별 fs_div로 *필터하면 안 된다* — fs_div를 요청한 응답은 단일 fs라 행에 `fs_div` 필드를 echo하지 않아 `row.get("fs_div")`가 None→전 행 스킵된다.** 계정은 `account_nm`(회사마다 표기 변동: "영업활동현금흐름"↔"영업활동으로 인한 현금흐름")이 아닌 **`account_id`(XBRL 표준, 예 `ifrs-full_CashFlowsFromUsedInOperatingActivities`)로 매칭**, 한 콜이 thstrm/frmtrm/bfefrmtrm로 3개 연도를 준다. 이자보상 분모는 `금융비용`(FinanceCosts, FX손실 포함 과대)이 아니라 **`이자의 지급`(`ifrs-full_InterestPaidClassifiedAsOperatingActivities`)**. (`market/kr.py` get_annual_financials_kr의 FCF·이자보상, task#117.)

- **yfinance는 `get_income_stmt()`/`get_balance_sheet()`/`get_cashflow()` *메서드*(무공백 index 라벨 `OperatingCashFlow`·`TotalRevenue`·`CapitalExpenditure`)와 `.income_stmt`/`.cash_flow` *프로퍼티*(공백 라벨 `Operating Cash Flow`·`Total Revenue`)의 index 라벨 규칙이 다르다** — `format._yf_val`은 exact 매칭(`key not in src.index`)이라 라벨이 어긋나면 예외 없이 **조용히 None**을 반환한다. `market/us.py`는 income_stmt/balance를 get_* 메서드로 쓰므로 **현금흐름도 반드시 `t.get_cashflow(freq='yearly', as_dict=False)` 메서드로** 받을 것 — `t.cash_flow` 프로퍼티를 쓰면 공백 라벨이라 FCF·CapEx가 전부 None이 된다. **단위테스트는 응답을 mock해 이 라벨 불일치를 못 잡고 라이브 yfinance에서만 드러나므로, 외부소스 파싱 슬라이스는 라이브 1종목 추출 대조를 DoD에 넣을 것**(KR Naver row·DART account_id도 동일 — fixture 통과·실데이터 실패가 task#111/#117에서 반복됨). (task#117.)

- **신규/개작 SQL은 query-mock 테스트가 라이브 정합을 못 잡는다 — 단건→배치 개작(ANY 배열화·VALUES 조인) 함정 2종 + 라이브 스모크 DoD(task#135)**: ① **uuid 컬럼에 `= ANY(%s)`로 파이썬 str 리스트를 넘기면 text[]가 돼 `operator does not exist: uuid = text` 라이브 즉사** — 단건 `WHERE user_id = %s`는 str이 unknown 타입으로 암묵 캐스트돼 *동작하던* 것이 배열화에서 깨진다(동작하던 단건 코드를 배치화할 때 특히 방심). `ANY(%s::uuid[])` 명시 캐스트. ② **VALUES 행 나열을 바깥 괄호로 감싸면(`VALUES ((a,b),(c,d))`) N행이 아니라 record 컬럼 1행**이 돼 `AS v(ticker,d)` 컬럼 매핑 에러 — 행별 `(a,b), (c,d)` 나열만(`consensus._values_placeholder` 주석·`test_values_placeholder_shape`가 형태를 못박음). 둘 다 pytest green(query mock) 상태의 배포-즉사 버그였다(①은 적대 리뷰, ②는 메인 세션 직독이 포착 — 워크플로우 verify는 둘 다 블라인드). **SQL을 새로 짜거나 단건→배치로 개작하는 슬라이스는 mock 테스트 외에 배포 후 해당 엔드포인트 라이브 스모크를 DoD에 포함**(위 외부소스 파싱 라이브 대조 가토의 SQL판 — fixture-pass-live-fail 가족).

- **키움 daily_df(tz-naive) ↔ yfinance(`^KS11` 등, tz-aware) 정렬은 tz strip 필수** — 키움 일봉 인덱스는 tz-naive, yfinance 인덱스는 tz-aware(Asia/Seoul)라 `pd.concat([naive, aware], axis=1)`가 `TypeError`를 낸다. 이걸 둘러싼 broad `try/except`가 삼키면 계산(KR beta 등)이 예외 없이 **조용히 None**으로 빠진다(라이브 전용 — fixture/단위테스트는 ^KS11 라이브 미모킹이라 미포착, 위 #111/#117 fixture-pass-live-fail 가족). **KR 종목 series를 yfinance 지수/자산 series와 정렬하는 모든 계산(베타·상관·상대강도 등)은 한쪽 인덱스를 `tz_localize(None)`으로 맞출 것.** 부수 교훈: 외부 정렬을 broad `except: pass`로 감싸면 "기능이 조용히 꺼짐"이 되니, 적어도 진단 로그를 남기거나 좁은 예외만 잡을 것. (`report_generator.generate_report` KR beta, task#116 — 적대적 Review phase가 fixture·빌드가 못 잡은 이 라이브 버그를 포착.)

- **"다른 지표는 다 나오는데 RSI만 빈" = fetch 실패가 아니라 히스토리 부족일 수 있다 — 원인 단정 전 라이브 프로브(task#126)** — RSI(14봉)는 상장 **<14거래일** 신규 종목에선 `calc_rsi`가 전부 NaN → `daily_rsi.rsi`=null(직렬화 시 sanitize로 null). 반면 EMA(ewm)·52주(max/min)·HV(std)·매물대(거래량 프로파일)는 짧은 히스토리서도 값이 나와서, "요약 탭 '매물대 & RSI 현황' 차트는 뜨는데 지표>기술·수급의 'RSI 예상 타점'만 빔"이 **fetch 실패로 오인**되기 쉽다. 실제 SPCX=상장 12거래일 사례: `_t.history(period="1y")`가 12행만 반환 → RSI NaN, EMA/52주/HV는 정상. **자연 해소**(14거래일 넘으면 자동 표시)이므로 코드 버그로 단정 말 것. **진단은 컨테이너 라이브 프로브로**: `docker exec -i portfolion-backend-1 python -`로 `yf.Ticker(t).history(period="1y")` 행수와 `indicators.calc_rsi(df["Close"]).notna().sum()`을 찍으면 히스토리 부족(행수<14) vs fetch 실패(0행)가 즉시 갈린다. **교훈: 외부데이터 증상은 라이브 프로브 선행** — fg-ask에서 라이브 데이터 없이 "ThreadPool 동시 fetch rate-limit"로 오진했다가 프로브로 정정(다 나오는데 하나만 빈 증상을 fetch 실패로 성급히 귀속하지 말 것). 부수: daily RSI가 `daily_df`와 별도로 `get_timeframe_rsi`→`get_history_df` 재fetch(ThreadPool 동시)하던 구조는 `get_timeframe_rsi(..., daily_df=None)` 옵셔널로 재사용 정리(중복 제거·잠재 flaky 완화)했으나 **신규종목 null의 원인은 아니었다**(두 경로가 동일 행 반환). 프론트: RSI 전무 시 기술·수급 탭은 `VolumeRsiSnapshot`(매물대) 폴백 표시(요약 탭과 parity), RSI 생기면 `RsiTable`(일/주/월 타점)로 전환(`ReportDetailTabs`의 `hasRsi` 분기).

- **yfinance 퍼센트 필드는 *소수분수*다 — 표시 ×100, 그리고 문서/fixture 예시값도 분수로 맞출 것** — `info.shortPercentOfFloat`(0.0098 = 0.98%)·`institutional_holders.pctHeld`/`pctChange`·`insider_purchases`의 `% Buy/Sell Shares`(0.0476 = 4.76%)·`info.dividendYield` 등은 0~1 소수분수로 온다. 프론트는 `(v*100).toFixed(n)%`로 표시해야 하고(한 필드만 빠뜨려 `0.01%`처럼 보이기 쉬움 — task#122 short% 실버그), **API_SPEC 예시값·테스트 fixture도 분수 스케일로 적어야** 한다(퍼센트로 적으면 실데이터와 어긋나 혼동 — task#123 doc/fixture 정정). 단위테스트는 렌더 %를 단언 안 해 이 스케일 트랩을 못 잡으니(fixture-pass-live-fail의 프론트 표시판), % 표시 필드는 *필드별로* 스케일 검증. (task#122·#123 2연속 — 적대적 Review가 포착.)

- **수주잔고/데이터 파싱 변경은 배포 후 전 종목 재적재 UAT 필수**. fixture 단위 테스트가 전부 통과해도 운영 재적재가 fixture에 없던 실데이터 케이스(외화 `(단위 : USD천)`, 단위 캡션 줄바꿈 분리, 연결 전 분기의 회사컬럼 표 등)를 잡아낸다. 특히 **단위 캡션 파싱 실패 시 '안전한 기본값(억원)' 폴백은 ×100 대형 오저장**을 만드니, 추출 실패는 기본값이 아니라 pending(누락)으로 처리할 것('wrong < missing').

- **KIS 국내선물옵션 *시세* TR은 응답이 `output1`/`output2`/`output3`으로 분할된다 — 주식 현재가(`output` 단수)와 다름(task#156)**: `inquire-price`(`FHMIF10000000`)·`inquire-daily-fuopchartprice`(`FHKIF03020100`) 모두 단수 `output`이 아니라 **output1**(계약 quote: `futs_prpr`·`futs_prdy_ctrt`·`mrkt_basis`·`futs_last_tr_date`·`hts_kor_isnm`)·**output2**(일봉이면 바 리스트: `stck_bsop_date`·`futs_prpr`·`futs_oprc/hgpr/lwpr`·`acml_vol`)·**output3**(기초 KOSPI200 지수)으로 온다. `d.get("output")`만 읽으면 `rt_cd=0`인데도 늘 빈값 → "코드/파라미터 오류"로 오진(그릴링 프로브가 이 함정에 빠져 데이터 확보 게이트를 '닫힘'으로 오판 → task#155에서 실제론 코드가 맞았고 파싱버그였음을 규명). **라이브 프로브는 fetch 200뿐 아니라 응답 봉투(output vs output1/2/3) 파싱까지 확인해야 완성**(fixture-pass-live-fail의 프로브판). 표시 **베이시스는 `mrkt_basis`(선물−현물=콘탱고/백워데이션)** — 이론 `basis`(이론가−현물) 아님(필드명 유사·값 상이 21.93 vs 5.41, 적대 리뷰 포착 task#156). 최근월물 코드는 공식 `"A01"+연%10+월`(분기월 3/6/9/12)+`futs_last_tr_date` 롤오버. 읽기전용 시세 TR만·경계 ADR-0022. (`backend/services/kis/futures.py`, task#156·157)

- **요청경로 시장지표 fetch도 "성공-but-빈응답"을 last-good에 박제 금지 — 외부 API `rt_cd=0`(무예외) 빈 output은 예외 가드를 통과한다(task#157)**: `market_indicators/kospi_futures.py`가 처음엔 `_fetch` *예외*만 가드하고 값 None은 통과시켜, KIS가 성공응답(rt_cd=0)에 빈 output1을 주면 all-None dict를 `_mc_save`로 박제→직전 양호값 클로버('wrong<missing' 위반, 적대 리뷰 포착). `indices.py`의 `if any(v is not None ...)` 지속 가드처럼 **값 수준 가드**(price None/빈 history면 fetch 실패 취급→last-good 폴백, degenerate `_mc_save` 금지)가 필요. 위 배치-백킹 뷰 클로버 가토의 *요청경로판* — 배치든 요청이든 외부소스 성공응답이 비어도 캐시를 덮지 않게. 회귀는 저장 양호값 시드 후 빈응답이 `_mc_save` 미호출·last-good 반환하는지 단언(예외 side_effect가 아니라 *값이 None인 반환*을 모킹해야 이 경로를 실제로 침).

- **KR 시장-날짜(최근월물/영업일 판정)는 `datetime.now(ZoneInfo("Asia/Seoul")).date()` — bare `date.today()` 금지(컨테이너 UTC, task#157)**: 백엔드 컨테이너에 TZ env가 없어 `date.today()`=UTC라, 00:00~09:00 KST(UTC 전일)엔 하루 뒤처져 최근월물 휴리스틱·롤오버 판정이 어긋난다. `kospi_signal.py`·`scheduler/schedule.py`의 `_KST=ZoneInfo("Asia/Seoul")` 패턴 재사용. (위 키움 daily_df tz-strip 가토는 naive↔aware series *정렬*용 `tz_localize(None)` — 이건 *어느 달력일이냐* 판정용이라 별개 문제.)

- **KR 리포트의 시세 소스(`get_quote_kr`)와 일봉 차트 소스(`get_history_df`)는 다른 TR이라 스케일이 어긋날 수 있다 — 매물대/RSI가 "깨져" 보이면 표시 버그가 아니라 박제된 price 값 자체를 의심.** 리포트 상세의 현재가 마커는 `get_quote_kr`(키움 ka10001 `_AL`→KIS→Naver), 매물대/RSI는 `get_history_df`(키움 ka10081 일봉)에서 온다. 한쪽만 액면/병합 조정되면 둘이 최대 5배까지 어긋나, 같은 차트에 마커가 매물대 밴드 밖으로 찍혀 깨진 듯 보인다(005930이 ~70k=실값 354k의 1/5로 박제된 사례). 방어: `get_quote_kr(regular=False)`(NXT 라이브)는 **독립 피드 2-of-N 다수결**(`_kr_pick_basic`/`_corroborated_pick`, `services/market/kr.py`, task#98) — 어떤 현재가 피드가 *다른 독립 피드 ≥1개*와 ±2x([0.5,2.0]) 이내로 **합의(corroborate)**해야 신뢰하고, trusted 중 우선순위 최상위(키움 NXT→KIS→Naver→키움 KRX)를 반환한다. lazy escalation으로 평소엔 ① 키움 NXT+키움 KRX 2콜 합의 → NXT 반환(KIS/Naver 미호출, 비용 무변), ② **불일치(어느 한 피드 글리치)면** KIS(설정 시)·Naver를 추가 호출해 최대 4피드 다수결로 합의값을 고르고 outlier(글리치)를 폐기한다. **표는 참조 종류가 아니라 독립 현재가 피드** — prev_close/일봉(ref_close)은 NXT와 같은 피드라 별도 표 아님(ref_close는 가드에서 빠지고 변동률 계산용으로만 남음). 어떤 단일 피드 글리치도 다수를 못 이기므로 **KRX-poison(KRX 단일 글리치)과 NXT 자기일관 `_AL` 전체오염(quote·prev·일봉ref 동시오염, task#96)을 둘 다 해소**한다(task#96 잔존이던 KRX 단일 앵커 false-reject 제거). 키움 부재/단일(불일치 아닌 outage)·전 피드 합의 불가는 **degenerate**: 우선순위 첫 피드를 자기 prev_close ±30%로만 자가검증(`_kr_pick_degenerate_lazy`, lazy short-circuit·기존 단일피드 보호 보존, wrong<missing). **후속 후보**: regular=True(리포트 스냅샷)는 다수결 미적용(KRX 자기일관 글리치는 task#94 "KRX 안정" 근거로 이론적 + 일배치 +500~2000콜/일 실비용) — `_kr_pick_regular`로 기존 ①prev±30%+②일봉2x 유지. 대시보드 핫패스(`get_quotes_batch`/`_changes_from_closes`)는 이 가드를 안 타며(ephemeral 용인) 무변경. 참조/피드 부재 시 해당 검증만 생략. 진단 시 네이버 공개 API(`m.stock.naver.com/api/stock/{code}/basic`)로 creds 없이 실값 대조 + 컨테이너에서 KRX(`005930`) vs SOR(`005930_AL`) 원값 직접 비교(`docker exec -i portfolion-backend-1 python - < probe.py`). **근본원인(task#94)**: 005930 ~70k는 영속 버그가 아니라 **NXT `_AL`(SOR 통합코드) 순간 이상체결**이 일배치에 박제된 *일시적* 값 — 영속 소스 버그로 단정해 소스부터 고치려 들지 말 것. 부수: 키움 `_AL`은 평시에도 NXT 시간외가(KRX 정규장과 ~1% 차)를 반환. (task#93·94) **⚠️ 정정(task#170, ADR-0020 amendment 2026-07-11)**: "005930이 *정확히* 70000.0으로 박제"된 극단값(354k의 1/5)의 원인은 피드 글리치가 아니라 **로컬 pytest가 prod DB에 fixture(`price:70000.0`)를 직접 쓴 오염**이었을 가능성이 매우 높다(오염일=테스트 실행일, execute 미mock+reload footgun, 당시 네이버 라이브 357.5k 정상). 실제 수정은 이 다수결/게이트가 아니라 **conftest `_block_real_db` 가드(task#169)**였다. **NXT `_AL` ~1% 시간외가 차·다수결 가드 자체는 유효**(진짜 글리치 방어) — 정정 대상은 오직 "정확히 70000.0"의 귀속뿐. 라운드 70k가 또 보이면 피드 글리치 전에 **테스트 오염부터 의심**하라.

- **시세 기준 이원화 — 리포트 스냅샷=KRX 정규장 / 라이브 대시보드=NXT(task#95, ADR-0020)**: 키움 코드선택 단일 분기점 `client.integrated_code(stk_cd, regular=False)`에 `regular` 플래그가 있다 — 기본 False=`_AL`(NXT 시간외), `regular=True`=평문 KRX 코드(정규장 종가). 시세 체인(`get_quote`→…→`get_basic_info`)·차트 체인(`get_history_df`→…→`fetch_bars`)으로 전파되고, **리포트 스냅샷 writer만 `regular=True`로 opt-in**(`report_generator` 당일/백필 daily_df·get_quote·경쟁사, `report.py:refresh_analyst`). RSI(weekly/monthly_df·`indicators`)·대시보드(`get_quotes_batch`)·종목추가·`resolve_name`은 NXT 기본 유지. `get_quote` TTL 캐시 키에 `regular` 포함(정규장/NXT quote 충돌 방지). → 같은 종목이 리포트(354k)와 대시보드(350.5k)에 ~1% 다른 현재가를 보일 수 있는 건 의도된 기준 차. **이게 리포트의 NXT `_AL` 글리치 노출을 없앤다**(리포트는 `_AL` 안 탐) — 위 다수결 발산 가드는 이제 대시보드(NXT) 백스톱. **자기일관적 `_AL` 전체오염(quote·prev_close·일봉ref가 다 같은 오스케일)도 다수결이 잡는다**(NXT≠KRX 불일치 → Naver escalate → NXT outlier 폐기, task#98 — 단일 피드 self-check ±30%/±2x로는 통과하던 케이스). **단, regular=True가 리포트의 *근본해결은 아니다*** — KRX 두 TR(quote ka10001·일봉 ka10081)이 같은 배치 시점에 함께 일시 글리치하는 **KRX 자기일관 오염**엔 면역이 아니다(같은 KRX 피드라 서로 합의해 동일피드 교차검증·`_price_sane` 블라인드, 005930 리포트 또 70k 사례 task#101). 이건 **박제-시 독립피드 게이트**(`report_generator.generate_report`, KR만)가 막는다 — 저장 직전 KRX와 독립인 ref 피드로 `price`·일봉 기준종가를 2x([0.5,2.0]) 교차검증해 어긋나면 그 종목 박제를 **스킵**(직전 양호 스냅샷 유지, wrong<missing). **ref 피드는 다중 독립피드(task#118 강화)**: ① 네이버 retry-once(`_kr_basic_naver`, 첫 호출 예외 시 0.5s 후 재시도 — transient rate-limit 완화) → ② 그래도 실패·None이면 KIS 폴백(`_kr_basic_kis`, 키 미설정이면 None이라 dormant-safe). **ref 전무(둘 다 실패·None) 시 박제 스킵** + loud `print("[Report] ...")` 로그(기존 단일 네이버 부재 시 "검증 생략·진행"이 게이트를 no-op으로 무력화하던 구멍 정정, task#118 — 005930 70k 재발 계기). ref가 하나라도 있으면 2x 교차검증 유지. `backfill_ticker`(과거 날짜)는 현재가 대조 불가라 미적용. 다수결(regular=False)과 같은 원리(독립 피드 교차검증)의 리포트판. 또 **"fix 배포는 이미 박제된 스냅샷을 소급 치료하지 않는다"** — 배포 전 일배치가 박제한 stale 70k는 *재생성*해야 KRX로 덮인다(재생성 전 프로브로 라이브 소스 깨끗 확인 → transient 글리치 재박제 방지). **⚠️ 정정(task#170, ADR-0020 amendment 2026-07-11)**: 여기서 반복 등장하는 "005930 리포트 또 70k(task#101)"·"70k 재발(task#118)"의 *정확히 70000.0* 박제는 실은 **로컬 pytest→prod DB 오염**이 유력하다(KR 리포트 테스트가 005930을 `price:70000.0`으로 mock하고 `execute`를 안 가려 prod snapshots에 오늘 날짜로 커밋; 오염일에 task#101·#118 당일 포함). **박제-시 독립피드 게이트는 유효**(진짜 KRX 자기일관 글리치 방어)하나 **테스트는 게이트를 우회하므로 관측된 70k엔 발동한 적 없다** — 실제로 오염을 멈춘 건 conftest `_block_real_db` 가드(task#169). 게이트/메커닉은 미래 글리치 보험으로 유지하되, "게이트가 70k를 해결했다"로 읽지 말 것.

- **배치-백킹 뷰(랭킹·KR 업종 모멘텀 등)는 외부 API(키움)를 *요청·기동 경로*에서 라이브 호출하지 말 것** — 배치가 사전계산해 `market_cache`/테이블에 저장하고 **요청은 저장값만 읽는다**(요청당 N콜 직렬=수초 지연; 보유→업종 인덱스처럼 "요청 시 라이브 빌드"가 끼면 캐시 만료마다 느려짐 — task#50). 외부 fetch는 ① **실패를 조용히 삼키지 말고 로깅**(silent except는 진단 불가 — task#48 `_fetch_one_sector`가 빈 종가를 삼켜 all-None 박제) ② **빈/all-None 결과를 캐시에 박제 금지**(전부 None이면 save 생략·직전 양호값 유지; 안 그러면 시드 가드가 "채워짐"으로 오판해 고착). **의심 트리거(예: base_dt)가 아니라 *실패 클래스(all-None)*를 가드**해야 근본원인 미상이어도 재발을 막는다. (task#48 위반→#49 기동시드가 노출·증폭→#50 확립 3-타석.) 기동 시 빈 캐시 적재는 `_seed_*_if_empty`(랭킹·kr_sector) 패턴.

- **배치-백킹 store의 delete-rewrite(replace) 갱신은 fetch 실패 시 delete를 *스킵*해야 — "빈 결과 박제 금지"의 파괴적 변형(task#160)**: 위 "빈/all-None 결과 캐시 박제 금지"는 *save 생략*을 말하지만, 저장을 `DELETE+INSERT`(예: `dividends.replace_schedule`)로 하는 store는 fetch 실패를 빈 결과로 삼키면 save를 생략하는 게 아니라 **직전 양호값을 DELETE로 파괴**한다(wrong<missing 위반, 박제보다 은밀 — 소멸이라 토스트도 없음). 근본 신호는 **fetch 성공 여부**: fetch 함수(`_dividend_history`)가 예외를 `[]`로 삼키지 말고 *전파*해, 호출측(배치 루프)이 실패 시 replace를 통째 스킵하고 직전 값을 보존하게 하라. genuine-empty(fetch 성공·무데이터)만 clear. + replace는 delete+insert를 **단일 트랜잭션**으로(중단 시 부분/빈 상태 방지). 구현자 셀프리뷰가 놓친 이 데이터손실 버그를 적대적 리뷰가 포착(task#160 #2).

- **⭐ 빈 결과 가드의 기본형은 "저장 직전 한 지점"이 아니라 *소스-폴백*이다 — 그리고 실패 클래스는 3종이다(task#242·#243·#244)**: 위 세 가토(박제 금지·요청경로판·delete-rewrite판)를 19개 저장 지점에 전수 적용해 보니, **안전한 곳과 취약한 곳이 가드의 *위치*로 갈렸다.** ① **안전한 형태(소스-폴백)** — `fx._fetch_fx`는 fetch 실패 시 `stored_history`를 담아 *반환*하고(`fx.py:36-40`), `cache._merge_history(prev, [])`는 **prev를 그대로 반환**하며(`cache.py:69-72`), `dividends`는 `fetch_dividend_schedule(...)`를 `replace_schedule` **진입 전에** 평가한다(`dividends.py:388-392`). 빈 결과가 각 필드에 도달하기 전에 이미 직전값으로 채워져 있으니, 마지막 `_mc_save`가 무엇을 쓰든 잃을 게 없다 — 가드가 *구조적*이다. ② **취약했던 형태(끝 가드)** — `exports`·`commodities`·`earnings`·`kr_sector`는 판정을 저장 직전 한 지점에 뒀고, 그래서 그 한 지점이 놓친 실패 클래스로 전부 새어나갔다. → **신규 증분 저장을 짤 땐 last-good을 fetch 계층에 실어 소스에서 폴백시켜라.** 끝 가드를 쓸 수밖에 없으면 **실패 클래스 3종을 모두** 물어야 한다: **(a) 예외**(try/except) **(b) 성공-but-빈응답**(`rt_cd=0`·200 with 0 items — 예외 가드를 그냥 통과한다. `exports`가 `all_months`를 `total>0`으로 만들어 200/0건에 `{"months": []}`를 반환한 게 실례) **(c) 부분 페이로드**(한 페이로드의 *일부 필드*만 가드 — `kr_sector`가 `sectors`만 보고 같은 payload의 `index`를 빠뜨려 `build_sector_index()=={}`가 보유→업종 매핑을 지웠다. 대응은 `index = build_sector_index() or load_sector_index()`처럼 필드별 직전값 보존). 부수 규칙: 저장 스킵 시 **admin 응답·로그가 "갱신됨"과 "생략·직전값 유지"를 구분**해야 관측이 성립한다(`job_runs`는 본문이 예외를 전파할 때만 `failed`라 스킵을 초록으로 기록한다 — 그 docstring이 이미 경고한 성질). ③ **실패 클래스가 3종이면 *처방*도 3종이다 — 집합의 성격으로 갈린다(task#261, 7차 버그헌트가 양쪽 절반을 확증)**: ⓐ **고정 명명 집합의 합**(M7 7종목·KR Top2 2종목처럼 비중 차트의 분자)은 한 종목만 빠져도 합계가 *정의상* 틀리므로 **완전성**을 요구한다(`m7_ok < len(M7)` → 저장 생략). ⓑ **유동적 대규모 집합**(rest ~490종목)은 완전성이 비현실적이니 **커버리지 임계**(`_REST_MIN_COVERAGE = 0.5`). ⓒ **독립 항목**(원자재 심볼·업종처럼 서로 합산되지 않는 것)은 **실패분만 개별 백필**. ⚠️ **`if not X:` all-or-nothing 게이트는 이 셋 중 어디에도 해당하지 않는다** — 유동 집합에 그것만 걸면 절반이 조용히 소실된다(7차 H1: `save_guru_managers`가 83명 중 40명을 "비어있지 않음"으로 통과시켜 43명 삭제, 게다가 화면은 성공 수가 아닌 *시도 총계*를 초록으로 표시). 임계 선택의 실무 비용도 계산할 것 — 티커당 실패율 2%면 고정 7종목은 `0.98^7 = 86.8%` 통과(연 52회 중 ~7회 스킵)인 반면, all-fail만 막으면 `P(7개 전부 실패) ≈ 0`이라 가드가 **사실상 발동하지 않고** 1~6개 실패(~13%)마다 축소된 합이 저장된다. ④ **"동형 이식"은 순서까지 같다는 뜻이 아니다 — 참조 구현으로 지목한 형제가 *올바른지* 먼저 확인하라(task#261 D2, 7차 L1이 잔존 확인)**: `commodities.get_treasury()`는 **백필 → 전량실패 판정** 순서라, 저장값이 있으면 전 심볼이 백필돼 `if not rates:`가 **영원히 발동하지 않는다**(경보 미발화 + `fetched_at`만 갱신). task#261 계획이 이걸 "동형 이식" 참조로 지목했는데 그대로 베꼈으면 완료기준이 원리적으로 달성 불가였다(착수 시 ⑥ 역산으로 사전 차단해 판정을 백필 *앞*으로 옮겼고, 형제 `get_commodities()`는 그래서 옳은 순서다). **판정은 백필 *전* raw fetch 결과로** — `if not any(results.values())`.

- **⭐ `wrong<missing`은 "틀린 값 vs 없는 값"의 선택 규칙이지 *면제부*가 아니다 — "과보수적일 뿐 틀리지 않다"는 자기진단과 "지금 실측 데이터엔 해당 없음"이라는 면제엔 둘 다 유효기간이 있다(task#248→#249)**: 위 가드 3부작이 `wrong<missing`을 근거로 쓰는데, 그 교리를 잘못 적용해 **알고도 미룬 결함이 4일 뒤 라이브에서 터진** 사례가 있다. #248이 피어 멀티플 이상치 가드를 넣을 때 셀프 리뷰가 한계를 **자진 기록**했다: "peer가 정확히 3개면 나머지 표본이 2개라 극단 outlier가 중앙값을 끌어 **정상 행까지 결측된다**… 지표가 통째 사라지면 `computePeerPremiums`가 그 지표를 생략하므로 **wrong<missing은 유지된다**(과보수적일 뿐 틀리지 않는다)… 실측 005930은 peer 4개라 **해당 없음**… 전 peer median을 쓰면 이 케이스가 해소되지만 계획이 배제한 설계라 바꾸지 않았다. 봉인 후 후속 후보로 남긴다." → 사용자가 경쟁사 목록에서 INTC를 빼 peer가 4→3개가 되자 정확히 그 케이스가 발동해 **정상 peer(SK하이닉스 PBR 3.48)가 결측되고 PBR 비교 칩이 통째 사라졌다**(오값을 지우려던 가드가 비교 자체를 없앴다). task#249가 고친 해법은 **#248 run.md가 이미 이름까지 적어둔 "전 peer median"** 이었다. 겹친 오판 3개: ① **교리 오적용** — `wrong<missing`은 "틀린 값을 보여주기 vs 안 보여주기"의 선택 규칙이고 **"정상값을 지우기 vs 보여주기"엔 적용되지 않는다.** 지표 소멸은 규칙상 합격이어도 사용자 가치로는 손실이다 — **"틀리지 않았다"가 "괜찮다"를 뜻하지 않는다.** ② **입력 분포에 기댄 면제** — "실측엔 해당 없음"의 근거였던 peer 4개는 **사용자가 언제든 바꾸는 데이터**(경쟁사 목록)였다. ③ **해법을 아는 한계를 "후속 후보"로 강등** — 알고도 미룬 것과 몰라서 놓친 것은 다르게 다뤄야 한다. 실천: 셀프 리뷰가 한계를 찾으면 **`wrong<missing` 합격 여부가 아니라 "그 케이스에서 사용자가 무엇을 잃는가"로 심각도를 판정**하고, 트리거가 **사용자가 바꿀 수 있는 데이터**면 "실측엔 해당 없음"을 면제 근거로 쓰지 말 것. 한계를 테스트로 못박을 때는(#248이 `three_peers_is_over_conservative_known`으로 했다) **"의도된 트레이드오프"인지 "미룬 결함"인지 이름·주석에 구분해 남길 것** — #249는 그 테스트를 뒤집는 것으로 시작했다.

- **⭐ 조회 실패를 「빈 결과」로 붕괴시키면 화면이 *「없음」이라는 거짓 진술*을 한다 — 상태는 셋이고(미조회·0건·실패), 이 저장소엔 이미 선례가 있었다(task#307)**: 바로 위 `wrong<missing` 가족의 **프론트 3상태판**이다. 저쪽이 "정상값을 지우는 것도 손실"이었다면 이쪽은 **"모르는 것을 안다고 말하는 것"**이다. 신규 훅 `useTechIndex`가 부가 인덱스(`GET /api/tech-reports/index`) 조회 실패를 `[]`로 반환하게 짰더니 `ready=true · index=[]`가 되어 포트폴리오 「기술 노출」 카드가 **「보유 종목과 겹치는 기술이 없습니다」**를 렌더했다. 그건 사실이 아니라 *물어보지 못한 것*인데, 사용자는 "내 포트폴리오엔 기술 노출이 없다"는 **투자 판단에 쓰이는 사실**로 읽는다(빈 상태 문구는 단정문이라 특히 위험하다). ⚠️ **`ready`(로딩 여부) 하나로는 부족하다** — 빈 배열의 의미가 **셋**(아직 안 옴 · 받았는데 0건 · 조회 실패)이라 `failed`를 따로 노출해야 소비처가 「카드를 아예 안 그림」과 「빈 상태 안내를 그림」을 가른다(수정: 실패는 `null` 반환 + `failed` 플래그, 소비처는 실패 시 카드 자체를 숨김). 실패는 **캐시하지도 말 것** — 다음 마운트가 재시도할 수 있어야 한다. **가장 아픈 부분은 선례가 이미 있었다는 것이다**: `useTrackedStocks`가 보유·관심·미추적 3값에 **「모름(unknown)」 4번째 값**을 두고, 그 docstring이 "**모름이면 액션을 제시하지 않는다**(`wrong<missing`의 어포던스 판)"와 "조회 성공 + 빈 결과는 **미추적**이다 — 빈 결과는 사실이다"까지 적어 두었다. 규율은 문서화돼 있었고 같은 사이클에서 그 파일을 열어 보기까지 했는데 **새 훅에서 재현하지 못했다**(task#297·#306의 "읽은 상태에서 놓쳤다"와 같은 계열 — 형제의 규율은 *목록으로 적어 하나씩 지워야* 옮겨진다). 실천: **비동기로 채워지는 목록을 소비해 「없음」류 문구를 렌더하는 화면**을 만들 때는, 그 빈 값이 *사실*인지 *미확인*인지를 **훅 반환값에서 구별할 수 있는지 먼저 확인**할 것 — 구별할 수 없으면 그 화면은 구조적으로 실패를 사실로 말하게 된다. **자동 게이트는 이 클래스에 원리적으로 블라인드하다**(타입도 맞고 렌더도 정상이며, 실패 케이스 테스트를 안 짜면 아무것도 안 깨진다). 부수 — 이번엔 그 결함이 「새 프론트 ↔ 옛 백엔드」 창(엔드포인트 미배포로 인덱스가 404)에서 **우연히** 드러났다. 그건 방법이 아니라 운이므로, 실패 경로는 **테스트로 못박을 것**(여기선 조회 실패 시 ⓐ 카드 미렌더 ⓑ 헤더 본문은 계속 렌더 두 축).

- **남이 박아둔 테스트 축과 충돌할 때의 절차 — 뒤집기 전에 "그것이 *기록된 결정*인가"를 판별하라(task#264)**: 바로 위 #249 항목이 *내가* 판정축을 바꿔 **통과하는 테스트를 거짓으로 만드는** 이야기였다면, 이건 그 짝 — **기존 테스트가 내 변경과 정반대를 단언하고 있을 때** 무엇을 해야 하는가다. 두 갈래뿐이다: ⓐ **기록된 결정**이면 뒤집지 말고 **fork wall**(사람의 몫) ⓑ **부수적 단언**(그 태스크가 "성공 경로를 잘못 타지 않았다"를 확인하려 곁들인 것)이면 **뒤집되 *왜* 뒤집는지를 테스트 주석에 남긴다**. **판별법은 그 테스트를 만든 태스크의 계획서를 읽는 것** — 완료기준·비목표에 그 축이 *이름으로* 등장하면 결정이고, 없으면 부수적이다. 실사례(task#264): OAuth 실패 되감기를 붙이는데 `auth-bootstrap.test.jsx` ③이 `expect(returnFromOAuth).not.toHaveBeenCalled()`로 **정확히 그 축**을 단언 중이었다. task#253 `plan.md:59`의 완료기준은 "정상 성공 경로 **동작 보존**"만 말하고 비목표도 성공 경로만 다뤄 **실패 경로 되감기는 요구도 금지도 아니었으므로** 뒤집는 것이 옳았다(6차 버그헌트 리포트도 "실패 경로 되감기는 어느 문서에도 논의·결정된 바 없음"을 독립 확인). 이 판별을 건너뛰면 **결정을 조용히 되돌리거나**(ⓐ를 ⓑ로 오판) **정당한 수정을 스스로 막는다**(ⓑ를 ⓐ로 오판). 계획이 "기존 N케이스는 손대지 않는다"고 적어 뒀어도 그건 *예측*이지 사실이 아니니, 착수 시 바꾸려는 축을 단언하는 기존 테스트를 먼저 grep할 것. **⭐ 그리고 세 번째 분기가 있다 — 뒤집기가 *정당해도* 「그 단언이 그 성질의 유일한 증거였는지」를 따로 물어라(task#336)**: `test_consume_refresh_token_is_one_time`의 `mock_exec.assert_called_once()`를 `assert_not_called()`로 뒤집은 것은 위 절차(ⓑ 부수적 단언)로 **정당하게 판정됐다** — task#108 계획서는 「회전 시 옛 토큰 row 삭제(최소 수정)」만 결정하고 구현 형태를 두 가지로 열어 뒀으므로 *SELECT/DELETE 2문 분리*는 결정이 아니었다. **그런데 그 뒤집기가 「DELETE가 발행됨」의 유일한 증거를 함께 지웠다**: 그 테스트의 `fake_query(sql, params)`는 **`sql`을 무시**하고 호출 횟수만으로 rows/`[]`를 돌려주므로, 구현이 삭제를 전혀 하지 않는 평범한 `SELECT`로 되돌아가도 **세 단언이 전부 통과**한다 — 이름이 `..._is_one_time`인 보안 테스트가 1회용을 더 이상 검증하지 않는 상태가 된다. 처방: 단언을 지우거나 뒤집을 때 **「이 단언이 없으면 무엇이 검증되지 않는가」를 한 줄 적고, 그 성질을 지키는 대체 단언을 같은 커밋에 넣을 것**(여기서는 `query`에 넘어간 SQL에 `DELETE`·`RETURNING`이 있음을 단언하고, 옛 `SELECT`를 주입해 실제로 FAIL하는 것을 확인한 뒤 원복했다). ⚠️ **적대 검토도 이것을 못 잡았다** — 검토는 「뒤집어도 되는가」를 판정했고 「뒤집은 뒤 무엇이 남는가」는 묻지 않았다.

- **일일 리포트 배치 시장별 분리**: 단일 `daily_report`가 `daily_report_kr`(기본 20:30 KST, NXT 마감 이후)·`daily_report_us`(기본 07:00 KST, 겨울 DST 안전)로 분리됨. 각 배치는 자기 시장 종목만 생성(파티션: KR=`market=="KR"`, US=`market!="KR"`로 비-KR 전부 US). "미생성" 판정은 시장별·시각인지 기대날짜(`storage.expected_report_date(market)` / `expected_report_dates()`)로 계산. `GET /api/report/list`의 `last_scheduled_date`는 단일 문자열→객체 `{"KR":..,"US":..}`로 형태 교체(외부 Cowork 파싱 영향). 근거: ADR-0012.

- **배치 시장 분류 + 실적/월간 시장별 분리**: `batch_registry.BATCHES`의 모든 배치가 `market` 속성(`KR`/`US`/`공통`)을 가지며 `GET /api/batches` 응답에 그대로 노출된다. 분류는 출처국 기준이라 FRED 경제지표(`monthly_us`)는 해외(US)로 둔다. 실적·월간 지표도 daily_report처럼 시장 분리: `earnings_kr`(KR Top2)/`earnings_us`(M7), `monthly_kr`(KR 수출)/`monthly_us`(FRED 경제지표). 수동 갱신은 `POST /api/market/refresh-earnings?market=KR|US`·`refresh-monthly?market=KR|US`로 호출하면 해당 시장만 갱신하고 시장별 id로 `job_runs.record`한다(고아 `refresh-econ`은 `monthly_us`로 흡수). 근거: ADR-0013.

- **배치의 fetch 소스를 바꾸면(예: KR 랭킹 Naver→키움) `batch_registry`의 그 배치 `source`도 갱신할 것(DoD) — 안 하면 배치 현황이 틀린 출처를 표시한다. `source`=데이터 fetch 출처(예: `["키움","KIS","Naver"]`)이지 `usage`(소비 UI, 예: `["리포트 탭"]`)와 반대 방향**: `source`는 배치가 데이터를 어디서 끌어오는지, `usage`는 그 데이터를 어디서 쓰는지다. `GET /api/batches`가 둘 다 그대로 노출하므로 코드의 fetch 체인만 바꾸고 `source`를 안 고치면 현황 카드가 stale 출처를 보인다.

- **⭐ `job_runs.record`는 *예외 전파*를 전제한다 — 실패를 삼키고 dict를 반환하는 fetch와 짝지으면 배치현황이 영원히 초록이다(task#293, 적대적 검토 HIGH)**: 위 「빈 결과 가드는 소스-폴백으로」가 *데이터를 지키는* 이야기라면 이건 그 짝 — **가드가 작동한 사실을 관측 가능하게 만드는** 이야기다. 소스-폴백 가드를 제대로 넣으면 `_fetch_and_save_*`는 **설계상 절대 raise하지 않는다**(예외·빈응답·부분실패를 전부 내부에서 삼키고 직전값을 반환한다). 그런데 `with job_runs.record(id, trigger):`는 **본문이 예외를 전파할 때만** `failed`를 기록하므로, 그 둘을 그대로 붙이면 **FRED가 며칠 죽어 전 계열이 실패해도 매 실행이 `success`로 기록**되고 배치현황은 계속 초록이다 — `market_cache`가 무기한 stale해져도 아무도 모른다. 게다가 그 조합은 **형제 배치를 복사하면 자동으로 재생산된다**(task#293이 `_refresh_macro_signals`를 그대로 베껴 이 결함을 그대로 물려받았다). 대응: ⓐ `_fetch_and_save_*`가 실패 상태를 **반환값에 실어**(`_status`: `"partial"`/`"skipped"`) 노출하고 — ⚠️ 그 메타는 **`_mc_save` *뒤에* 새 dict로** 붙일 것(`{**merged, "_status": ...}`; `merged`를 mutate하면 저장 캐시가 오염된다) — ⓑ auto(`scheduler/jobs.py`)·manual(라우터) **두 레인 모두** `with job_runs.record(...) as run:`으로 받아 `run.set_status("skipped"|"partial"|"failed", ...)`를 명시하며 ⓒ admin 응답도 `ok = (status == "success")`로 **「갱신됨」과 「생략」을 구분**해 반환한다(안 하면 API 키 미설정이 `0/0`으로, 전량 실패가 직전 성공 카운트로 보여 둘 다 성공으로 오인된다). 참조 구현 3쌍: `formation.py`·`labor.py`·`inflation.py` + 각각의 `jobs.py`·`routers/market_indicators.py` 짝(task#293~295). **⚠️ 미해소 부채 — 같은 결함이 형제 잡 ~14개에 그대로 남아 있다**(`job_runs.py` docstring이 set_status 배선 *예외* 목록을 수동 나열하는 것이 그 증거다). 새 배치를 만들 때 그 목록에 없는 형제를 템플릿으로 고르면 결함을 복제하게 되니, **템플릿은 위 3쌍 중에서 고를 것.**

- **⭐ 비동기 잡의 *진행* 판정에 출력 파일 크기를 쓰지 말 것 — `claude -p`는 완료 시 1회 출력하므로 0바이트 로그가 「죽음」과 「정상 진행」을 동시에 의미한다(task#300)**: 바로 위 `job_runs.record` 가토가 **거짓 초록**(실패가 `success`로 기록)이었다면 이건 그 거울상인 **거짓 빨강**이다 — 정상 진행을 실패로 오진한다. 실측: 루틴 fire(`POST /api/admin/cowork/fire`) 후 `~/portfolion-routine-runs/<ts>/run.log`가 **10분간 0바이트**였고 DB에도 행이 없었다. `scripts/cowork-fire-listener.py`가 `claude -p --model opus`를 spawn하며 stdout/stderr를 그 파일로 리다이렉트하는데, **`claude -p`는 스트리밍이 아니라 완료 시 1회 출력**하므로 진행 중 로그는 **원리적으로 비어 있다**(실제로는 정상 동작 중이었고 15분 뒤 발행됐다). **여기서 재fire하면 같은 slug에 중복 발행을 쏜다** — 계획의 실패 처리(「N회 시도로도 갱신되지 않으면 재시도」)를 *로그 크기*로 트리거하면 그 재시도가 곧 부작용이다(프로덕션 쓰기 경로에서는 특히). 판정을 3분할할 것: **생존 = 프로세스**(`ps aux | grep "[c]laude -p"`) · **완료 = 결과 상태**(DB 행·`published_date`) · **실패 원인 = 로그**(단 *끝난 뒤에만* 읽을 수 있다). 갈림점은 **직전 런과의 대조**였다 — 20:42 런이 4782바이트 로그를 남기고 19분에 끝난 기록이 있어 「메커니즘 자체는 동작한다」가 먼저 확정됐고, 그래서 0바이트를 실패로 귀속하지 않았다(가토 ⑧ⓔ 「대조군 없이는 *앱이 안 그런다*와 *계측기가 못 본다*가 구별되지 않는다」의 **로그판**). 일반화: **버퍼링되는 출력은 진행 신호가 아니다** — `claude -p`·`subprocess` 파이프·리다이렉트된 stdout을 폴링 루프의 진행률로 쓸 때 크기 0을 실패 조건에 넣지 말 것. 참고로 이 경로의 *발사* 실패는 조용하지 않다(`routers/admin.py::cowork_fire`가 미설정 503 · `fire()` 실패 502를 던지므로 `ok=False`가 200으로 새지 않는다) — 즉 200을 받았으면 발사는 됐고, 그 뒤 판정은 위 3분할로 해야 한다.

- **배치 id를 `batch_registry.BATCHES`에서 빼면 그 id를 쓰는 *모든* 표면을 전수 grep**: ① 데이터 read(스케줄 소비처), ② 표시 문자열(`schedule_desc` 등), ③ **`job_runs.record(id, ...)` 모든 lane — auto뿐 아니라 manual·backfill 경로까지**, ④ 그 id를 단언하는 테스트. 한 곳이라도 옛 id로 남으면 stale read(과거)·**배치 현황 실행이력에서 사라지는 회귀**(이번: 수동생성/백필이 은퇴한 `daily_report`로 record → 카드에서 증발, daily_report-market-split task15·17·45 재발)·고아 run 누적이 생긴다. 특히 옛 id를 단언하던 테스트는 깨진 동작을 고정해 TDD green이 회귀를 못 잡으니(이번엔 적대적 리뷰가 포착), id 은퇴 시 테스트도 grep 대상. 단, 옛 id를 *읽는* 시드 마이그레이션(옛 스케줄 행→신규 id 승계)은 정당한 잔존이다 — "잔존 0"은 stale *consumption*(소비처가 옛 id로 read/record) 기준이지, 옛 store를 읽어 신규 id로 옮기는 마이그레이션 read는 청소하면 스케줄 승계가 깨진다(task46 earnings/monthly 분리). **id를 *추가*할 때도 테스트의 count/set 하드코딩 단언을 전수 grep**(`grep -rn "BATCHES) ==\|len(data) ==\|EXPECTED_IDS" backend/tests/`) — exact-count/exact-set 단언이 4파일(test_scheduler_seed·test_batch_market_split·test_batches_router·test_macro_signals_batch)에 흩어져 있어 한 파일만 고치면 나머지가 배포 전 스위트에서 깨진다(us_sector_fetch 추가 시 계획은 1파일만 인지 — 적대 리뷰·verify 전체 스위트가 나머지 3파일 포착, task#136). ⚠️ **그런데 그 grep 패턴 자체가 갱신 지점의 절반을 원리적으로 못 본다(task#293·294·295 3연속 실측 정정)**: 파일 목록은 맞지만 **파일 *안*의 지점**은 매번 **8곳**이었고 패턴이 잡는 건 그중 4곳뿐이다. 못 잡는 4곳 — ⓐ `test_scheduler_seed.py::test_all_editable_jobs`의 `set(...) ==` ⓑ `test_scheduler_seed.py::test_seed_only_fills_missing_rows`의 `expected_seeded` `set(...) ==` ⓒ `test_batch_market_split.py`의 **`_MARKET_BY_ID`**(id→market 완전 매핑 dict) ⓓ 같은 파일의 **시장별 개수 dict**(`{"KR":16,"US":11,"공통":5}` — 새 배치의 market에 해당하는 값을 +1 해야 한다). 패턴이 `BATCHES) ==`·`len(data) ==`·`EXPECTED_IDS` 세 리터럴만 보므로 **`set(...) ==` 형태와 dict 리터럴에 블라인드**하다. 이건 인증 문서 가토의 「감사 패턴을 좁히면 그 감사는 통과해도 무의미하다」의 **배치 id판**이고, 실제 게이트는 grep이 아니라 **전체 스위트**다(293에서 실행 중 4곳을 발견해야 했고, 그 목록을 294·295 계획에 *지시로* 주입하니 누락 0이 됐다). 실천: grep은 "어느 파일을 볼지"만 좁히는 데 쓰고, **그 4파일을 열어 직접 읽을 것**.

- **모듈에서 심볼(import·함수)을 제거/개명하면 그 심볼을 patch하는 테스트를 *파일 불문* 전수 grep**(`grep -rn "모듈경로.심볼" backend/tests/`) — mock 타깃은 "그 기능의 주 테스트 파일"에만 있지 않다. digest_service에서 `yf` import를 제거하자 주 테스트(test_digest_service)는 마이그레이션됐지만 **다른 파일**(test_disclosure_endpoint_digest)이 `services.digest_service.yf.Ticker`를 patch하고 있어 `ModuleNotFoundError`로 파손(task#136). 위 mock 타깃 이동 가토(quote-fetch-perf·task#135)의 *제거판* — 호출처 변경뿐 아니라 심볼 소멸도 patch 경로를 깨뜨린다.

- **컨텍스트매니저의 *yield 값*을 바꿀 땐 `as` 바인딩 grep만으로 부족하다 — 진짜 파장은 "가짜 CM으로 그 함수를 통째 대체한 테스트 헬퍼"에 있다(task#274)**: 위 두 가토(#135 호출처 이동·#136 심볼 소멸)가 patch *경로*가 깨지는 경우였다면, 이건 **대체 구현의 계약**이 깨지는 경우다. `job_runs.record`가 `run_id`(int) 대신 상태 지정 핸들을 yield하도록 바꾸자 전체 스위트에서 8건이 깨졌는데, **`as run_id` 바인딩은 2건뿐**이었고 나머지 대부분은 `@contextmanager def fake_record(job_id, trigger): yield 1` 형태로 그 함수를 monkeypatch하던 **헬퍼 2곳**(`test_guru_router._stub_job_runs`·`test_job_runs_instrumentation.spy`)이 만든 것이다 — 그 2곳이 테스트 8건을 파손했다. 즉 **계약 변경의 영향 범위를 `as` 바인딩 수로 추정하면 크게 빗나간다**(계획이 "호출부 57곳 전부 `as` 없음 → 하위호환"이라 적었는데 실제는 89곳이었고, `as`가 없다는 사실 자체는 안전을 보장하지 않았다). 탐지: `grep -rn "yield 1\|yield None" backend/tests/` + `grep -rn "monkeypatch.setattr(<모듈>, \"<함수>\"" backend/tests/`. 그리고 **SQL 리터럴을 단언하던 테스트도 함께 깨진다** — 상태를 SQL 텍스트에서 파라미터로 옮기면 `assert "success" in sql`이 원리적으로 실패하니, 단언을 `call_args[0][1]`(params)로 옮길 것(그게 더 정확한 단언이기도 하다). 절차는 기존대로 **의존성을 바꾼 뒤 전체 스위트를 먼저 돌리고 실제로 깨지는 것만** 고친다(선제 grep은 "어디를 볼지"를 좁히는 용도).

- **문서 갱신 슬라이스를 "필드 N개 추가"로 잡으면 그 절이 *이미* stale한지는 안 본다 — 착수 시 그 절 전체를 실응답과 대조할 것(task#274)**: 위 인증 문서 가토의 「감사 패턴을 좁히면 그 감사는 통과해도 무의미하다」와 같은 가족이되, 이쪽은 *감사*가 아니라 **갱신 범위**가 좁은 경우다. task#274 계획은 `API_SPEC.md`의 `GET /api/guru/crawl/progress` 응답에 `dropped`·`held` **추가**만 지시했는데, 실제 문서는 그보다 두 세대 뒤처져 `result`/`fresh`/`stale`(task#262·#267에 추가된 필드)조차 없이 `{running, done, total, current}`만 적고 있었다 — **지시받은 두 필드만 넣었으면 여전히 틀린 문서**가 됐을 자리다(실제로는 응답 전체를 필드 표로 다시 썼다). `test_api_doc_sync.py`는 엔드포인트 *존재*만 대조하므로 이런 **응답 스키마 drift는 스위트가 원리적으로 못 잡는다**. 실천: 문서 슬라이스에 착수하면 고칠 필드만 보지 말고 **그 절의 예시 응답을 실제 코드가 반환하는 것과 한 번 대조**할 것(엔드포인트 1개면 1콜, 아니면 반환 dict 직독). 부수 — 그 절에 `done`/`total`처럼 **의미를 오해하기 쉬운 필드**가 있으면 표에 함정을 명시할 것(여기선 "저장 건수가 아니라 **시도** 기준" — BH7-H1이 정확히 그 오해로 났던 버그다).

- **코드에 줄을 추가·삭제하면 그 파일을 *줄번호로 참조하는* 문서가 조용히 stale해진다 — 시프트는 균일하지 않으니 산술 추정 금지, `git show HEAD:<file>`로 옛 줄을 읽어 **의미로** 확정할 것(task#278)**: 위 task#274 가토가 「고칠 필드만 보면 그 절이 이미 stale한지 못 본다」였다면, 이건 **내가 *손대지도 않은* 인접 절을 내 변경이 깨뜨리는** 경우다. `.forge/codebase/CONCERNS.md`는 `file.py:NNN` 참조로 뒤덮여 있어(§3.5·3.6만 해도 여러 곳) 상시 위험이다. 실측 — task#278이 `routers/portfolio.py`에 줄을 넣자(`@@ -37,0 +38,2` 이후 +2, `@@ -148 +150,3` 이후 **추가** +2) 같은 문서의 손대지 않은 절 참조 **4곳**이 어긋났다: `164→168`(`sanitize(get_rebalance)`) · `208→212`(`sanitize(get_exposure)`) · `205→209`(`beta_map` float 캐스트) · `108→110`(`amt * qty`) + 부수로 주석 `73-74→75-76`. ⚠️ **시프트가 부위별로 달라(+2/+4) 산술로 추정하면 틀린다** — 반드시 `git show HEAD:<file>`로 옛 줄을 읽어 그 참조가 *무엇을* 가리켰는지 확정하고 그 코드의 현재 위치를 찾을 것. **스위트는 이 클래스에 원리적으로 블라인드**하다(줄번호는 아무도 단언하지 않는다). 그리고 **적대적 리뷰어가 준 줄번호도 검증 대상이다** — task#278 리뷰어는 4곳 중 3곳만 찾았고 `:205`를 211로 적었는데, 211은 `compute_exposure` **호출**이고 그 문장이 주장하는 것은 "밖에서 캐스트한다"이므로 **캐스트가 있는 209가 맞다**(형태가 그럴듯해서 그대로 믿기 쉽다). 실천: 코드 줄 수를 바꾼 슬라이스는 완료 전에 `grep -n '<파일명>:[0-9]' .forge/codebase/*.md`로 그 파일을 참조하는 문서 줄을 전수 확인할 것. **⭐ 그 실천을 *계획의 완료기준에 명시*했더니 실제로 지켜졌다 — 3번째 미준수를 막은 양성 사례(task#285)**: task#284 회고가 "배경지식이 아니라 완료기준으로 옮겨라"라고 적었고, task#285 계획서가 S6의 완료기준에 이 grep 한 줄을 그대로 박았더니 **보정 20건**이 실행됐다(2연속 미준수 뒤 첫 준수). 가토 ⑥("회고에 남긴 제약이 *지시*로 옮겨지지 않으면 무력하다")의 양성 사례이며, 「프론트 빌드 금지」·「서브에이전트 git 되돌리기 금지」와 같은 패턴이다 — **교훈을 재발 방지로 바꾸는 것은 문서화가 아니라 *그 사이클의 완료기준에 넣는 것*이다.** 부수 발견: 이때 **선재 drift**(내 변경과 무관하게 이미 어긋나 있던 참조)가 9건 드러났는데, HEAD 기준으로도 내용이 안 맞아 "무엇을 가리켰는지" 확정 자체가 불가능했다 — 시프트 보정과 선재 오류는 **별개 축**이니 후자는 `fg-map` 재생성으로 처리할 것(개별 추적은 비용만 든다). **⭐ 그 재생성이 실제로 일어났고, 이 결함 클래스를 구조적으로 없앴다(task#290 실측)**: `ed98f72`(fg-map 전체 Refresh) 이후의 `.forge/codebase/*.md`는 코드 위치를 **`파일::심볼`과 `§N` 섹션 앵커로만** 가리키고 `파일:줄번호` 포인터를 **프로젝트 전역에서 0건** 쓴다. task#290이 9개 파일의 줄 수를 바꾸고 전수 확인했더니 **보정 0 · 표본 3 · 빈 표본 9 · 선재 drift 0**이었다(표본 3은 포인터가 아니라 "`get_dashboard` — 173줄"류 *길이* 서술이었고 셋 다 실측과 일치). 함의 2가지: ⓐ **이 grep은 계속 돌리되 0건이 정상 기대값**이다(완료기준에서 빼지 말 것 — 값싸고, 0이라는 사실 자체가 맵이 아직 앵커 규율을 지키고 있다는 증거다) ⓑ **맵을 재생성할 때 `파일:줄번호` 포인터를 되살리지 말 것** — 심볼·섹션 앵커는 줄 수 변화에 면역이라 이 클래스가 다시 생기지 않는다. **⭐ 그런데 task#285의 「완료기준에 넣어라」는 *필요조건이지 충분조건이 아니다* — 실효한 것들의 공통점은 「완료기준에 있었다」가 아니라 **「한 줄 명령이었다」**다(task#325 실측)**: task#325 계획은 9차 회고의 「다음에 다르게」 6항목을 완료기준으로 성실히 이식했는데(가토 ⑥의 처방을 그대로 따랐다) **그중 「배정 목록과 「볼 것」을 1:1 대조해 이유 없는 배정 0」은 수행되지 않았다** — 결과로 A4에 배정된 「신규·변경 테스트 43」이 볼 이유를 못 받아 **테스트 24개가 미열람**으로 남았다(A 커버리지 82/130 = 63%). 9차 학습 2의 **직접 재발**이고, 계획이 그것을 완료기준으로 옮겼음에도 재발했다. 갈림은 형태였다 — task#285·#290에서 실효한 것은 전부 `grep -n '<파일명>:[0-9]' .forge/codebase/*.md` 같은 **그 자리에서 실행 가능한 한 줄**이었고, 실패한 것은 「~를 1:1 대조한다」처럼 **사람의 *행위*를 요구하는 서술**이었다. 행위형 완료기준은 그것을 수행하는 **슬라이스가 없으면 선언으로 남는다**(체크박스는 있는데 아무 명령도 그것을 강제하지 않는다). **실천: 완료기준을 쓸 때 「이것을 어떻게 판정하는가」를 물어 ⓐ 실행 가능한 명령으로 환원되면 그 명령을 그대로 적고 ⓑ 환원되지 않으면 *별도 슬라이스*로 승격할 것.** 둘 다 아니면 그 완료기준은 지켜지지 않는다고 가정하라. (같은 사이클에서 이 항목의 grep 처방 자체는 **실효했다** — task#325가 `CONCERNS §0`에 새 행을 쓰며 `파일:줄번호` 포인터 **7건**을 만들어 baseline 0을 깨뜨렸고 그 한 줄이 즉시 잡아냈다. ⚠️ **그리고 그 수정에 두 번째 층이 있었다** — 7건을 고친 뒤 그 grep은 0을 냈지만 `(:42)`·`:64` 같은 **bare `:NNN` 6건**이 남아 있었고 그 형태는 같은 이유로 드리프트하는데 **그 grep이 원리적으로 못 본다**. `git show HEAD:<file>`로 재보니 bare 형태도 baseline이 0이었다 → **감사 baseline은 「0이면 통과」가 아니라 「HEAD와 같으면 통과」로 쓸 것** — 전자는 내가 무엇을 늘렸는지를 드러내지 못한다. 「감사 패턴을 좁히면 그 감사는 통과해도 무의미하다」의 자기적용 사례다.)

- **⭐ 블라인드는 「그 문서를 못 읽게 하는 것」이 아니라 「그 내용이 *어떤 경로로도* 닿지 않게 하는 것」이다 — 그리고 그것은 「배정 파일을 볼 *이유*를 주기」와 **직접 충돌하는 목표**다(task#325)**: 서브에이전트·렌즈가 기존 결함 카탈로그를 **모른 채** 같은 코드를 훑게 해서 「독립 재발견율」을 재는 설계는 매력적이다(중복 배정보다 값싸고, 통과하면 강한 신호다). task#325가 그것을 했다 — A·B 렌즈에게 `CONCERNS §0`(열린 29건)을 숨기고 C 레인이 그 29건을 판정하게 해, 「렌즈가 §0을 모르고 같은 결함을 다시 집는가」를 측정하려 했다. **프롬프트 차원의 블라인드는 완벽하게 작동했다** — 미열람을 못박았고 렌즈들이 실제로 지켜 `scanned`에 「§0[33-98] 의도적으로 미열람」을 기록했다. **그런데 계획서의 「볼 것」 자체가 §0 항목 2건을 실질적으로 복창하고 있었다**: `§0 B9`(「프론트에 access token 갱신 경로가 없다 — 백엔드 `/api/auth/refresh`는 존재하는데 아무도 안 부른다」)가 인증 렌즈의 「볼 것」에 거의 그대로 있었고, `§0 B62`(「`_table_unit`의 억원 기본값 폴백 — ×100 오저장」)도 파서 렌즈의 「볼 것」에 있었다. **즉 파일은 숨겼는데 내용이 지시문을 타고 들어갔고**, 그래서 그 측정(독립 재발견 0/18)은 **무효**다. **왜 이것이 우연한 실수가 아닌가** — 「볼 것」을 풍부하게 쓰라는 요구(= 배정만 하고 볼 이유를 안 주면 그 파일은 사각으로 남는다)와 블라인드는 **재료가 같다**. 「볼 것」의 재료는 결국 *이미 아는 결함 목록*에서 오기 때문이다. 그래서 둘을 동시에 요구하면 **원리적으로 하나가 깨진다**. **실천 3가지**: ⓐ 블라인드를 재려면 「볼 것」을 결함 **카탈로그**가 아니라 가토의 결함 **클래스**에서만 파생시킬 것(클래스는 일반형이라 특정 항목을 지목하지 않으므로 둘을 동시에 만족시킨다 — 「빈 결과 3클래스를 각 저장 경로마다 물어라」는 되고 「`§0 B43`의 US 섹터 부분 페이로드를 봐라」는 안 된다) ⓑ 블라인드 성립 여부를 **완료기준에 넣고 *지시문 쪽*에서 검증**할 것(「에이전트가 그 파일을 읽었나」가 아니라 「내 프롬프트에 그 내용이 있나」 — 전자만 보면 이번처럼 통과한다) ⓒ **깨졌으면 그 지표를 무효로 표시하고 결론에 쓰지 말 것**(고쳐서 덮지 말고 깨졌다는 사실을 남긴다). 부수 — 이 저장소에서 **렌즈 간 수렴은 6사이클 연속 ~0**이다(공유 표면 1·0·2·0·0 + 블라인드 0/18). 원인이 실측으로 확인됐다: **렌즈의 스탠스가 무엇을 볼지 결정하므로 같은 파일을 배정해도 수렴이 안 생긴다** — `routers/auth.py`를 두 레인이 함께 봤는데 한쪽은 `ALL_MENUS` 드리프트·OAuth 크래시를, 다른 쪽은 `SESSION_SECRET` 임포트타임 폴백·레이트리밋 부재를 지목해 **파일은 겹쳤고 결함은 겹치지 않았다**. → **수렴을 지표로 설계하지 말 것. 커버리지(사각의 크기)가 더 정직하다** — task#325에서 그것이 실제로 「배정만 받고 볼 이유를 못 받은 테스트 24개」를 잡아냈다.

- **테스트는 실 DB 접근이 conftest `_block_real_db` autouse 가드로 차단돼 있다 — DB를 타는 테스트는 반드시 services.db(query/execute) 또는 그 상위를 mock할 것(task#169)**: 로컬 `DATABASE_URL`이 도커 postgres(=라이브 DB, 5432 노출)를 가리켜, 가드 전엔 `generate_report` end-to-end 테스트의 스냅샷 INSERT가 **prod `snapshots`에 그대로 커밋**됐다(005930 스냅샷 11일치+가 fixture price 70000으로 클로버, admin 삭제 테스트는 prod `calendar_cache` 전체 DELETE 실행). 오염은 **선택적**이라 격리된 것처럼 보였다 — 가짜 티커(TEST)는 FK로 실패해 무해해 *보이고* 실존 티커만 오염(fixture-pass-live-fail의 *역방향*: fixture-writes-live). 라이브 값이 지나치게 라운드(70000, 정확히 400조)면 피드 글리치보다 **테스트 오염을 먼저 의심**하고, reload-패턴 테스트(`importlib.reload(report_generator)`)는 **모듈 자체 정의 심볼 patch가 reload로 무효화**되니 하위 모듈 속성(`services.db.execute`·`_naver_get` 등)을 patch할 것. 가드가 raise하면 그 테스트가 실 DB에 닿고 있다는 뜻 — 가드를 풀지 말고 mock을 추가하라. **⭐ 그리고 이 사건이 남긴 가장 일반적인 교훈 — 가드를 추가했는데 증상이 반복되면, 그 가드를 *우회하는* 경로가 있는지부터 확인하라(task#170 포렌식)**: task#101이 박제-시 독립피드 게이트를, task#118이 ref 피드 강화를 넣었는데도 "정확히 70000.0"이 계속 나타났다. 실제 원인은 **로컬 pytest가 prod DB에 직접 쓴 오염**이었고 테스트는 애초에 그 게이트를 타지 않는 **제3의 writer**였다 — 즉 두 번의 가드 추가가 증상을 못 막았다는 사실 자체가 "가드가 그 경로에 없다"는 신호였는데, 그때마다 피드 글리치 쪽으로 원인을 *더 정교하게 다듬는* 방향으로 갔다. **같은 증상에 두 번째 가드를 붙이려는 순간이 멈춰서 writer를 전수 세어볼 시점이다**(`grep`으로 그 테이블·필드에 쓰는 경로를 전부). 위 BH7-L1/L5("가드는 있는데 *앞의 코드*가 판정을 가로채 도달 불가")의 사촌 — 그쪽은 도달 불가, 이쪽은 **게이트 밖에서 쓰는 경로가 따로 있음**이다.

- **⭐ 무거운 복구 동작을 가벼운 것으로 바꾸면, 그 무거움이 *우연히 덮고 있던* 결함이 드러난다 — 제거 전에 "이게 부수적으로 무엇을 고쳐주고 있었나"를 물을 것(task#283)**: 위 가토가 「가드를 우회하는 경로」였다면 이건 반대로 **가드(혹은 아무 무거운 동작)가 *의도치 않게 수행하던 복구*를 잃는** 경우다. 전체 리로드·프로세스 재시작·캐시 전량 무효화·문서 교체처럼 **상태를 통째로 리셋하는 동작은 겨냥한 문제 하나만 고치지 않는다** — 그 시점에 존재하는 *모든* 상태 결함을 함께 리셋한다. 그래서 그것을 in-place 처리로 바꾸면 **가려져 있던 결함들이 한꺼번에 노출**되고, 증상은 대개 원래 버그보다 나쁘다. 실측: task#283이 bfcache 인증 가드의 `location.replace('/')`(전체 리로드)를 `setSession` in-place 뒤집기로 바꾸자, `useAuthBootstrap`의 OAuth 성공 분기가 **혼자만 `resolveStored()`를 부르지 않아 `authLoading`이 영원히 `true`로 남는** 기존 결함이 처음으로 문제가 됐다 — 리로드는 새 문서를 만들어 그 훅을 처음부터 다시 돌렸으므로 **결함을 매번 세탁**하고 있었고, in-place 뒤집기는 `setSession`만 하므로 `authLoading`이 안 풀려 **새로고침 전까지 빠져나올 수 없는 백지**가 된다(원래 버그는 "로그인 화면이 잠깐 보임"이었다). **어떤 자동 게이트도 이걸 못 잡는다** — vitest·라이브 프로브·빌드 전부 통과했고, **적대적 리뷰만이** 잡았다. 이유는 결함의 성질이 "코드가 틀렸다"가 아니라 "**제거한 것이 무엇을 덮고 있었나**"라서 diff를 아무리 봐도 안 보이기 때문이다. 실천: ⓐ **리셋성 동작을 제거·경량화하는 슬라이스에는 적대적 리뷰를 붙일 것**(diff 리뷰로는 원리적으로 안 보인다). ⓑ 리뷰 프롬프트에 렌즈를 명시하라 — 「예전엔 이 동작이 상태를 통째로 날렸다. **그 초기화에 의존하던 코드**가 있는가?」. ⓒ 특히 **초기화 경로에서만 세팅되는 플래그**(`authLoading`·`initialized`·`hydrated` 같은 1회성 상태)를 grep해 "리로드 없이 그 값이 풀리는 경로가 있는가"를 확인할 것. 이건 「빈 결과 가드의 기본형은 소스-폴백」 가족과 방향이 반대다 — 저쪽은 *덮어쓰기*가 데이터를 파괴하는 것이고, 이쪽은 *덮어쓰기를 그만두자* 숨어 있던 파손이 드러나는 것이다. **⭐ 그리고 이 가토에는 *역방향 짝*이 있다 — 덮개를 새로 넣을 때는 "그 덮개가 언제·누구에 의해 걷히는가"를 세라(task#285)**: 위가 「무거운 동작을 *제거*하니 그것이 덮고 있던 결함이 드러났다」였다면, 이건 **덮개를 *도입*했는데 그 덮개가 다른 계층에서 걷혀 구멍이 남는** 경우다. 실측 — task#285가 OAuth 이탈~복귀 구간을 덮으려고 `index.html`의 `#root` 안에 정적 스플래시를 넣었는데, **React `createRoot().render()`가 첫 커밋에서 `#root`의 기존 자식을 지운다**(리뷰어가 `render(null)`로 실증). 그 순간 `authLoading`은 아직 true라 `App.jsx`가 `null`을 반환해, **덮으려던 바로 그 구간(코드교환 fetch 대기, 실측 143ms+)에 blank가 다시 생겼다** — 없애려던 "화면이 바뀐다"가 문서 내부에서 그대로 재발할 자리였다. **어떤 자동 게이트도 못 잡는다**: vitest는 정적 노드→React 렌더의 *인계*를 아무도 단언하지 않고, 라이브 프로브는 그 시점에 아직 없었다(적대적 리뷰만 잡았고, auth·상태 표면에서 **3사이클 연속**이다). 일반화: **초기 화면을 정적 HTML로 칠하면 프레임워크 마운트가 그것을 지운다.** 덮개(스플래시·스켈레톤·FOUC 방지 스크립트)를 도입할 땐 "무엇을 덮는가"만 보지 말고 **① 이 덮개가 걷히는 지점이 어디인가 ② 걷힌 직후 무엇이 그려지는가**를 함께 확인할 것 — 인계가 끊기면 증상이 그 자리에서 재발한다. 이 저장소엔 이제 그 덮개가 실재하므로(`index.html` + `App.jsx`의 early-return 2개 + `LoginPage`의 `leaving` 분기), **`App.jsx`의 초기 분기를 건드리는 변경은 그 인계를 다시 확인**해야 한다. **⭐ 세 번째 형태 — *판정축 교체*판: 전역 판정을 국소 판정으로 좁히면 전역이 공짜로 막아주던 입력이 통째로 새어 나온다(task#303)**: 앞의 둘이 *동작*(리로드·덮개)이었다면 이건 **판정 알고리즘**이다. B60을 고치며 이상치 판정을 「1년 창 **중앙값** 대비 5배」(전역)에서 「고립 스파이크 — 양 이웃과 어긋나되 이웃끼리는 정합」(국소)으로 바꿨는데, 그 교체가 옛 축의 **부수 효과 두 개**를 함께 잃었다. ⓐ **비양수 점** — 옛 필터는 `median > 0`인 한 `v <= 0`을 밴드 밖으로 **항상** 배제했다. 새 판정의 비율 계산이 `a <= 0 or b <= 0`에서 「정합」을 반환하자 값이 정확히 `0`이거나 음수인 점(yfinance 결측 경로)이 **첫 점·중간 점 어디에 있어도 영영 안 걸리는 신규 사각**이 됐다. ⓑ **쓰레기 「런」** — 국소 판정의 「이웃끼리 정합」 조건은 쓰레기가 **정확히 1점일 때만** 성립한다. `[0.01, 0.01, 100.0, …]`처럼 2점 연속이면 그 둘이 서로 정합해버려 판정이 통째로 무력화되고, 이건 원 버그(「가장 오래된 쓰레기 점」)가 그대로 재발하는 경로다(선두 런 가드 `_LEAD_ABSURD_RATIO=50`으로 해소 — 임계는 지속 이동의 중앙값 대비 최대 이탈 `^IRX` **12배**와 실측 쓰레기 **2143·10450배** 사이에서 골랐다). **둘 다 적대적 리뷰가 잡았고 pytest·프로브는 원리적으로 못 잡았다** — 신규 4축이 *새* 판정을 검증하도록 짜였지 *옛* 판정이 무엇을 막고 있었는지는 묻지 않았기 때문이다. 실천: **판정축(필터·검증·정렬 기준)을 전역→국소로 좁히는 변경에서는 「옛 축이 *부수적으로* 배제하던 입력」을 전수 열거하고 각각을 새 축이 여전히 배제하는지 확인할 것.** 전역 축은 대개 정의역 전체에 대한 성질(여기선 `v <= 0` 배제, 극단값 일괄 배제)을 공짜로 갖는데, 국소 축은 그것을 **하나도 물려받지 않는다.** 회귀 테스트는 새 축의 목표뿐 아니라 **옛 축이 지키던 성질**도 함께 못박아야 한다(이번에 5축을 추가하며 전부 *수정 전 알고리즘 대조*로 이빨을 확인했다 — 수정 전엔 4케이스 모두 쓰레기가 100% 생존했다). **⭐ 네 번째 형태 — *동기적 「전부 날리기」*를 *비동기 「부분 갱신」*으로 바꾸면, 그 동기성이 **원리적으로 불가능하게 만들던 인터리빙**이 생긴다(task#336)**: 앞의 셋이 리로드(#283)·덮개(#285)·판정축(#303)이었다면 이건 **타이밍**이다. `api.js`의 401 핸들러는 종전에 **항상** 토큰 2개를 지우고 `location.replace('/')`를 했으므로, 「로그아웃 이후에 다른 비동기 작업이 `localStorage`에 토큰을 **다시 써넣는**」 경로가 **원리적으로 존재하지 않았다**. 반사적 단일비행 갱신이 그 경로를 만들자, 사용자가 로그아웃한 *뒤* 그 전에 걸려 있던 refresh의 성공 콜백이 새 토큰을 재기입해 **다음 새로고침에 자동 재로그인**된다(사용자는 로그아웃했다고 믿는다). 근본원인의 절반은 백엔드였다 — `auth_service.consume_refresh_token`이 `SELECT` → 별도 `DELETE` **2문**이라 **TOCTOU**가 있어, 동시 로그아웃이 그 틈에 같은 행을 지워도 **SELECT 스냅샷만 보고 성공을 반환**했다(폐기된 세션에 새 토큰 발급). 처방 2겹: **`DELETE … RETURNING` 단일 원자문** + 프론트의 **`stillCurrent` 가드**(응답이 도착했을 때 저장된 refresh 토큰이 내가 보낸 그것과 다르면 **덮지 않는다** — 로그아웃이었다면 새 토큰을 버리는 게 맞고, 다른 탭이 먼저 회전한 것이라면 그쪽 최신값을 쓰는 게 맞다). **실천: 「항상 X를 한다」를 「조건부로 X를 한다」로 바꿀 때, X가 *배제하고 있던 동시 시나리오*를 열거할 것** — diff에는 안 보인다(적대 검토만이 잡았고, auth·상태 표면에서 6사이클 연속이다). ⚠️ 부수로 **공유 헬퍼의 docstring이 코드 경로를 가둔다**: `db.query`는 「단일 SELECT」로 문서화돼 있는데 이제 변형문이 그 경로를 타므로(동작은 옳다 — `get_connection`이 정상 종료 시 커밋한다) 누군가 `query`를 읽기전용 커넥션·리드 리플리카로 돌리면 **토큰 회전이 조용히 깨진다**. 그 의존을 docstring에 명시했다.

- **⭐ 동시성 — 「단일 프로세스」가 「경합 없음」을 뜻하지 않고, 동시성 프리미티브를 넣으면 그것이 **새 단일 장애점**이 된다(task#336·#337)**: 두 방향이 한 쌍이다. **ⓐ 백엔드 — sync `def` 핸들러는 스레드풀에서 *진짜로* 병렬 실행된다.** `backend/Dockerfile`의 `CMD`에 `--workers`가 없어 uvicorn이 단일 프로세스인 것은 **워커 수**의 이야기이고, `routers/auth.py::login`·`::register`처럼 **`async def`가 아닌 `def`** 핸들러는 Starlette가 `run_in_threadpool`로 **실제 OS 스레드에서 동시에** 돌린다. 그래서 프로세스 전역 상태(`services/rate_limit.py`의 `_buckets` dict + deque)를 락 없이 「판정-후-기록」하면 **세 가지로** 깨진다 — ⓘ **과다 허용**(20스레드 강제 동시 진입에서 `allowed=13`, limit=10: 여러 스레드가 각자 `len(bucket) < limit`을 보고 아무도 아직 append하지 않았다) ⓘⓘ **`popleft`가 빈 deque에서 `IndexError`**(만료 prune의 `while bucket and now - bucket[0] > window` 조건을 둘이 함께 True로 읽는다) → **로그인이 500**(20스레드 중 19건 재현) ⓘⓘⓘ **신규 키 버킷 생성의 확인-후-대입**으로 기록 소실. 셋 다 `threading.Lock`으로 크리티컬 섹션을 묶어 해소했고, **재현은 barrier를 심어 강제**해야 했다(pytest가 스레드를 저절로 겹치게 하지 않으므로 이 클래스는 **일반 테스트로는 원리적으로 안 잡힌다**). **실천: 프로세스 전역 가변 상태를 도입하면 그 핸들러가 `def`인지 `async def`인지 먼저 보고, `def`면 락을 기본값으로 둘 것**(「단일 프로세스라 카운터가 정확하다」는 ADR 문장을 「경합이 없다」로 읽지 말 것 — 그 ADR은 워커만 다룬다). **ⓑ 프론트 — 공유 in-flight promise는 새 결합을 만든다.** `api.js`의 모듈 레벨 단일비행(`refreshInFlight`)에 타임아웃이 없으면 half-hang에서 그 값이 **영구 non-null**로 남아, 이후 발생하는 **모든 401이 로그아웃도 재시도도 못 한 채 그 promise를 영원히 기다린다.** 종전엔 401마다 동기 로그아웃이라 **어떤 요청도 남의 상태에 걸려 멈추지 않았다** — 즉 「1회만 나가게 하기」가 곧 「하나가 멈추면 전부 멈춤」이다. `AbortController` + 10초로 닫았다(⚠️ `AbortSignal.timeout`은 **Safari 16+ 전용**이라 구형 iOS PWA를 지원하는 이 앱에서는 쓰지 말 것). 그리고 in-flight는 **성공·실패 무관하게 `finally`에서 비울 것** — 안 비우면 첫 실패가 세션 내내 갱신을 막는다.

- **~~그 가드는 DB write만 막는다~~ → 해결(task#234). 남는 교훈은 "부수효과 확인"과 *은폐된 간헐성*이다(task#231→#234)**: 전에는 전체 스위트를 돌리면 `backend/data/sp500_tickers.json`·`kospi_tickers.json`이 modified로 떴다 — 실제 diff는 `ECHO`·`FDXF`·`HONA` 같은 **비실존 티커를 넣고** `CPB`·`EPAM`·`POOL`·`CAG`를 빼는 품질 열위 결과였고, 모르고 커밋하면 정적 참조 데이터가 오염됐다. **근본 원인은 `earnings.py`의 티커 7일 캐시가 그 파일을 *캐시로 겸용*(read+write)한 것**이며, task#234가 캐시를 `market_cache`(키 `sp500_tickers`·`kospi_tickers`)로 옮기고 파일을 **read-only 시드로 격하**해 write 경로를 0으로 만들었다(시드 mtime을 8일 전으로 되돌린 뒤 전체 스위트 재실행에도 write 0으로 검증). **`services/recommendation/universe.py`는 write자가 아니다** — `_SP500_PATH`를 `open()`으로 read만 한다(이 가토의 옛 서술이 잘못 지목했다). 트리거 메커니즘: 테스트에서 `_block_real_db`가 DB를 막아 `_mc_load`가 None → 캐시 미스 → `fetch_fn` → 라이브 스크레이프 → **파일 write**. 남는 두 교훈: ① **전체 스위트 실행 후 `git status`로 부수효과를 확인**하는 습관(다른 경로가 또 생길 수 있다), ② **파일 mtime을 TTL 기준으로 쓰면 덮어쓴 직후 mtime이 신선해져 증상이 스스로 숨는다** — "간헐 발생"으로 보이면 캐시 신선도 판정이 자기 자신을 갱신하는 구조인지 볼 것(task#234가 판정을 `market_cache.fetched_at`으로 옮긴 이유). 위 `_block_real_db` 가토(prod DB 오염)의 *추적 파일판*이었다.
